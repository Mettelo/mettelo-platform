import type {SupabaseClient} from '@supabase/supabase-js';

type NotifyInput={userId:string;email?:string|null;projectId?:string|null;applicationId?:string|null;type:string;title:string;body:string;actionUrl?:string|null;subject?:string;templateKey?:string;payload?:Record<string,unknown>;eventKey?:string;dedupeKey?:string|null};
type OutboxRow={id:string;user_id:string|null;recipient_email:string;template_key:string;subject:string;payload:Record<string,unknown>;status:string;attempts:number;max_attempts:number;next_attempt_at:string|null;permanent_failure:boolean};
type EmailPresentation={eyebrow:string;title:string;intro:string;cta:string;celebration?:boolean;nextSteps?:string[]};

function escapeHtml(value:string){return value.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}
function backoffMinutes(attempt:number){return Math.min(60*12,Math.max(2,Math.pow(2,Math.max(0,attempt-1))*2))}
function providerErrorPermanent(status:number){return status>=400&&status<500&&status!==408&&status!==409&&status!==425&&status!==429}
function siteUrl(){const configured=process.env.NEXT_PUBLIC_SITE_URL?.trim();if(configured)return configured.replace(/\/$/,'');const vercel=process.env.VERCEL_PROJECT_PRODUCTION_URL?.trim()||process.env.VERCEL_URL?.trim();if(vercel)return `https://${vercel.replace(/^https?:\/\//,'').replace(/\/$/,'')}`;return 'https://mettelo-platform.vercel.app'}
function absoluteUrl(value?:string|null){if(!value)return siteUrl();if(/^https?:\/\//i.test(value))return value;return `${siteUrl()}${value.startsWith('/')?value:`/${value}`}`}
function value(payload:Record<string,unknown>,key:string){const raw=payload[key];return typeof raw==='string'&&raw.trim()?raw.trim():null}
function numberValue(payload:Record<string,unknown>,key:string){const raw=payload[key];return typeof raw==='number'&&Number.isFinite(raw)?raw:null}
function stringList(payload:Record<string,unknown>,key:string){const raw=payload[key];return Array.isArray(raw)?raw.filter((item):item is string=>typeof item==='string'&&Boolean(item.trim())).map(item=>item.trim()):[]}
function firstName(name:string|null,email:string){if(name)return name.trim().split(/\s+/)[0]||'there';const local=email.split('@')[0]?.replace(/[._-]+/g,' ').trim();return local?local.split(/\s+/)[0]:'there'}
function projectLabel(payload:Record<string,unknown>){const type=value(payload,'project_type');return type==='partner'?'Partner Project':type==='open'?'Open Project':null}

function presentation(templateKey:string,body:string,payload:Record<string,unknown>,email:string):EmailPresentation{
  const name=firstName(value(payload,'recipient_name'),email);const role=value(payload,'role_title');const project=value(payload,'project_title');const verified=body.toLowerCase().includes('verified');
  if(templateKey==='career_application_submitted'||templateKey==='career_submitted')return{eyebrow:'APPLICATION RECEIVED',title:`Thanks for applying, ${name}.`,intro:`We have received your application${role?` for ${role}`:''}. Your application is now in the Mettelo recruitment queue and you will receive an email whenever its status changes.`,cta:'Track your application',nextSteps:['We will review your application against the role requirements.','If you progress, the next stage will appear in My Mettelo and by email.','Keep your profile and contact details up to date while your application is active.']};
  if(templateKey==='career_in_review')return{eyebrow:'APPLICATION UPDATE',title:`Your application is now under review, ${name}.`,intro:`The Mettelo team is reviewing your application${role?` for ${role}`:''}. No action is needed from you right now.`,cta:'View application',nextSteps:['We will contact you when a decision or next stage is available.','You can continue using Mettelo while the review is in progress.']};
  if(templateKey==='career_shortlisted')return{eyebrow:'SHORTLISTED',title:`Congratulations, ${name}. You made the shortlist.`,intro:`Your application${role?` for ${role}`:''} has progressed to the shortlist. Your experience stood out strongly enough for us to take a closer look at the next stage.`,cta:'View your progress',celebration:true,nextSteps:['Check My Mettelo for the latest status.','Watch your inbox for interview or next-stage details.','If you share this milestone, please avoid posting any private recruitment information.']};
  if(templateKey==='career_interview')return{eyebrow:'INTERVIEW STAGE',title:`Congratulations, ${name}. We would like to meet you.`,intro:`You have progressed to interview${role?` for ${role}`:''}. Review the interview details below and use My Mettelo as your source of truth if anything changes.`,cta:'View interview details',celebration:true,nextSteps:['Confirm the date and time shown in your application.','Prepare examples that show what you personally delivered and learned.','Contact us if the interview information is incomplete or you need reasonable adjustments.']};
  if(templateKey==='career_offer')return{eyebrow:'OFFER STAGE',title:`Congratulations, ${name}. You have reached the offer stage.`,intro:`Your application${role?` for ${role}`:''} has progressed to offer. This is an important milestone and the Mettelo team will provide the formal details attached to your application.`,cta:'Review your application',celebration:true,nextSteps:['Read the offer information carefully.','Ask us about anything that is unclear before accepting.','Keep a copy of the final agreed details for your records.']};
  if(templateKey==='career_hired')return{eyebrow:'WELCOME TO METTELO',title:`Welcome to the team, ${name}.`,intro:`Your application${role?` for ${role}`:''} is complete and you have been marked as hired. We are excited to see what you build and contribute with Mettelo.`,cta:'Open My Mettelo',celebration:true,nextSteps:['Look out for your onboarding information.','Complete any profile or access steps we send you.','You can share your new role publicly once your onboarding details are confirmed.']};
  if(templateKey==='career_rejected')return{eyebrow:'APPLICATION OUTCOME',title:`Thank you for your time, ${name}.`,intro:`We have completed the review of your application${role?` for ${role}`:''} and will not be progressing it further on this occasion. We appreciate the work you put into applying.`,cta:'Explore Mettelo',nextSteps:['Your Mettelo account remains available.','You can apply for future roles that match your experience.','Continue building visible proof through projects and contributions.']};
  if(templateKey==='application_approved')return{eyebrow:'PROJECT APPROVED',title:`Congratulations, ${name}. You are joining the project.`,intro:`Your application${project?` for ${project}`:''} has been approved. Your place is now moving into team formation or kickoff, depending on the current team status.`,cta:'View project status',celebration:true,nextSteps:['Check your project status and assigned team.','Complete any profile details needed by your Project Lead.','When the team starts, use the shared workspace for tasks, meetings and evidence.']};
  if(templateKey==='application_declined')return{eyebrow:'PROJECT APPLICATION UPDATE',title:`An update on your project application, ${name}.`,intro:`Your application${project?` for ${project}`:''} was not selected for this team. This does not prevent you from applying again later when the brief remains open, or joining another project.`,cta:'Find another project',nextSteps:['Review other Open Projects and Partner Projects.','Keep your profile and Proof up to date.','You can reapply later where the project remains open.']};
  if(['project_kickoff','project_force_started'].includes(templateKey))return{eyebrow:'PROJECT KICKOFF',title:`Your team is ready. Let’s build, ${name}.`,intro:`Your project team${project?` for ${project}`:''} is now active. The workspace is open for collaboration, ownership, meetings and delivery.`,cta:'Open project workspace',celebration:true,nextSteps:['Meet your team and confirm responsibilities.','Review milestones and the first assigned tasks.','Use the workspace to keep progress and evidence visible from the start.']};
  if(templateKey==='project_completed')return{eyebrow:'PROJECT COMPLETED',title:`Congratulations, ${name}. You completed the project.`,intro:`Your team has completed${project?` ${project}`:' its Mettelo project'} and the completion has been recorded. The work you completed can now strengthen your Mettelo Proof record where it has been verified.`,cta:'View your Proof',celebration:true,nextSteps:['Review the verified work attached to your profile.','Make sure your contribution titles and evidence links are clear.','If you share this milestone, link back to your Mettelo Proof or completed project when appropriate.']};
  if(templateKey==='proof_status_changed'&&verified)return{eyebrow:'PROOF VERIFIED',title:`Your work has been verified, ${name}.`,intro:`A contribution${project?` from ${project}`:''} has passed review and is now part of your verified Mettelo Proof.`,cta:'View your Proof',celebration:true,nextSteps:['Review how the contribution appears on your Proof profile.','Keep the evidence link available and accurate.','Use verified Proof when demonstrating your capability to collaborators or employers.']};
  if(templateKey==='task_assigned')return{eyebrow:'NEW PROJECT TASK',title:`You have a new task, ${name}.`,intro:`A new task${project?` has been assigned to you on ${project}`:' has been assigned to you'}. Open the workspace to review the requirement, owner and due date.`,cta:'Open task',nextSteps:['Read the task and any linked milestone.','Update the status as you work.','Attach evidence when the task is ready for review.']};
  return{eyebrow:'METTELO UPDATE',title:`Hi ${name},`,intro:body||'There is an update on your Mettelo account.',cta:'Open Mettelo'};
}

function emailHtml(item:OutboxRow){
  const payload=item.payload||{};const body=String(payload.body||'');const copy=presentation(item.template_key,body,payload,item.recipient_email);const action=absoluteUrl(value(payload,'action_url'));const project=value(payload,'project_title');const role=value(payload,'role_title');const partner=value(payload,'partner_name');const team=numberValue(payload,'team_number');const interview=value(payload,'interview_at');const verifiedCount=numberValue(payload,'verified_count');const contributions=stringList(payload,'contribution_titles');
  const details:[string,string][]=[];if(role)details.push(['Role',role]);if(project)details.push(['Project',project]);const type=projectLabel(payload);if(type)details.push(['Project type',type]);if(partner)details.push(['Partner',partner]);if(team)details.push(['Team',`Team ${team}`]);if(interview)details.push(['Interview',interview]);if(verifiedCount!==null)details.push(['Verified Proof',`${verifiedCount} contribution${verifiedCount===1?'':'s'}`]);
  const detailHtml=details.length?`<div style="margin:26px 0;padding:18px;border:1px solid #E7E1D6;border-radius:14px;background:#FBF7EE">${details.map(([label,val])=>`<div style="display:flex;gap:16px;justify-content:space-between;padding:7px 0;border-bottom:1px solid #EEE8DD"><span style="color:#6B7280;font-size:13px">${escapeHtml(label)}</span><strong style="text-align:right;color:#171B28;font-size:13px">${escapeHtml(val)}</strong></div>`).join('')}</div>`:'';
  const contributionHtml=contributions.length?`<div style="margin:24px 0"><div style="font-weight:800;color:#171B28;margin-bottom:8px">Verified work from this project</div><ul style="margin:0;padding-left:20px;color:#4F5967">${contributions.slice(0,6).map(title=>`<li style="margin:7px 0">${escapeHtml(title)}</li>`).join('')}</ul></div>`:'';
  const nextHtml=copy.nextSteps?.length?`<div style="margin:24px 0"><div style="font-weight:800;color:#171B28;margin-bottom:8px">What happens next</div><ol style="margin:0;padding-left:20px;color:#4F5967">${copy.nextSteps.map(step=>`<li style="margin:8px 0">${escapeHtml(step)}</li>`).join('')}</ol></div>`:'';
  const supporting=body&&body!==copy.intro?`<p style="margin:20px 0 0;color:#5B6472;font-size:14px;line-height:1.7">${escapeHtml(body).replace(/\n/g,'<br>')}</p>`:'';
  const milestone=copy.celebration?`<div style="margin:26px 0;padding:16px 18px;border-left:4px solid #C6892A;background:#F7EFDD;border-radius:0 12px 12px 0"><strong style="display:block;color:#6F4B16;margin-bottom:4px">A milestone worth keeping</strong><span style="color:#725B38;font-size:13px;line-height:1.6">This email records a positive step in your Mettelo journey. You are welcome to save or share the milestone, while keeping private team or recruitment information confidential.</span></div>`:'';
  return `<!doctype html><html><body style="margin:0;padding:0;background:#F3F4F6"><div style="display:none;max-height:0;overflow:hidden;color:transparent">${escapeHtml(copy.intro.slice(0,120))}</div><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#F3F4F6;padding:28px 12px"><tr><td align="center"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:680px;background:#FFFFFF;border:1px solid #E7E1D6;border-radius:18px;overflow:hidden"><tr><td style="padding:22px 28px;background:#FCFBF7;border-bottom:1px solid #E7E1D6"><a href="${escapeHtml(siteUrl())}" style="text-decoration:none;display:inline-block"><img src="${escapeHtml(siteUrl())}/mettelo-logo-dark.svg" width="132" alt="Mettelo" style="display:block;width:132px;height:auto;border:0"><span style="display:none">Mettelo</span></a></td></tr><tr><td style="padding:34px 28px 30px;font-family:Arial,Helvetica,sans-serif;color:#10131D"><div style="font-size:11px;letter-spacing:1.4px;font-weight:800;color:#8B5A17;margin-bottom:10px">${escapeHtml(copy.eyebrow)}</div><h1 style="font-family:Arial,Helvetica,sans-serif;font-size:30px;line-height:1.18;letter-spacing:-.7px;margin:0 0 16px;color:#10131D">${escapeHtml(copy.title)}</h1><p style="font-size:16px;line-height:1.75;color:#4F5967;margin:0">${escapeHtml(copy.intro)}</p>${detailHtml}${supporting}${contributionHtml}${nextHtml}${milestone}<p style="margin:28px 0 0"><a href="${escapeHtml(action)}" style="display:inline-block;background:#10131D;color:#FFFFFF;text-decoration:none;padding:13px 19px;border-radius:9px;font-weight:800;font-size:14px">${escapeHtml(copy.cta)} →</a></p><div style="margin-top:34px;padding-top:22px;border-top:1px solid #E7E1D6;color:#5B6472;font-size:14px;line-height:1.7"><p style="margin:0 0 4px">Best wishes,</p><strong style="color:#171B28">The Mettelo Team</strong><div style="font-size:12px;color:#8B5A17;margin-top:3px">Built for What’s Next.</div></div></td></tr><tr><td style="padding:18px 28px;background:#10131D;font-family:Arial,Helvetica,sans-serif;color:#BFC4CE;font-size:11px;line-height:1.6"><div>This is a transactional email about your Mettelo account, application, project or contribution.</div><div style="margin-top:6px"><a href="${escapeHtml(siteUrl())}/privacy" style="color:#E7C47F">Privacy</a> &nbsp;·&nbsp; <a href="${escapeHtml(siteUrl())}/contact" style="color:#E7C47F">Contact Mettelo</a></div></td></tr></table></td></tr></table></body></html>`;
}

async function channelPreference(db:SupabaseClient,userId:string,eventKey:string){const {data}=await db.from('notification_preferences').select('in_app_enabled,email_enabled').eq('user_id',userId).eq('event_key',eventKey).maybeSingle();return {inApp:data?.in_app_enabled!==false,email:data?.email_enabled!==false}}

async function enrichPayload(db:SupabaseClient,input:NotifyInput){
  const payload:Record<string,unknown>={...(input.payload||{})};
  try{
    const {data:profile}=await db.from('profiles').select('full_name').eq('id',input.userId).maybeSingle();if(profile?.full_name)payload.recipient_name=profile.full_name;
    if(input.projectId){
      const {data:project}=await db.from('projects').select('title,project_type,partner_name').eq('id',input.projectId).maybeSingle();if(project){payload.project_title=project.title;payload.project_type=project.project_type;payload.partner_name=project.partner_name||null}
      const {data:member}=await db.from('project_members').select('team_role,project_run_id').eq('project_id',input.projectId).eq('user_id',input.userId).maybeSingle();if(member?.team_role)payload.project_role=member.team_role;if(member?.project_run_id){const {data:run}=await db.from('project_runs').select('run_number').eq('id',member.project_run_id).maybeSingle();if(run?.run_number)payload.team_number=run.run_number}
      const {data:proof}=await db.from('contributions').select('title').eq('project_id',input.projectId).eq('user_id',input.userId).eq('verification_status','verified').order('verified_at',{ascending:false}).limit(6);payload.verified_count=proof?.length||0;payload.contribution_titles=(proof||[]).map(row=>row.title).filter(Boolean);
    }
    if(input.applicationId){const {data:application}=await db.from('project_applications').select('requested_role,project_roles(title)').eq('id',input.applicationId).maybeSingle();const role=application?.requested_role||(Array.isArray(application?.project_roles)?application?.project_roles[0]?.title:(application?.project_roles as {title?:string}|null)?.title);if(role)payload.role_title=role}
  }catch(error){console.warn('email payload enrichment skipped',error)}
  return payload;
}

export async function enqueueEmail(db:SupabaseClient,input:{userId?:string|null;to:string;templateKey:string;subject:string;body:string;actionUrl?:string|null;eventKey?:string;dedupeKey?:string|null;payload?:Record<string,unknown>}){const row={user_id:input.userId||null,recipient_email:input.to,template_key:input.templateKey,subject:input.subject,payload:{body:input.body,action_url:input.actionUrl||null,...(input.payload||{})},status:'queued',event_key:input.eventKey||input.templateKey,dedupe_key:input.dedupeKey||null,next_attempt_at:new Date().toISOString()};const {data,error}=await db.from('email_outbox').insert(row).select('*').single();if(error){if(error.code==='23505')return null;throw error}return data as OutboxRow}

export async function deliverOutboxItem(db:SupabaseClient,item:OutboxRow){if(['sent','dead_letter','cancelled'].includes(item.status))return {status:item.status};const apiKey=process.env.RESEND_API_KEY;const from=process.env.METTELO_EMAIL_FROM;const attempt=(item.attempts||0)+1;const now=new Date().toISOString();await db.from('email_outbox').update({status:'sending',last_attempt_at:now,updated_at:now}).eq('id',item.id);await db.from('email_delivery_attempts').insert({outbox_id:item.id,attempt_number:attempt,status:'sending'});if(!apiKey||!from){const error='Email provider is not configured.';await recordFailure(db,item,attempt,error,503,false);return {status:'failed',error}}try{const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${apiKey}`,'content-type':'application/json'},body:JSON.stringify({from,to:item.recipient_email,subject:item.subject,html:emailHtml(item)})});const raw=await response.text();let providerId:string|null=null;try{providerId=(JSON.parse(raw) as {id?:string}).id||null}catch{}if(!response.ok){const permanent=providerErrorPermanent(response.status);const message=`Email provider returned ${response.status}${raw?`: ${raw.slice(0,500)}`:''}`;await recordFailure(db,item,attempt,message,response.status,permanent);return {status:permanent?'dead_letter':'failed',error:message}}await db.from('email_outbox').update({status:'sent',attempts:attempt,sent_at:now,last_error:null,provider_message_id:providerId,next_attempt_at:null,updated_at:now}).eq('id',item.id);await db.from('email_delivery_attempts').update({status:'sent',provider_message_id:providerId}).eq('outbox_id',item.id).eq('attempt_number',attempt);return {status:'sent',providerId}}catch(error){const message=error instanceof Error?error.message:'Email delivery failed';await recordFailure(db,item,attempt,message,null,false);return {status:'failed',error:message}}}

async function recordFailure(db:SupabaseClient,item:OutboxRow,attempt:number,message:string,httpStatus:number|null,permanent:boolean){const exhausted=attempt>=Math.max(1,item.max_attempts||5);const dead=permanent||exhausted;const next=dead?null:new Date(Date.now()+backoffMinutes(attempt)*60_000).toISOString();await db.from('email_outbox').update({status:dead?'dead_letter':'retrying',attempts:attempt,last_error:message,permanent_failure:permanent,dead_letter_at:dead?new Date().toISOString():null,next_attempt_at:next,updated_at:new Date().toISOString()}).eq('id',item.id);await db.from('email_delivery_attempts').update({status:permanent?'permanent_failure':'failed',error_message:message,http_status:httpStatus}).eq('outbox_id',item.id).eq('attempt_number',attempt)}

export async function processEmailQueue(db:SupabaseClient,limit=25){const now=new Date().toISOString();const {data}=await db.from('email_outbox').select('*').in('status',['queued','retrying','failed']).or(`next_attempt_at.is.null,next_attempt_at.lte.${now}`).order('created_at',{ascending:true}).limit(limit);const results=[];for(const item of (data||[]) as OutboxRow[])results.push({id:item.id,...await deliverOutboxItem(db,item)});return results}

export async function notifyUser(db:SupabaseClient,input:NotifyInput){const eventKey=input.eventKey||input.type;const dedupe=input.dedupeKey||null;const preference=await channelPreference(db,input.userId,eventKey);if(preference.inApp){const notification={user_id:input.userId,project_id:input.projectId||null,application_id:input.applicationId||null,type:input.type,title:input.title,body:input.body,action_url:input.actionUrl||null,event_key:eventKey,dedupe_key:dedupe,channel:input.email&&preference.email?'email_and_in_app':'in_app'};const {error}=await db.from('notifications').insert(notification);if(error&&error.code!=='23505')throw error}if(!input.email||!preference.email)return;const enriched=await enrichPayload(db,input);const outbox=await enqueueEmail(db,{userId:input.userId,to:input.email,templateKey:input.templateKey||input.type,subject:input.subject||input.title,body:input.body,actionUrl:input.actionUrl,eventKey,dedupeKey:dedupe?`${input.userId}:${dedupe}`:null,payload:enriched});if(outbox)await deliverOutboxItem(db,outbox)}

export async function notifyAdmins(db:SupabaseClient,input:Omit<NotifyInput,'userId'|'email'>){const {data}=await db.auth.admin.listUsers({page:1,perPage:1000});const admins=data.users.filter(user=>user.app_metadata?.role==='admin');for(const admin of admins)await notifyUser(db,{...input,userId:admin.id,email:admin.email||null,dedupeKey:input.dedupeKey?`${admin.id}:${input.dedupeKey}`:null})}
