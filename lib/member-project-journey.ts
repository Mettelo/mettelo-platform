import {projectAcceptsApplications as lifecycleAcceptsApplications,projectApplicationDeadlinePassed} from './project-lifecycle-policy';

export type MemberProjectState='open_eligible'|'register_interest'|'application_submitted'|'application_action_required'|'application_in_review'|'team_forming'|'confirmed'|'active'|'completed'|'closed'|'ineligible'|'full'|'cancelled';
export type MemberProjectQualificationReason='ELIGIBLE'|'PROFILE_INCOMPLETE'|'INTEREST_EXISTS'|'PROJECT_CLOSED'|'CAPACITY_FULL'|'ALREADY_PARTICIPATING'|'DEADLINE_PASSED'|'CAPACITY_UNKNOWN'|'PROJECT_CANCELLED'|'PROJECT_COMPLETED';
export type MemberProjectQualification={state:MemberProjectState;reason:MemberProjectQualificationReason;eligible:boolean};
type ProjectInput={status:string;project_type?:string|null;applications_open?:boolean|null;application_deadline?:string|null;visibility?:string|null};
type ApplicationInput={id:string;status:string;project_run_id?:string|null}|null|undefined;
type MembershipInput={membership_status:string;project_run_id?:string|null}|null|undefined;
type RunInput={status:string}|null|undefined;
export type ResolveMemberProjectStateInput={project:ProjectInput;application?:ApplicationInput;membership?:MembershipInput;run?:RunInput;applicationReady:boolean;capacityAvailable?:boolean;capacityKnown?:boolean;hasAvailableRole?:boolean;roleAvailabilityKnown?:boolean;now?:number};
const applicationClosed=new Set(['declined','withdrawn']);const applicationInReview=new Set(['in_review','shortlisted']);const applicationForming=new Set(['approved','accepted','waiting_for_team']);
export function projectAcceptsApplications(project:ProjectInput,now=Date.now()){const projectType=project.project_type||'open';if(projectApplicationDeadlinePassed({project_type:projectType,application_deadline:project.application_deadline},now))return false;return lifecycleAcceptsApplications({project_type:projectType,status:project.status,applications_open:project.applications_open??false,visibility:project.visibility||'public'})}
export function resolveMemberProjectQualification(input:ResolveMemberProjectStateInput):MemberProjectQualification{
 const {project,application,membership,run}=input;const now=input.now??Date.now();
 if(project.status==='cancelled'||run?.status==='cancelled')return{state:'cancelled',reason:'PROJECT_CANCELLED',eligible:false};
 if(membership){if(membership.membership_status==='completed'||project.status==='completed'||run?.status==='completed')return{state:'completed',reason:'PROJECT_COMPLETED',eligible:false};if(membership.membership_status==='active'&&run&&['active','review','paused'].includes(run.status))return{state:'active',reason:'ALREADY_PARTICIPATING',eligible:false};if(['waiting','active'].includes(membership.membership_status))return{state:'confirmed',reason:'ALREADY_PARTICIPATING',eligible:false}}
 if(application&&!applicationClosed.has(application.status)){if(application.status==='team_complete')return{state:'confirmed',reason:'INTEREST_EXISTS',eligible:false};if(applicationForming.has(application.status))return{state:'team_forming',reason:'INTEREST_EXISTS',eligible:false};if(application.status==='action_required'||application.status==='needs_changes')return{state:'application_action_required',reason:'INTEREST_EXISTS',eligible:false};if(applicationInReview.has(application.status))return{state:'application_in_review',reason:'INTEREST_EXISTS',eligible:false};return{state:'application_submitted',reason:'INTEREST_EXISTS',eligible:false}}
 if(project.status==='completed')return{state:'completed',reason:'PROJECT_COMPLETED',eligible:false};
 if(projectApplicationDeadlinePassed({project_type:project.project_type||'open',application_deadline:project.application_deadline},now))return{state:'closed',reason:'DEADLINE_PASSED',eligible:false};
 const accepting=lifecycleAcceptsApplications({project_type:project.project_type||'open',status:project.status,applications_open:project.applications_open??false,visibility:project.visibility||'public'});
 if(project.status==='pilot'&&!accepting)return{state:'register_interest',reason:'PROJECT_CLOSED',eligible:false};if(!accepting)return{state:'closed',reason:'PROJECT_CLOSED',eligible:false};if(!input.applicationReady)return{state:'ineligible',reason:'PROFILE_INCOMPLETE',eligible:false};
 // Capacity describes placement, not whether an open project may collect interest.
 return{state:'open_eligible',reason:'ELIGIBLE',eligible:true};
}
export function resolveMemberProjectState(input:ResolveMemberProjectStateInput):MemberProjectState{return resolveMemberProjectQualification(input).state}
export function memberProjectPrimaryAction(state:MemberProjectState,projectId:string){if(state==='open_eligible'||state==='full')return{label:'Submit Interest',href:`/member/discover/${projectId}/apply`};if(['application_submitted','application_action_required','application_in_review','team_forming'].includes(state))return{label:'View interest',href:'/member/applications'};if(state==='confirmed'||state==='active')return{label:'Open in Projects',href:'/member/projects'};if(state==='completed')return{label:'View in Projects',href:'/member/projects?state=completed'};return null}
export function memberProjectCatalogueAction(state:MemberProjectState,projectId:string){if(['application_submitted','application_action_required','application_in_review','team_forming'].includes(state))return{label:'View interest',href:'/member/applications'};if(state==='confirmed'||state==='active')return{label:'Open in Projects',href:'/member/projects'};if(state==='completed')return{label:'View in Projects',href:'/member/projects?state=completed'};return{label:'View project',href:`/member/discover/${projectId}`}}
export function memberProjectStateLabel(state:MemberProjectState){const labels:Record<MemberProjectState,string>={open_eligible:'Applications open',register_interest:'Interest not yet open',application_submitted:'Interest submitted',application_action_required:'Action required',application_in_review:'Interest in review',team_forming:'Place confirmed · team forming',confirmed:'Your place is confirmed',active:'Project in progress',completed:'Completed',closed:'Applications closed',ineligible:'Profile update required',full:'Applications open',cancelled:'Project cancelled'};return labels[state]}
export function memberProjectStateCopy(state:MemberProjectState){const copy:Record<MemberProjectState,string>={open_eligible:'This project is accepting interest. Capacity is assessed separately when places are allocated.',register_interest:'This project is not currently accepting interest.',application_submitted:'Your interest has already been submitted. Use Applications to track its current state.',application_action_required:'Your existing interest needs an update. Continue in Applications to see the required action.',application_in_review:'Your interest is being reviewed. Continue in Applications for status updates.',team_forming:'Your place is confirmed while Mettelo forms the delivery team.',confirmed:'Your place is confirmed. Projects is now the source of truth for this work.',active:'This project is in progress for you. Continue from Projects.',completed:'This project is part of your completed project history in Projects.',closed:'The project is no longer accepting interest.',ineligible:'Complete the required profile information before you can submit interest.',full:'The current place allocation is full, but the application window remains open for interest.',cancelled:'This project has been cancelled and is not accepting interest.'};return copy[state]}
