import {redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import AdminProjectManager from '@/components/AdminProjectManager';
import AdminProjectCreateButton from '@/components/AdminProjectCreateButton';
export const dynamic='force-dynamic';
export default async function ProjectsPage(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect('/signin');if(user.app_metadata?.role!=='admin')redirect('/member');return <section className="section softSection"><div className="shell"><div className="adminPageHeader"><div><div className="eyebrow">Admin / Projects / Projects</div><h1>Projects</h1><p>Search, filter and manage every Mettelo project from one operational table.</p></div><AdminProjectCreateButton/></div><AdminProjectManager/><style>{`.adminPageHeader{display:flex;align-items:flex-start;justify-content:space-between;gap:24px;margin-bottom:18px}.adminPageHeader h1{margin:4px 0 6px;font-size:clamp(2rem,4vw,3.3rem)}.adminPageHeader p{margin:0;color:#5b6470}.adminPageHeader>.button{min-height:44px;flex:none}@media(max-width:700px){.adminPageHeader{display:grid}.adminPageHeader>.button{width:100%}}`}</style></div></section>}
