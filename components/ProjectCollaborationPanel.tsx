'use client';

import {FormEvent,useState} from 'react';
import ProjectMessagePanel from '@/components/ProjectMessagePanel';

type TeamMember={id:string;name:string;headline:string|null;role:string};type Discussion={id:string;author_user_id:string;message_type:string;body:string;mentioned_user_ids:string[];created_at:string;updated_at?:string;edited_at?:string|null;deleted_at?:string|null;pinned_at?:string|null};type Resource={id:string;title:string;resource_type:string;url:string;description:string|null;created_at:string};type Meeting={id:string;title:string;purpose:string|null;platform:string;starts_at:string;ends_at:string|null;join_url:string;status:string};type Slot={id:string;starts_at:string;ends_at:string;meeting_url:string|null;location_label:string|null};type Presentation={id:string;status:string;meeting_url:string|null;deck_url:string|null;reviewer_notes:string|null;presented_at:string|null}|null;type Readiness={ready:boolean;required_milestones:number;completed_milestones:number;required_tasks:number;completed_tasks:number;project_members_requiring_proof:number;members_with_verified_proof:number;pending_contributions:number;presentation_required:boolean;presentation_status:string};
type Props={projectId:string;projectRunId?:string|null;currentUserId:string;projectStatus:string;team:TeamMember[];discussions:Discussion[];resources:Resource[];meetings:Meeting[];availableSlots:Slot[];presentation:Presentation;presenterIds:string[];presentationRequired:boolean;canLead:boolean;canReview:boolean;isAdmin:boolean;readiness:Readiness};

export default function ProjectCollaborationPanel(props:Props){
 const [working,setWorking]=useState('');const [message,setMessage]=useState('');
 async function send(action:string,payload:Record<string,unknown>,form?:HTMLFormElement){setWorking(action);setMessage('');try{const response=await fetch('/api/project-collaboration',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,project_id:props.projectId,project_run_id:props.projectRunId||null,...payload})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'Unable to update this project.');setMessage(body.message||'Saved.');form?.reset();setTimeout(()=>window.location.reload(),350)}catch(error){setMessage(error instanceof Error?error.message:'Unable to update this project.')}finally{setWorking('')}}
 function values(form:HTMLFormElement){return Object.fromEntries(new FormData(form).entries())}function resource(event:FormEvent<HTMLFormElement>){event.preventDefault();void send('resource',values(event.currentTarget),event.currentTarget)}function meeting(event:FormEvent<HTMLFormElement>){event.preventDefault();void send('meeting',values(event.currentTarget),event.currentTarget)}function book(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,fd=new FormData(form);void send('book_presentation',{slot_id:fd.get('slot_id'),deck_url:fd.get('deck_url'),presenter_ids:fd.getAll('presenters')},form)}
 const checks=[['Milestones',props.readiness.completed_milestones,props.readiness.required_milestones,props.readiness.required_milestones>0&&props.readiness.completed_milestones===props.readiness.required_milestones],['Tasks',props.readiness.completed_tasks,props.readiness.required_tasks,props.readiness.completed_tasks===props.readiness.required_tasks],['Verified Proof',props.readiness.members_with_verified_proof,props.readiness.project_members_requiring_proof,props.readiness.members_with_verified_proof===props.readiness.project_members_requiring_proof&&props.readiness.pending_contributions===0]] as const;
 return <div className="collaborationStack">
  <section className="panel" id="team"><div className="panelHead"><div><span className="cardNumber">TEAM</span><h3 style={{marginTop:8}}>People working on this project</h3></div><span className="chip">{props.team.length} MEMBERS</span></div><div className="teamGrid">{props.team.length?props.team.map(member=><div className="teamMember" key={member.id}><div className="personAvatar">{member.name.split(' ').map(part=>part[0]).join('').slice(0,2).toUpperCase()}</div><div><strong>{member.name}</strong><span>{member.role.replaceAll('_',' ')}</span>{member.headline&&<small>{member.headline}</small>}</div></div>):<div className="emptyState"><h3>No team members yet.</h3><p>Approved members will appear here.</p></div>}</div></section>
  <ProjectMessagePanel projectId={props.projectId} projectRunId={props.projectRunId} currentUserId={props.currentUserId} team={props.team} initialMessages={props.discussions} canLead={props.isAdmin||props.team.some(member=>member.id===props.currentUserId&&member.role==='project_lead')} readOnly={props.isAdmin&&!props.team.some(member=>member.id===props.currentUserId)}/>
  <div className="dashboardGrid"><section className="panel" id="resources"><div className="panelHead"><div><span className="cardNumber">RESOURCES</span><h3 style={{marginTop:8}}>Files and working links</h3></div></div>{props.resources.length?<div className="resourceList">{props.resources.map(item=><a className="resourceItem" href={item.url} target="_blank" rel="noopener noreferrer" key={item.id}><div><strong>{item.title}</strong><small>{item.resource_type.replaceAll('_',' ')}{item.description?` · ${item.description}`:''}</small></div><span>↗</span></a>)}</div>:<div className="emptyState"><h3>No resources yet.</h3><p>Add the repository, dataset, dashboard or shared folder the team needs.</p></div>}{!props.isAdmin&&<details className="workspaceComposer"><summary>Add resource</summary><form className="formCard compactForm" onSubmit={resource}><label>Title</label><input name="title" required/><label>Type</label><select name="resource_type" defaultValue="documentation"><option value="github">GitHub</option><option value="google_drive">Google Drive</option><option value="onedrive">OneDrive</option><option value="figma">Figma</option><option value="canva">Canva</option><option value="power_bi">Power BI</option><option value="tableau">Tableau</option><option value="looker">Looker</option><option value="dataset">Dataset</option><option value="documentation">Documentation</option><option value="other">Other</option></select><label>URL</label><input name="url" type="url" required placeholder="https://"/><label>Note</label><input name="description"/><button className="button dark" disabled={working==='resource'}>{working==='resource'?'Saving…':'Add resource'}</button></form></details>}</section>
  <section className="panel" id="meetings"><div className="panelHead"><div><span className="cardNumber">MEETINGS</span><h3 style={{marginTop:8}}>Team sessions</h3></div></div>{props.meetings.length?<div className="meetingList">{props.meetings.map(item=><article className="meetingItem" key={item.id}><div><span className="chip">{item.platform.replaceAll('_',' ').toUpperCase()}</span><strong>{item.title}</strong><small>{new Date(item.starts_at).toLocaleString('en-GB')}{item.purpose?` · ${item.purpose}`:''}</small></div><a className="button ghost" href={item.join_url} target="_blank" rel="noopener noreferrer">Join →</a></article>)}</div>:<div className="emptyState"><h3>No meetings scheduled.</h3><p>The Project Lead can add the next team session here.</p></div>}{props.canLead&&<details className="workspaceComposer"><summary>Schedule meeting</summary><form className="formCard compactForm" onSubmit={meeting}><label>Title</label><input name="title" required/><label>Purpose</label><textarea name="purpose"/><div className="fieldRow"><div><label>Platform</label><select name="platform" defaultValue="google_meet"><option value="google_meet">Google Meet</option><option value="microsoft_teams">Microsoft Teams</option><option value="zoom">Zoom</option><option value="other">Other</option></select></div><div><label>Starts</label><input name="starts_at" type="datetime-local" required/></div></div><label>Joining URL</label><input name="join_url" type="url" required placeholder="https://"/><button className="button dark" disabled={working==='meeting'}>{working==='meeting'?'Saving…':'Schedule meeting'}</button></form></details>}</section></div>
  <section className="panel" id="presentation"><div className="panelHead"><div><span className="cardNumber">PRESENTATION</span><h3 style={{marginTop:8}}>Final project presentation</h3></div><span className={`chip ${props.presentation?.status==='verified'?'green':''}`}>{props.presentationRequired?(props.presentation?.status||'not_booked').replaceAll('_',' ').toUpperCase():'NOT REQUIRED'}</span></div>{!props.presentationRequired?<p className="panelNote">This project does not require a final presentation.</p>:<>{props.presentation?<div className="presentationSummary"><p><strong>Status:</strong> {props.presentation.status.replaceAll('_',' ')}</p>{props.presentation.deck_url&&<a className="linkArrow" href={props.presentation.deck_url} target="_blank" rel="noopener noreferrer">Open presentation →</a>}{props.presentation.meeting_url&&<a className="linkArrow" href={props.presentation.meeting_url} target="_blank" rel="noopener noreferrer">Open meeting room →</a>}{props.presentation.reviewer_notes&&<p><strong>Feedback:</strong> {props.presentation.reviewer_notes}</p>}</div>:<div className="emptyState"><h3>No presentation booked.</h3><p>The Project Lead can choose an available slot.</p></div>}{props.canLead&&(!props.presentation||['not_booked','changes_required'].includes(props.presentation.status))&&<form className="formCard compactForm" onSubmit={book}><label>Available slot</label><select name="slot_id" required defaultValue=""><option value="" disabled>Select a slot</option>{props.availableSlots.map(slot=><option value={slot.id} key={slot.id}>{new Date(slot.starts_at).toLocaleString('en-GB')} · {slot.location_label||'Online'}</option>)}</select><label>Presentation URL</label><input name="deck_url" type="url" placeholder="https://"/><label>Presenters</label><select name="presenters" multiple size={Math.min(5,Math.max(2,props.team.length))}>{props.team.map(member=><option value={member.id} key={member.id}>{member.name}</option>)}</select><button className="button dark" disabled={working==='book_presentation'||!props.availableSlots.length}>{working==='book_presentation'?'Booking…':'Book presentation'}</button></form>}{props.canLead&&props.presentation?.status==='booked'&&<button className="button dark" onClick={()=>void send('presentation_status',{status:'presented'})} disabled={working==='presentation_status'}>Mark as presented</button>}{props.canReview&&props.presentation&&['presented','changes_required'].includes(props.presentation.status)&&<ReviewBox working={working==='presentation_status'} onSubmit={(status,notes)=>void send('presentation_status',{status,reviewer_notes:notes})}/>}</>}</section>
  <section className="panel completionPanel" id="completion"><div className="panelHead"><div><span className="cardNumber">COMPLETION</span><h3 style={{marginTop:8}}>Is this team ready to finish?</h3></div><span className={`chip ${props.readiness.ready?'green':''}`}>{props.readiness.ready?'READY':'NOT READY'}</span></div><div className="completionChecks">{checks.map(([label,done,total,ok])=><div className="completionCheck" key={label}><span className={ok?'checkOk':'checkPending'}>{ok?'✓':'○'}</span><div><strong>{label}</strong><small>{done} / {total}</small></div></div>)}{props.presentationRequired&&<div className="completionCheck"><span className={props.readiness.presentation_status==='verified'?'checkOk':'checkPending'}>{props.readiness.presentation_status==='verified'?'✓':'○'}</span><div><strong>Presentation</strong><small>{props.readiness.presentation_status.replaceAll('_',' ')}</small></div></div>}</div><p className="panelNote">The Project Lead sends completed work for review. Admin confirms final completion.</p>{props.canLead&&props.readiness.ready&&props.projectStatus!=='completed'&&<button className="button dark" onClick={()=>void send('submit_completion',{})} disabled={working==='submit_completion'}>{working==='submit_completion'?'Saving…':props.isAdmin?'Approve completion':'Send for completion review'}</button>}</section>
  <div className="formStatus" role="status" aria-live="polite">{message}</div>
 </div>;
}
function ReviewBox({working,onSubmit}:{working:boolean;onSubmit:(status:string,notes:string)=>void}){const [notes,setNotes]=useState('');return <div className="presentationReview"><label htmlFor="presentation-notes">Feedback</label><textarea id="presentation-notes" value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Add feedback for the team."/><div className="reviewActions"><button className="button ghost" type="button" disabled={working||!notes.trim()} onClick={()=>onSubmit('changes_required',notes)}>Request changes</button><button className="button dark" type="button" disabled={working} onClick={()=>onSubmit('verified',notes)}>Approve presentation</button></div></div>}
