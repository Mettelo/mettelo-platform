import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');const checks=[];const expect=(name,ok)=>checks.push({name,ok:Boolean(ok)});
const org=read('app/organisations/page.tsx'),partnership=read('app/partnership/page.tsx'),feedback=read('app/feedback/page.tsx'),adminPage=read('app/admin/intake/page.tsx'),admin=read('components/AdminIntakeQueue.tsx'),api=read('app/api/admin/intake/route.ts'),docs=read('app/api/admin/intake/documents/route.ts'),migration=read('supabase/migrations/20260816104500_phase8_organisation_partnership_support.sql'),newsletter=read('app/newsletter/page.tsx'),newsletterApi=read('app/api/newsletter/route.ts'),prefs=read('app/newsletter/preferences/page.tsx'),prefsApi=read('app/api/newsletter/preferences/route.ts');
expect('Organisation journey explains enquiry review and relevant follow-up',org.includes('Understand the options')&&org.includes('Mettelo reviews the enquiry')&&org.includes('Agree a relevant next step')&&org.includes('After you enquire')&&org.includes('It does not guarantee publication, project acceptance, hiring support or partnership.'));
expect('Partnership intake captures type and operational context',partnership.includes('partnershipType')&&partnership.includes('objective')&&partnership.includes('contribution'));
expect('Partnership CRM has all programme stages',['new','reviewing','qualified','discovery_call','proposal','active','closed'].every(stage=>migration.includes(`'${stage}'`))&&admin.includes('Discovery call'));
expect('Admin intake supports owner, notes, communication and follow-up',api.includes("action==='assign'")&&api.includes("action==='note'")&&api.includes("action==='communicate'")&&api.includes("action==='follow_up'"));
expect('Partnership proposal attachments are private PDFs',docs.includes("'intake-proposals'")&&docs.includes("file.type!=='application/pdf'")&&migration.includes("false,10485760,array['application/pdf']"));
expect('Intake preserves permanent notes and workflow history',migration.includes('form_submission_notes')&&migration.includes('form_submission_history')&&adminPage.includes('initialHistory'));
expect('Contact and feedback expose age/SLA and resolution',admin.includes('Age / SLA')&&admin.includes('over 48h')||admin.includes('Over 48h'));expect('Resolution can optionally notify submitter',api.includes("action==='resolve'")&&api.includes('notify_submitter')&&admin.includes('Notify submitter'));
expect('Feedback already captures category/area',feedback.includes('feedback-area')&&feedback.includes('Area *'));
expect('Newsletter subscription exposes marketing topic preferences',newsletter.includes('Choose marketing topics')&&newsletterApi.includes('marketing_preferences'));
expect('Newsletter supports secure preferences and unsubscribe',prefs.includes('Unsubscribe from all marketing')&&prefsApi.includes("status:'unsubscribed'")&&migration.includes('unsubscribe_token'));
expect('Newsletter controls cannot suppress transactional messages',prefs.includes('Transactional account, security, recruitment and project communications')&&migration.includes('Transactional account, project, recruitment and security communications'));
expect('Phase 8 surfaces specify desktop, tablet and mobile rules',[org,admin,prefs,newsletter].every(text=>text.includes('min-width:1025px')&&text.includes('min-width:481px')&&text.includes('max-width:480px')));
expect('Phase 8 interactive UI includes focus-visible treatment',admin.includes(':focus-visible')&&prefs.includes(':focus-visible')&&newsletter.includes(':focus-visible'));
const failed=checks.filter(check=>!check.ok);for(const check of checks)console.log(`${check.ok?'PASS':'FAIL'} ${check.name}`);if(failed.length)process.exit(1);console.log(`Phase 8 organisation/support audit passed (${checks.length} checks).`);
