import {notFound} from 'next/navigation';
const widths=[320,375,390,430,768,1024,1280,1440];
export default function PhaseFiveResponsiveGate(){if(process.env.VERCEL_ENV==='production')notFound();return <main style={{padding:20,background:'#e9ecf1'}}><h1 style={{fontSize:28}}>Phase 5 responsive release check</h1><p>Project Architect proposal and governed-work hub at every required viewport width.</p>{[['New governed project','/dev/phase-5-architect-form'],['Architect project hub','/dev/phase-5-architect-hub']].map(([label,src])=><section key={src} style={{marginTop:32}}><h2 style={{fontSize:22}}>{label}</h2><div style={{display:'grid',gap:24}}>{widths.map(width=><section key={`${src}-${width}`} data-gate-width={width} data-gate-page={src} style={{width:'max-content',maxWidth:'none'}}><h3 style={{fontSize:18}}>{width}px</h3><iframe title={`${label} at ${width}px`} data-width={width} src={src} width={width} height="1100" style={{display:'block',width,height:1100,border:'2px solid #10131d',background:'#fff'}}/></section>)}</div></section>)}</main>}
