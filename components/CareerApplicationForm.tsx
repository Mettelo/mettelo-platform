'use client';

import {useEffect,useMemo,useRef,useState} from 'react';
import {normaliseOptionalWebUrl} from '@/lib/normalise-web-url';

type Question={id?:string;label?:string;required?:boolean}|string;
type InitialValues={full_name?:string;email?:string;phone?:string;location?:string;linkedin_url?:string;portfolio_url?:string;work_authorisation?:string};
type ReviewData={name:string;email:string;location:string;links:{label:string;value:string}[];workAuth:string;motivation:string;experience:string;questions:{label:string;answer:string}[];fileName:string};

function normaliseQuestion(item:Question,index:number){if(typeof item==='string')return{id:`q${index+1}`,label:item,required:true};return{id:item.id||`q${index+1}`,label:item.label||`Question ${index+1}`,required:item.required!==false};}

function normaliseUrlFields(form:HTMLFormElement){
  for(const name of ['linkedin_url','portfolio_url']){
    const field=form.elements.namedItem(name);
    if(!(field instanceof HTMLInputElement))continue;
    const result=normaliseOptionalWebUrl(field.value);
    if(!result.ok){
      field.setCustomValidity('Enter a valid web address with a domain, such as linkedin.com/in/name.');
      field.focus();
      field.reportValidity();
      return false;
    }
    field.setCustomValidity('');
    if(field.value!==result.value){
      field.value=result.value;
      field.dispatchEvent(new Event('input',{bubbles:true}));
    }
  }
  return true;
}

export default function CareerApplicationForm({roleId,roleTitle,questions=[],initialValues={}}:{roleId:string;roleTitle:string;questions?:Question[];initialValues?:InitialValues}){
  const formRef=useRef<HTMLFormElement>(null);const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);const [reviewing,setReviewing]=useState(false);const [review,setReview]=useState<ReviewData|null>(null);const [draftState,setDraftState]=useState('');const [uploadProgress,setUploadProgress]=useState<number|null>(null);const normalised=useMemo(()=>questions.map(normaliseQuestion),[questions]);const storageKey=`mettelo:career-draft:${roleId}`;

  useEffect(()=>{const form=formRef.current;if(!form)return;try{const saved=localStorage.getItem(storageKey);if(saved){const values=JSON.parse(saved) as Record<string,string>;for(const [name,value] of Object.entries(values)){const field=form.elements.namedItem(name);if(field instanceof HTMLInputElement||field instanceof HTMLTextAreaElement)field.value=value;}setDraftState('Draft restored from this device.');}}catch{}
    let timer:number|undefined;const save=()=>{window.clearTimeout(timer);timer=window.setTimeout(()=>{const values:Record<string,string>={};const data=new FormData(form);for(const [key,value] of data.entries()){if(key==='cv'||key==='role_id')continue;if(typeof value==='string')values[key]=value;}try{localStorage.setItem(storageKey,JSON.stringify(values));setDraftState('Draft saved automatically.');}catch{}},350)};form.addEventListener('input',save);return()=>{form.removeEventListener('input',save);window.clearTimeout(timer)};
  },[storageKey]);

  function buildReview(){const form=formRef.current;if(!form)return;setStatus('');if(!form.reportValidity()||!normaliseUrlFields(form))return;const data=new FormData(form);const file=data.get('cv');const q=normalised.map(item=>({label:item.label,answer:String(data.get(`question_${item.id}`)||'').trim()}));const links=[{label:'LinkedIn',value:String(data.get('linkedin_url')||'')},{label:'Portfolio / GitHub',value:String(data.get('portfolio_url')||'')}].filter(item=>Boolean(item.value));setReview({name:String(data.get('full_name')||''),email:String(data.get('email')||''),location:String(data.get('location')||''),links,workAuth:String(data.get('work_authorisation')||''),motivation:String(data.get('motivation')||''),experience:String(data.get('relevant_experience')||''),questions:q,fileName:file instanceof File?file.name:''});setReviewing(true);requestAnimationFrame(()=>document.getElementById('career-review-heading')?.focus());}

  async function submit(){const form=formRef.current;if(!form||busy)return;setBusy(true);setStatus('');setUploadProgress(0);const data=new FormData(form);data.set('role_id',roleId);
    try{await new Promise<void>((resolve,reject)=>{const xhr=new XMLHttpRequest();xhr.open('POST','/api/careers/apply');xhr.responseType='json';xhr.upload.onprogress=event=>{if(event.lengthComputable)setUploadProgress(Math.round(event.loaded/event.total*100));};xhr.onerror=()=>reject(new Error('The application could not be uploaded. Check your connection and try again.'));xhr.onload=()=>{const body=xhr.response||{};if(xhr.status<200||xhr.status>=300){reject(new Error(body.error||'Unable to submit application.'));return;}resolve();};xhr.send(data);});try{localStorage.removeItem(storageKey);}catch{}window.location.assign(`/submitted?type=career_application&role=${encodeURIComponent(roleTitle)}`);}catch(error){setStatus(error instanceof Error?error.message:'Unable to submit application.');setBusy(false);setUploadProgress(null);}
  }

  return <form ref={formRef} className="careerApplyForm" onSubmit={event=>{event.preventDefault();buildReview();}} noValidate={false}>
    <div hidden={reviewing} aria-hidden={reviewing}>
      <div className="careerDraftBar" role="status" aria-live="polite"><span>Application draft</span><strong>{draftState||'Your answers save automatically on this device.'}</strong></div>
      <div className="careerFormSection"><div className="careerFormHeading"><span>01</span><div><small>Your details</small><h3>Tell us who you are</h3></div></div><div className="careerFormGrid"><label>Full name<input name="full_name" required maxLength={140} autoComplete="name" defaultValue={initialValues.full_name||''} placeholder="Your full name"/></label><label>Email<input name="email" type="email" required autoComplete="email" defaultValue={initialValues.email||''} placeholder="you@example.com"/></label><label>Phone <small>Optional</small><input name="phone" maxLength={50} autoComplete="tel" defaultValue={initialValues.phone||''} placeholder="Include country code"/></label><label>Current location<input name="location" maxLength={160} autoComplete="address-level2" defaultValue={initialValues.location||''} placeholder="City, country"/></label></div></div>
      <div className="careerFormSection"><div className="careerFormHeading"><span>02</span><div><small>Your work</small><h3>Show us what you’ve done</h3></div></div><div className="careerFormGrid"><label>LinkedIn URL <small>Optional · https:// is added automatically</small><input name="linkedin_url" type="text" inputMode="url" autoCapitalize="none" autoCorrect="off" spellCheck={false} maxLength={800} defaultValue={initialValues.linkedin_url||''} placeholder="linkedin.com/in/your-name" onInput={event=>event.currentTarget.setCustomValidity('')}/></label><label>Portfolio / GitHub URL <small>Optional · https:// is added automatically</small><input name="portfolio_url" type="text" inputMode="url" autoCapitalize="none" autoCorrect="off" spellCheck={false} maxLength={800} defaultValue={initialValues.portfolio_url||''} placeholder="yourportfolio.com or github.com/you" onInput={event=>event.currentTarget.setCustomValidity('')}/></label><label className="careerFull">Work eligibility / authorisation <small>Optional for volunteer roles</small><input name="work_authorisation" maxLength={300} defaultValue={initialValues.work_authorisation||''} placeholder="For example: based in Nigeria / UK unrestricted / requires sponsorship"/></label><label className="careerFull careerFile">CV <small>PDF or DOCX · max 5MB · selected files are not stored in your browser draft</small><input name="cv" type="file" required accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document"/></label></div></div>
      <div className="careerFormSection"><div className="careerFormHeading"><span>03</span><div><small>Your application</small><h3>Why this role, and why you?</h3></div></div><label>Why do you want to join Mettelo?<textarea name="motivation" required minLength={80} maxLength={3000} rows={6} placeholder="Tell us what interests you about Mettelo and this role. Keep it specific."/></label><label>Relevant experience for {roleTitle}<textarea name="relevant_experience" required minLength={100} maxLength={4000} rows={7} placeholder="Share relevant projects, work, volunteering or practical experience. Focus on what you actually did and the outcome."/></label>{normalised.length>0&&<div className="customQuestions"><h4>Role-specific questions</h4>{normalised.map(item=><label key={item.id}>{item.label}{!item.required&&<small>Optional</small>}<textarea name={`question_${item.id}`} required={item.required} maxLength={2500} rows={4}/></label>)}</div>}</div>
      <div className="careerFormPrivacy"><div className="careerPrivacyIcon" aria-hidden="true">✓</div><div><strong>Your application stays private.</strong><p>We use this information only for Mettelo recruitment. CV files are stored privately and are never added to your public Mettelo profile.</p></div></div>
      <div className="careerSubmitRow"><button className="button dark" type="submit">Review application →</button><small>Nothing is submitted yet. You will review everything before confirming.</small></div>
    </div>{reviewing&&<section className="careerReview" aria-labelledby="career-review-heading"><div className="careerReviewHead"><span>04 · REVIEW</span><h3 id="career-review-heading" tabIndex={-1}>Check your application before submitting</h3><p>Confirm the details below. Use “Edit application” if anything needs changing.</p></div>{review&&<div className="careerReviewGrid"><div><small>Candidate</small><strong>{review.name}</strong><span>{review.email}{review.location?` · ${review.location}`:''}</span></div><div><small>CV</small><strong>{review.fileName||'No file selected'}</strong><span>Private recruitment document</span></div>{review.links.map(item=><div key={item.label}><small>{item.label}</small><a href={item.value} target="_blank" rel="noreferrer">{item.value}</a></div>)}{review.workAuth&&<div><small>Work eligibility / authorisation</small><p>{review.workAuth}</p></div>}<div className="careerReviewFull"><small>Motivation</small><p>{review.motivation}</p></div><div className="careerReviewFull"><small>Relevant experience</small><p>{review.experience}</p></div>{review.questions.map(item=><div className="careerReviewFull" key={item.label}><small>{item.label}</small><p>{item.answer}</p></div>)}</div>}<div className="careerReviewActions"><button className="button ghost" type="button" disabled={busy} onClick={()=>setReviewing(false)}>← Edit application</button><button className="button dark" type="button" disabled={busy} onClick={submit}>{busy?'Submitting…':'Confirm & submit application'}</button></div>{busy&&<div className="uploadStatus" role="status" aria-live="polite"><div><strong>Submitting your application</strong><span>{uploadProgress===null?'Preparing secure upload…':`${uploadProgress}% uploaded`}</span></div><progress max={100} value={uploadProgress||0}>{uploadProgress||0}%</progress><small>Keep this page open until you see the confirmation page.</small></div>}</section>}
    <div className={`formStatus${status?' careerStatusVisible':''}`} role="alert" aria-live="assertive">{status}</div>
    <style jsx>{`
      .careerApplyForm{width:100%;max-width:100%;min-width:0;padding:30px;border:1px solid rgba(16,19,29,.1);border-radius:22px;background:#fff;box-shadow:0 18px 48px rgba(16,19,29,.045)}.careerApplyForm *{box-sizing:border-box}.careerDraftBar{display:flex;justify-content:space-between;gap:16px;margin:-4px 0 24px;padding:11px 13px;border-radius:11px;background:#f6f4ee;color:#59616c;font-size:.7rem}.careerDraftBar span{font-weight:850;text-transform:uppercase;letter-spacing:.06em}.careerDraftBar strong{font-weight:650}.careerFormSection{min-width:0;padding:8px 0 30px;margin-bottom:26px;border-bottom:1px solid rgba(16,19,29,.08)}.careerFormHeading{display:flex;min-width:0;align-items:center;gap:14px;margin-bottom:22px}.careerFormHeading>div{min-width:0}.careerFormHeading>span{display:grid;width:34px;height:34px;flex:0 0 34px;place-items:center;border-radius:9px;background:#111521;color:#fff;font-size:.62rem;font-weight:850}.careerFormHeading small{display:block;margin-bottom:2px;color:#8a5a12;font-size:.62rem;font-weight:850;letter-spacing:.09em;text-transform:uppercase}.careerFormHeading h3{margin:0;font-size:1.35rem;letter-spacing:-.02em;overflow-wrap:anywhere}.careerFormGrid{display:grid;width:100%;max-width:100%;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:16px;min-width:0}.careerApplyForm label{display:grid;min-width:0;max-width:100%;gap:8px;margin-top:15px;color:#2d333d;font-size:.77rem;font-weight:750}.careerApplyForm label>small{display:inline;color:#69727e;font-size:.66rem;font-weight:600;overflow-wrap:anywhere}.careerApplyForm input,.careerApplyForm textarea{display:block;width:100%;max-width:100%;min-width:0;border:1px solid rgba(16,19,29,.18);border-radius:11px;background:#fff;padding:12px 13px;color:#151922;font:inherit;line-height:1.45;outline:none}.careerApplyForm input{min-height:46px}.careerApplyForm input::placeholder,.careerApplyForm textarea::placeholder{color:#737c88}.careerApplyForm input:focus-visible,.careerApplyForm textarea:focus-visible{border-color:#1e40af;box-shadow:0 0 0 3px rgba(30,64,175,.16)}.careerApplyForm textarea{min-height:130px;resize:vertical}.careerFull{grid-column:1/-1}.careerFile input{padding:10px;background:#f8f6f0;font-size:.75rem;overflow:hidden}.customQuestions{min-width:0;margin-top:22px;padding-top:8px}.customQuestions h4{margin:0 0 4px}.careerFormPrivacy{display:grid;min-width:0;grid-template-columns:36px minmax(0,1fr);gap:13px;padding:17px;border-radius:14px;background:#f4f8f5}.careerPrivacyIcon{display:grid;width:32px;height:32px;place-items:center;border-radius:50%;background:#e2f3e9;color:#0f7049;font-weight:900}.careerFormPrivacy strong{display:block;margin-bottom:4px;color:#27302b;font-size:.8rem}.careerFormPrivacy p{margin:0;color:#59655f;font-size:.73rem;line-height:1.55;overflow-wrap:anywhere}.careerSubmitRow{display:flex;min-width:0;align-items:center;justify-content:space-between;gap:18px;margin-top:22px}.careerSubmitRow .button{min-width:210px;min-height:46px;justify-content:center}.careerSubmitRow>small{max-width:300px;color:#69727e;font-size:.67rem;line-height:1.45}.careerReview,.careerReviewHead,.careerReviewGrid,.careerReviewActions{width:100%;max-width:100%;min-width:0}.careerReviewHead span{color:#8a5a12;font-size:.67rem;font-weight:900;letter-spacing:.08em}.careerReviewHead h3{max-width:100%;margin:7px 0 7px;font-size:1.45rem;overflow-wrap:anywhere}.careerReviewHead p{max-width:100%;margin:0;color:#596270;overflow-wrap:anywhere}.careerReviewGrid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:10px;margin:22px 0}.careerReviewGrid>div{display:grid;width:100%;max-width:100%;gap:4px;min-width:0;padding:14px;border:1px solid #e0e3e7;border-radius:12px}.careerReviewGrid small{color:#5b6470;font-size:.62rem;font-weight:850;text-transform:uppercase;letter-spacing:.07em}.careerReviewGrid strong{font-size:.83rem}.careerReviewGrid strong,.careerReviewGrid span,.careerReviewGrid p,.careerReviewGrid a{min-width:0;max-width:100%;overflow-wrap:anywhere;word-break:break-word;margin:0;color:#48515e;font-size:.75rem;line-height:1.5}.careerReviewGrid a{display:block;color:#1e40af;text-decoration-thickness:1px;text-underline-offset:2px}.careerReviewFull{grid-column:1/-1}.careerReviewActions{display:flex;justify-content:space-between;gap:10px;flex-wrap:wrap}.careerReviewActions .button{max-width:100%;min-height:46px;white-space:normal;text-align:center}.uploadStatus{display:grid;width:100%;max-width:100%;min-width:0;gap:8px;margin-top:16px;padding:14px;border-radius:12px;background:#eff6ff}.uploadStatus>div{display:flex;min-width:0;justify-content:space-between;gap:12px}.uploadStatus span,.uploadStatus small{color:#48515e;font-size:.7rem;overflow-wrap:anywhere}.uploadStatus progress{width:100%;max-width:100%;height:9px}.formStatus{max-width:100%;margin-top:14px;padding:0;color:#303640;font-size:.76rem;overflow-wrap:anywhere}.careerStatusVisible{padding:12px 14px;border-radius:10px;background:#fff1e8;color:#7a3312}@media(min-width:481px) and (max-width:1024px){.careerApplyForm{padding:24px}}@media(max-width:680px){.careerApplyForm{padding:20px}.careerDraftBar{display:grid}.careerFormGrid,.careerReviewGrid{grid-template-columns:minmax(0,1fr)}.careerFull,.careerReviewFull{grid-column:auto}.careerSubmitRow,.careerReviewActions{align-items:stretch;flex-direction:column}.careerSubmitRow .button,.careerReviewActions .button{width:100%;min-width:0}.uploadStatus>div{display:grid}}@media(max-width:480px){.careerApplyForm{padding:16px;border-radius:16px}.careerFormSection{padding-bottom:22px;margin-bottom:20px}.careerFormHeading{gap:10px;margin-bottom:16px}.careerFormHeading h3,.careerReviewHead h3{font-size:1.12rem;line-height:1.25}.careerFormHeading>span{width:30px;height:30px;flex-basis:30px}.careerDraftBar{margin-bottom:18px;padding:10px}.careerSubmitRow{gap:12px}.careerFormPrivacy{grid-template-columns:30px minmax(0,1fr);gap:10px;padding:13px}.careerPrivacyIcon{width:28px;height:28px}.careerApplyForm input,.careerApplyForm textarea{padding:11px}.careerApplyForm textarea{min-height:118px}.careerFile input{font-size:.68rem;padding:8px}.careerReviewGrid{margin:18px 0}.careerReviewGrid>div{padding:12px}.careerReviewActions .button,.careerSubmitRow .button{min-height:48px}}
    `}</style>
  </form>;
}
