import {NextResponse} from 'next/server';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';

async function admin(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();return user?.app_metadata?.role==='admin'?user:null}
function text(value:unknown,max=1000){return String(value??'').trim().slice(0,max)}

export async function PATCH(request:Request){
 try{const user=await admin();if(!user)return NextResponse.json({error:'Admin access required.'},{status:403});const db=serviceDb();if(!db)return NextResponse.json({error:'Intake service not configured.'},{status:503});const body=await request.json();const ids=Array.isArray(body.ids)?body.ids.map(String).filter(Boolean).slice(0,100):[String(body.id||'')].filter(Boolean);const action=String(body.action||'');if(!ids.length)return NextResponse.json({error:'Choose at least one intake record.'},{status:400});const now=new Date().toISOString();if(action==='status'){const status=String(body.status||'');if(!['new','in_progress','resolved','duplicate'].includes(status))return NextResponse.json({error:'Invalid triage status.'},{status:400});const patch:Record<string,unknown>={status,updated_at:now};if(status==='in_progress')patch.reviewed_at=now;if(status==='resolved')patch.resolved_at=now;if(status==='duplicate')patch.resolved_at=now;const {data,error}=await db.from('form_submissions').update(patch).in('id',ids).select('id,status,assigned_to_user_id,reviewed_at,resolved_at,duplicate_of_id,converted_application_id,updated_at');if(error)throw error;return NextResponse.json({ok:true,items:data||[]});}
 if(action==='assign'){const assigned=String(body.assigned_to_user_id||'').trim()||null;const {data,error}=await db.from('form_submissions').update({assigned_to_user_id:assigned,updated_at:now}).in('id',ids).select('id,status,assigned_to_user_id,reviewed_at,resolved_at,duplicate_of_id,converted_application_id,updated_at');if(error)throw error;return NextResponse.json({ok:true,items:data||[]});}
 if(action==='duplicate'){const duplicateOf=String(body.duplicate_of_id||'').trim()||null;if(ids.length!==1)return NextResponse.json({error:'Choose one submission when linking a duplicate.'},{status:400});const {data,error}=await db.from('form_submissions').update({status:'duplicate',duplicate_of_id:duplicateOf,resolved_at:now,updated_at:now}).eq('id',ids[0]).select('id,status,assigned_to_user_id,reviewed_at,resolved_at,duplicate_of_id,converted_application_id,updated_at').single();if(error)throw error;return NextResponse.json({ok:true,items:[data]});}
 if(action==='convert'){if(ids.length!==1)return NextResponse.json({error:'Convert one submission at a time.'},{status:400});const {data:submission}=await db.from('form_submissions').select('id,form_type,payload,status,converted_application_id').eq('id',ids[0]).maybeSingle();if(!submission)return NextResponse.json({error:'Submission not found.'},{status:404});if(submission.form_type!=='project_application')return NextResponse.json({error:'Only Project Application intake can be converted to the governed Project Applications queue.'},{status:409});if(submission.converted_application_id)return NextResponse.json({error:'This intake record has already been converted.'},{status:409});const payload=(submission.payload&&typeof submission.payload==='object'&&!Array.isArray(submission.payload)?submission.payload:{}) as Record<string,unknown>;const email=text(payload.email,320).toLowerCase(),projectTitle=text(payload.project,240),contribution=text(payload.contribution,4000),requestedRole=text(payload.role,160),portfolio=text(payload.profile,800);if(!email||!projectTitle||!contribution)return NextResponse.json({error:'This legacy submission is missing email, project or contribution details required for conversion.'},{status:409});const {data:project}=await db.from('projects').select('id,title').ilike('title',projectTitle).limit(1).maybeSingle();if(!project)return NextResponse.json({error:'No governed project matches the submitted project name. Link/clean the intake record before conversion.'},{status:409});const users=await db.auth.admin.listUsers({page:1,perPage:1000});const account=users.data.users.find(candidate=>candidate.email?.toLowerCase()===email);if(!account)return NextResponse.json({error:'The submitter does not have a linked Mettelo account. Ask them to create/sign in to an account before conversion.'},{status:409});const {data:existing}=await db.from('project_applications').select('id').eq('project_id',project.id).eq('user_id',account.id).neq('status','withdrawn').maybeSingle();if(existing){await db.from('form_submissions').update({status:'duplicate',converted_application_id:existing.id,resolved_at:now,updated_at:now}).eq('id',submission.id);return NextResponse.json({ok:true,application_id:existing.id,duplicate:true});}const {data:application,error}=await db.from('project_applications').insert({project_id:project.id,user_id:account.id,portfolio_url:portfolio||null,contribution_statement:contribution,requested_role:requestedRole||null,status:'submitted',application_kind:'application'}).select('id').single();if(error)throw error;await db.from('project_application_events').insert({application_id:application.id,from_status:null,to_status:'submitted',note:'Converted by Admin from legacy intake submission.',actor_user_id:user.id});await db.from('form_submissions').update({status:'resolved',converted_application_id:application.id,reviewed_at:now,resolved_at:now,updated_at:now}).eq('id',submission.id);return NextResponse.json({ok:true,application_id:application.id,duplicate:false});}
 return NextResponse.json({error:'Unknown intake action.'},{status:400});
 }catch(error){console.error('admin intake error',error);return NextResponse.json({error:'Unable to update intake.'},{status:500})}
}
