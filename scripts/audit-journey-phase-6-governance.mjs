import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');const checks=[];const expect=(name,ok)=>checks.push({name,ok:Boolean(ok)});
const member=read('components/ProjectArchitectApplication.tsx'),memberCss=read('components/ProjectArchitectApplication.module.css'),admin=read('components/AdminProjectArchitectQueue.tsx'),adminCss=read('components/AdminProjectArchitectQueue.module.css'),adminApi=read('app/api/admin/project-architect-applications/route.ts'),lead=read('app/member/project-lead/page.tsx'),progress=read('components/ArchitectProjectProgress.tsx'),governance=read('app/api/admin/project-governance/route.ts'),migration=read('supabase/migrations/20260816101500_phase6_architect_lead_governance.sql');
expect('Architect application has readiness and review timeline',member.includes('APPLICATION READINESS')&&member.includes('REVIEW TIMELINE')&&member.includes('Examples of acceptable evidence'));
expect('Architect project path exposes all governance stages',['Brief','Roles','Team','Deliverables','Data','Events','Risks','Governance','Review'].every(value=>progress.includes(`['${value}'`)));
expect('Admin review has rubric private notes and communication preview',admin.includes('Review checklist & scoring rubric')&&admin.includes('Private Admin notes')&&admin.includes('Communication preview'));
expect('Private Architect review records are Admin-only',migration.includes('project_architect_reviews')&&migration.includes('admins read architect reviews')&&migration.includes("='admin'"));
expect('Review API stores private notes separately',adminApi.includes('private_notes:privateNotes||null')&&adminApi.includes('project_architect_reviews'));
expect('Admin governance can request changes and preserve history',governance.includes("'request_changes'")&&governance.includes("eventType:`admin_${action}`"));
expect('Project Lead workspace is live rather than fixture copy',lead.includes('serviceDb()')&&lead.includes('project_tasks')&&lead.includes('Blocked & overdue work')&&lead.includes('Completion readiness')&&!lead.includes('Cost of Living Explorer'));
expect('Phase 6 responsive breakpoints are explicit',[memberCss,adminCss,lead,progress].every(text=>text.includes('max-width:480px'))&&[memberCss,adminCss,lead,progress].every(text=>text.includes('min-width:481px')&&text.includes('max-width:1024px')));
const failed=checks.filter(check=>!check.ok);for(const check of checks)console.log(`${check.ok?'PASS':'FAIL'} ${check.name}`);if(failed.length)process.exit(1);console.log(`Phase 6 journey audit passed (${checks.length} checks).`);
