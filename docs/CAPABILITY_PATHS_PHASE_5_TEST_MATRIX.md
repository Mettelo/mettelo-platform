# Phase 5 test matrix

Blocking Phase 5 validation includes:

- deterministic import architecture audit;
- isolated Supabase migrations;
- realistic 15 Path / 225 placement / 117 project dry run;
- one existing canonical project reused rather than recreated;
- 116 new private Draft projects created for the remaining fixture candidates;
- 225 placement origins across 15 Draft Paths;
- idempotent second commit;
- duplicate source fingerprint opens existing active batch;
- Amber resource cannot be Store allowed;
- conservative rollback removes only safe import-created entities;
- existing project's application, membership and contribution counts unchanged;
- same source fingerprint can be reviewed again after rollback;
- inherited Phase 1–4 audits and browser regressions remain blocking.
