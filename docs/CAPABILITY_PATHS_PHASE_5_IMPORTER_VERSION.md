# Capability Paths importer version contract

Phase 5 writes `importer_version = capability-paths-v1.0.0` into each import batch.

Changing workbook normalization semantics, matching rules, governance decisions or commit behaviour requires an importer version change so dry-run evidence can be traced to the exact transformation contract used.
