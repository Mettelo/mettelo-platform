'use client';

import {useEffect,useState} from 'react';

type Member={id:string;name:string;headline:string|null;team_role:string;membership_status:string};
type Team={id:string;run_id:string;run_number:number;title:string;project_type:string;partner_name:string|null;status:string;team_size_threshold:number;forming_deadline:string|null;kickoff_at:string|null;filled:number;team:Member[]};

export default function AdminTeamFormation(){
  const [items,setItems]=useState<Team[]>([]);const [working,setWorking]=useState('');const [message,setMessage]=useState('');const [reason,setReason]=useState<Record<string,string>>({});
  async function load(){const response=await fetch('/api/admin/project-flow');const body=await response.json().catch(()=>({}));if(response.ok)setItems(body.items||[]);else setMessage(body.error||'Unable to load project teams.');}
  useEffect(()=>{void load();},[]);
  async function act(item:Team,action:string,userId?:string){const key=`${item.run_id}:${action}`;setWorking(key);setMessage('');try{const response=await fetch('/api/admin/project-flow',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({project_id:item.id,run_id:item.run_id,action,user_id:userId,reason:reason[item.run_id]||''})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'Unable to update this team.');setMessage(body.message||'Team updated.');await load();}catch(error){setMessage(error instanceof Error?error.message:'Unable to update this team.');}finally{setWorking('');}}
  if(!items.length)return <div className="emptyState"><h3>No project teams need attention.</h3><p>Forming, active and review-stage teams will appear here.</p></div>;
  return <div className="formationAdminList">{items.map(item=>{const pct=Math.min(100,Math.round((item.filled/Math.max(1,item.team_size_threshold))*100));return <article className="panel formationAdminCard" key={item.run_id}><div className="panelHead"><div><span className="cardNumber">{item.project_type==='partner'?'PARTNER PROJECT':'OPEN PROJECT'} · TEAM {item.run_number}</span><h3 style={{marginTop:7}}>{item.title}</h3>{item.partner_name&&<small>{item.partner_name}</small>}</div><span className={`chip ${item.status==='active'?'green':''}`}>{item.status.replaceAll('_',' ').toUpperCase()}</span></div><div className="formationAdminMeta"><strong>{item.filled} of {item.team_size_threshold} places filled</strong><span>{item.forming_deadline?`Form by ${new Date(item.forming_deadline).toLocaleDateString('en-GB')}`:item.kickoff_at?`Started ${new Date(item.kickoff_at).toLocaleDateString('en-GB')}`:'No deadline set'}</span></div><div className="formationAdminTrack" aria-label={`${pct}% of team places filled`}><span style={{width:`${pct}%`}}/></div><div className="formationTeam">{item.team.length?item.team.map(member=><div key={member.id}><div><strong>{member.name}</strong><small>{member.headline||member.membership_status}</small></div><div className="formationMemberActions"><span className="chip">{member.team_role.replaceAll('_',' ').toUpperCase()}</span>{member.team_role!=='project_lead'&&<button type="button" className="button ghost" disabled={working!==''} onClick={()=>act(item,'assign_lead',member.id)}>Make lead</button>}</div></div>):<div className="emptyState"><p>No approved members in this team yet.</p></div>}</div><div className="reviewActions">{item.status==='forming'&&<button className="button dark" type="button" disabled={working!==''||item.filled===0} onClick={()=>act(item,'force_start')}>Start this team</button>}<a className="button ghost" href={`/member/projects/${item.id}?run=${item.run_id}`}>Open workspace →</a></div>{item.status!=='completed'&&<details className="workspaceComposer"><summary>Cancel this team</summary><div className="formCard compactForm"><label htmlFor={`cancel-${item.run_id}`}>Reason</label><textarea id={`cancel-${item.run_id}`} value={reason[item.run_id]||''} onChange={event=>setReason(current=>({...current,[item.run_id]:event.target.value}))} placeholder="Explain why this team is being cancelled."/><button className="button ghost" type="button" disabled={working!==''||!(reason[item.run_id]||'').trim()} onClick={()=>act(item,'cancel')}>Cancel team</button></div></details>}</article>})}<div className="formStatus" role="status" aria-live="polite">{message}</div><style jsx>{`.formationAdminList{display:grid;gap:16px}.formationAdminCard{margin:0;min-width:0}.formationAdminMeta{display:flex;justify-content:space-between;gap:16px;margin:14px 0 8px;font-size:.78rem}.formationAdminMeta span{color:#66707e}.formationAdminTrack{height:9px;border-radius:999px;overflow:hidden;background:#e7e2d7}.formationAdminTrack span{display:block;height:100%;background:#c6892a}.formationTeam{display:grid;gap:8px;margin:18px 0}.formationTeam>div{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:12px;border:1px solid rgba(16,19,29,.08);border-radius:12px;min-width:0}.formationTeam small{display:block;margin-top:3px;color:#6b7380}.formationMemberActions{display:flex;align-items:center;gap:8px}@media(max-width:680px){.formationAdminMeta,.formationTeam>div{display:grid}.formationMemberActions{justify-content:start}.reviewActions{display:grid}.reviewActions .button{width:100%}}`}</style></div>;
}
