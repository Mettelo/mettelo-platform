'use client';

import {useEffect,useState} from 'react';
import type {ProjectTeamOverview,ProjectTeamOverviewMember as Member,ProjectTeamOverviewTeam as Team} from '@/lib/project-team-overview';
function initials(name:string){return name.split(/\s+/).map(part=>part[0]).join('').slice(0,2).toUpperCase()}
function roleLabel(role:string){return role==='project_lead'?'Leader':role==='project_architect'?'Architect':role==='reviewer'?'Reviewer':role.replaceAll('_',' ')}

export default function ProjectTeamRoster({projectId,projectRunId,currentUserId,canManageSubmissionPermissions=false,initialOverview}:{projectId:string;projectRunId:string|null;currentUserId:string;canManageSubmissionPermissions?:boolean;initialOverview:ProjectTeamOverview}){
 const[teams,setTeams]=useState<Team[]>(initialOverview.teams);const[projectType,setProjectType]=useState<'open'|'partner'>(initialOverview.project_type==='open'?'open':'partner');const[error,setError]=useState('');useEffect(()=>{void fetch(`/api/project-team-overview?project_id=${encodeURIComponent(projectId)}&project_run_id=${encodeURIComponent(projectRunId||'')}`).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to load project teams.');setTeams(b.teams||[]);setProjectType(b.project_type==='open'?'open':'partner')}).catch(e=>setError(e instanceof Error?e.message:'Unable to load project teams.'))},[projectId,projectRunId]);const current=teams.find(team=>team.id===projectRunId&&team.is_member)||teams.find(team=>team.is_member)||null;
 return <section className="panel teamRoster" id="team" aria-labelledby="team-roster-title"><div className="panelHead"><div><span className="cardNumber">{projectType==='open'?'COHORTS':'TEAM'}</span><h3 id="team-roster-title">People working on this project</h3></div><span className="chip">{projectType==='open'?`${teams.length} TEAMS`:`${current?.members.length||0} MEMBERS`}</span></div>{projectType==='open'&&teams.length>1&&<nav className="cohortSwitcher" aria-label="Open project cohorts">{teams.map(team=>team.is_member?<a key={team.id} aria-current={team.id===current?.id?'page':undefined} href={`?run=${team.id}#team`}>Team {team.run_number}<small>{team.status.replaceAll('_',' ')}</small></a>:<span className="lockedCohort" key={team.id} aria-disabled="true" title="Not a member of this cohort">Team {team.run_number}<small>{team.status.replaceAll('_',' ')} · Not a member</small></span>)}</nav>}{current?<><div className="teamMeta"><span><strong>Team {current.run_number}</strong> · {current.status.replaceAll('_',' ')}</span><span>{current.members.length}/{current.required_team_size??current.members.length} members</span></div><div className="teamGrid">{current.members.length?current.members.map((member:Member)=><article className="teamMember" key={member.id}>{member.avatar_url?<span className="personAvatar photo" style={{backgroundImage:`url(${member.avatar_url})`}} aria-label={`${member.name} profile photo`}/>:<span className="personAvatar" aria-hidden="true">{initials(member.name)}</span>}<div><strong>{member.name}</strong>{member.username&&<span className="memberHandle">@{member.username}</span>}<div className="memberBadges"><span>{roleLabel(member.role)}</span><span>{member.status.replaceAll('_',' ')}</span>{member.can_submit_final_proof&&<span>Final Proof delegate</span>}</div>{member.headline&&<small>{member.headline}</small>}{canManageSubmissionPermissions&&member.id!==currentUserId&&<a className="permissionLink" href="#completion">Manage permissions</a>}</div></article>):<div className="emptyState"><h3>You’re the first member of this team.</h3><p>Other approved members will appear here as the cohort fills.</p></div>}</div></>:<div className="emptyState"><h3>Teams will appear here once you are placed.</h3><p>You can see project cohorts, but roster details are available only for the cohort you belong to.</p></div>}{error&&<div className="formStatus error" role="status">{error}</div>}<style jsx>{`.teamRoster h3{margin:8px 0 0}.cohortSwitcher{display:flex;gap:8px;overflow-x:auto;margin:14px 0;padding:0 2px 8px;scrollbar-width:thin}.cohortSwitcher a,.cohortSwitcher .lockedCohort{flex:0 0 auto;min-width:128px;min-height:52px;display:grid;align-content:center;gap:2px;padding:8px 10px;border:1px solid #d8dde3;border-radius:10px;background:#fff;color:#10131d;font-weight:800}.cohortSwitcher a[aria-current=page]{border-color:#72501b;background:#fbf6eb;box-shadow:inset 0 0 0 1px #72501b}.cohortSwitcher a:focus-visible,.permissionLink:focus-visible{outline:3px solid #8b6b2e;outline-offset:2px}.cohortSwitcher .lockedCohort{opacity:.62;cursor:default;background:#f4f5f6;border-style:dashed}.cohortSwitcher small{font-size:.62rem;color:#66707e;text-transform:capitalize}.teamMeta{display:flex;justify-content:space-between;gap:12px;margin:12px 0;padding:10px 12px;border-radius:10px;background:#f7f4ed;font-size:.72rem;color:#4e5866}.teamGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.teamMember{min-width:0;display:grid;grid-template-columns:48px minmax(0,1fr);align-items:center;gap:10px;padding:12px;border:1px solid #e0e4e8;border-radius:12px;background:#fff}.personAvatar{width:48px;height:48px;display:grid;place-items:center;border-radius:50%;background:#e8dfcc;color:#4d3512;font-weight:900;background-size:cover;background-position:center}.personAvatar.photo{font-size:0}.teamMember>div{min-width:0}.teamMember strong{display:block;overflow-wrap:anywhere}.memberHandle{display:block;margin-top:2px;color:#5b6472;font:650 .68rem/1.4 var(--font-plex-mono),ui-monospace,monospace;overflow-wrap:anywhere}.teamMember small{display:block;margin-top:5px;color:#697482;line-height:1.35}.memberBadges{display:flex;gap:5px;flex-wrap:wrap;margin-top:4px}.memberBadges span{padding:3px 6px;border-radius:999px;background:#eef1f4;color:#3f4956;font-size:.58rem;font-weight:800;text-transform:capitalize}.memberBadges span:first-child{background:#f3e8cf;color:#6f4b16}.permissionLink{display:inline-flex;align-items:center;min-height:44px;margin-top:4px;color:#5d4015;font-size:.68rem;font-weight:800}@media(max-width:640px){.teamGrid{grid-template-columns:1fr}.teamMeta{display:grid}.teamMember{grid-template-columns:44px minmax(0,1fr)}.personAvatar{width:44px;height:44px}.cohortSwitcher{margin-inline:-2px}}`}</style></section>
}
