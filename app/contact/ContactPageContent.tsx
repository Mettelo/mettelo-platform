import type { Metadata } from 'next';
import SubmissionForm from '@/components/SubmissionForm';
import {getPublicWebsitePage} from '@/lib/website-pages';

export const metadata: Metadata = {title:'Contact us',description:'Contact Mettelo about membership, projects, events, media, support or general enquiries.'};

export default async function ContactPage(){const copy=(await getPublicWebsitePage('contact')).values;return <>
<section className="hero"><div className="shell heroGrid"><div><div className="eyebrow">{copy.hero_eyebrow}</div><h1>{copy.hero_title}</h1><p className="heroLead">{copy.hero_lead}</p></div><aside className="heroPanel"><span className="chip">{copy.partner_chip}</span><h3 style={{marginTop:18}}>{copy.partner_title}</h3><p>{copy.partner_body}</p><div className="actions"><a className="button dark" href={copy.partner_cta_href}>{copy.partner_cta_label}</a></div></aside></div></section>
<section className="section"><div className="shell contactGrid"><div className="contactOptions"><div><div className="eyebrow">{copy.routes_eyebrow}</div><h2 style={{fontSize:'clamp(2.4rem,5vw,4.2rem)',margin:'0 0 22px'}}>{copy.routes_title}</h2></div><div className="contactOption"><small>MEMBERSHIP</small><strong>Joining or accessing Mettelo</strong><p>Questions about membership, onboarding, community access or your account.</p></div><div className="contactOption"><small>PROJECTS</small><strong>Labs and project applications</strong><p>Questions about a project brief, application, contributor role or active project.</p></div><div className="contactOption"><small>EVENTS & MEDIA</small><strong>Speaking, events and content</strong><p>Event questions, speaker enquiries, recorded sessions or media collaboration.</p></div><div className="contactOption"><small>OTHER</small><strong>General Mettelo enquiry</strong><p>Anything that does not fit the routes above.</p></div></div>
<SubmissionForm formType="contact" submitLabel="Send message →"><h3 style={{fontSize:'1.7rem',marginTop:0}}>{copy.form_title}</h3><p style={{color:'var(--slate)'}}>{copy.form_body}</p><div className="fieldRow"><div><label htmlFor="contact-name">Full name *</label><input id="contact-name" required name="name" autoComplete="name"/></div><div><label htmlFor="contact-email">Email *</label><input id="contact-email" required name="email" type="email" autoComplete="email"/></div></div><label htmlFor="contact-topic">What is this about? *</label><select id="contact-topic" required name="topic" defaultValue=""><option value="" disabled>Select a topic</option><option value="membership_account">Membership / account</option><option value="project_application">Project application</option><option value="active_project">Active Mettelo project</option><option value="events_speaking">Events / speaker enquiry</option><option value="media">Media</option><option value="jobs_opportunities">Jobs / opportunities</option><option value="spotlight_awards">Spotlight / awards</option><option value="technical_issue">Technical issue</option><option value="community_moderation">Community / moderation</option><option value="general">General enquiry</option></select><label htmlFor="contact-message">How can we help? *</label><textarea id="contact-message" required minLength={10} name="message" placeholder="Include the project, page or account context when relevant so we can route this without unnecessary back-and-forth."/><label className="consent"><input required type="checkbox" name="consent" value="yes"/><span>I agree that Mettelo can use the information provided to respond to this enquiry.</span></label></SubmissionForm>
</div></section>
<section className="section softSection"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">{copy.faster_eyebrow}</div><h2>{copy.faster_title}</h2></div></div><div className="grid3"><article className="card"><span className="chip">PROJECTS</span><h3 style={{marginTop:18}}>{copy.project_card_title}</h3><p>{copy.project_card_body}</p><div className="actions"><a className="linkArrow" href={copy.project_card_href}>{copy.project_card_label}</a></div></article><article className="card"><span className="chip">MEMBERSHIP</span><h3 style={{marginTop:18}}>{copy.member_card_title}</h3><p>{copy.member_card_body}</p><div className="actions"><a className="linkArrow" href={copy.member_card_href}>{copy.member_card_label}</a></div></article><article className="card"><span className="chip">PARTNERS</span><h3 style={{marginTop:18}}>{copy.partner_card_title}</h3><p>{copy.partner_card_body}</p><div className="actions"><a className="linkArrow" href={copy.partner_card_href}>{copy.partner_card_label}</a></div></article></div></div></section>
</>}
