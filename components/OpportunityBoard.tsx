'use client';

import Link from 'next/link';
import {useMemo,useState} from 'react';
import SaveOpportunityButton from './SaveOpportunityButton';

type Opportunity={id:string;title:string;organisation:string|null;opportunity_type:string;summary:string|null;location:string|null;eligibility:string|null;source_url:string|null;official_application_url:string|null;closes_at:string|null;published_at:string|null;data_ai_relevance_score:number|null;remote_scope:string|null;source_organisation:string|null;country_code:string|null;region_code:string|null;applicant_scope:string;work_arrangement:string|null;sponsorship_status:string};
const PAGE_SIZE=6;
const countryLabels:Record<string,string>={GB:'United Kingdom',NG:'Nigeria',IN:'India',US:'United States',CA:'Canada',AU:'Australia',DE:'Germany',FR:'France',NL:'Netherlands',ES:'Spain',IE:'Ireland',IT:'Italy',PL:'Poland',SK:'Slovakia',HU:'Hungary',RO:'Romania',RS:'Serbia',GLOBAL:'Remote / Global'};
const regionLabels:Record<string,string>={UK:'United Kingdom',EUROPE:'Europe',AFRICA:'Africa',ASIA:'Asia',NORTH_AMERICA:'North America',OCEANIA:'Oceania',GLOBAL:'Remote / Global'};
const sponsorshipLabels:Record<string,string>={confirmed:'Sponsorship confirmed',licensed_sponsor:'Licensed sponsor',not_offered:'No sponsorship',not_stated:'Sponsorship not stated',unclear:'Sponsorship unclear',unknown:'Sponsorship unknown'};
const scopeLabels:Record<string,string>={worldwide:'Worldwide applicants',international_accepted:'International applicants',country_restricted:'Country restricted',remote_worldwide:'Remote worldwide',unknown:'Eligibility unknown'};
function cleanText(value:string|null){if(!value)return '';let text=value;for(let i=0;i<2;i++)text=text.replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/&nbsp;/g,' ');return text.replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim()}
function short(value:string|null,max=170){const text=cleanText(value);return text.length>max?`${text.slice(0,max).trim()}…`:text}function workLabel(value:string|null){if(!value||value==='unknown')return null;if(value==='onsite')return 'On-site';return value[0].toUpperCase()+value.slice(1)}function initials(name:string|null){return (name||'Mettelo').split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function freshness(value:string|null){if(!value)return null;const days=Math.floor((Date.now()-new Date(value).getTime())/86400000);if(days<=0)return 'Added today';if(days===1)return 'Added yesterday';if(days<7)return `Added ${days} days ago`;return `Added ${new Date(value).toLocaleDateString('en-GB')}`}

export default function OpportunityBoard({items}:{items:Opportunity[]}){
  const [query,setQuery]=useState('');const [type,setType]=useState('all');const [country,setCountry]=useState('all');const [region,setRegion]=useState('all');const [arrangement,setArrangement]=useState('all');const [sponsorship,setSponsorship]=useState('all');const [scope,setScope]=useState('all');const [sort,setSort]=useState('newest');const [page,setPage]=useState(1);
  const countries=useMemo(()=>Array.from(new Set(items.map(item=>item.country_code).filter(Boolean) as string[])).sort((a,b)=>(countryLabels[a]||a).localeCompare(countryLabels[b]||b)),[items]);const regions=useMemo(()=>Array.from(new Set(items.map(item=>item.region_code).filter(Boolean) as string[])).sort((a,b)=>(regionLabels[a]||a).localeCompare(regionLabels[b]||b)),[items]);
  const filtered=useMemo(()=>{const q=query.trim().toLowerCase();const rows=items.filter(item=>{const hay=[item.title,item.organisation,item.location,item.summary,item.country_code?countryLabels[item.country_code]:null,item.region_code?regionLabels[item.region_code]:null].filter(Boolean).join(' ').toLowerCase();return(!q||hay.includes(q))&&(type==='all'||item.opportunity_type===type)&&(country==='all'||item.country_code===country)&&(region==='all'||item.region_code===region)&&(arrangement==='all'||(arrangement==='remote'?Boolean(item.remote_scope||item.work_arrangement==='remote'):item.work_arrangement===arrangement))&&(sponsorship==='all'||item.sponsorship_status===sponsorship)&&(scope==='all'||(scope==='international'?['worldwide','international_accepted','remote_worldwide'].includes(item.applicant_scope):item.applicant_scope===scope))});rows.sort((a,b)=>sort==='oldest'?new Date(a.published_at||0).getTime()-new Date(b.published_at||0).getTime():new Date(b.published_at||0).getTime()-new Date(a.published_at||0).getTime());return rows},[items,query,type,country,region,arrangement,sponsorship,scope,sort]);
  const totalPages=Math.max(1,Math.ceil(filtered.length/PAGE_SIZE));const safePage=Math.min(page,totalPages);const visible=filtered.slice((safePage-1)*PAGE_SIZE,safePage*PAGE_SIZE);function resetPage(){setPage(1)}function clearFilters(){setQuery('');setType('all');setCountry('all');setRegion('all');setArrangement('all');setSponsorship('all');setScope('all');setSort('newest');setPage(1)}const hasFilters=Boolean(query||type!=='all'||country!=='all'||region!=='all'||arrangement!=='all'||sponsorship!=='all'||scope!=='all');
  return <div className="opportunityBoard"><div className="opportunityToolbar"><div className="opportunitySearch"><label htmlFor="opportunity-search">Search</label><input id="opportunity-search" value={query} onChange={e=>{setQuery(e.target.value);resetPage()}} placeholder="Role, company, skill or location"/></div><div><label htmlFor="opportunity-type">Type</label><select id="opportunity-type" value={type} onChange={e=>{setType(e.target.value);resetPage()}}><option value="all">All types</option><option value="job">Jobs</option><option value="internship">Internships</option><option value="graduate">Graduate</option><option value="apprenticeship">Apprenticeships</option><option value="volunteer">Volunteer</option><option value="fellowship">Fellowships</option></select></div><div><label htmlFor="opportunity-country">Country</label><select id="opportunity-country" value={country} onChange={e=>{setCountry(e.target.value);resetPage()}}><option value="all">All countries</option>{countries.map(code=><option key={code} value={code}>{countryLabels[code]||code}</option>)}</select></div><div><label htmlFor="opportunity-region">Region</label><select id="opportunity-region" value={region} onChange={e=>{setRegion(e.target.value);resetPage()}}><option value="all">All regions</option>{regions.map(code=><option key={code} value={code}>{regionLabels[code]||code}</option>)}</select></div><div><label htmlFor="opportunity-arrangement">Work style</label><select id="opportunity-arrangement" value={arrangement} onChange={e=>{setArrangement(e.target.value);resetPage()}}><option value="all">Any</option><option value="remote">Remote</option><option value="hybrid">Hybrid</option><option value="onsite">On-site</option></select></div><div><label htmlFor="opportunity-sponsorship">Sponsorship</label><select id="opportunity-sponsorship" value={sponsorship} onChange={e=>{setSponsorship(e.target.value);resetPage()}}><option value="all">Any</option><option value="confirmed">Confirmed</option><option value="licensed_sponsor">Licensed sponsor</option><option value="not_stated">Not stated</option><option value="not_offered">Not offered</option></select></div><div><label htmlFor="opportunity-scope">Applicants</label><select id="opportunity-scope" value={scope} onChange={e=>{setScope(e.target.value);resetPage()}}><option value="all">Any</option><option value="international">International / worldwide</option><option value="country_restricted">Country restricted</option><option value="unknown">Unknown</option></select></div><div><label htmlFor="opportunity-sort">Sort</label><select id="opportunity-sort" value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select></div></div>
    <div className="opportunityResultsBar"><div><strong>{filtered.length} Data & AI opportunit{filtered.length===1?'y':'ies'}</strong><span>Showing {filtered.length?((safePage-1)*PAGE_SIZE)+1:0}–{Math.min(safePage*PAGE_SIZE,filtered.length)} of {filtered.length}</span></div>{hasFilters&&<button className="button ghost" type="button" onClick={clearFilters}>Clear filters</button>}</div>
    {visible.length?<div className="opportunityList">{visible.map(item=>{const location=item.country_code?countryLabels[item.country_code]||item.country_code:item.region_code?regionLabels[item.region_code]||item.region_code:item.location;return <article className="opportunityCard opportunityCardV2" key={item.id}><div className="opportunityIdentity"><div className="opportunityLogo" aria-hidden="true">{initials(item.organisation)}</div><div><span className="opportunityType">{item.opportunity_type.replaceAll('_',' ').toUpperCase()}</span><h3><Link href={`/opportunities/${item.id}`}>{item.title}</Link></h3><div className="opportunityCompany"><strong>{item.organisation||'Organisation'}</strong>{location&&<span>{location}</span>}</div></div></div>{item.summary&&<p className="opportunitySummary">{short(item.summary)}</p>}<div className="opportunitySignals">{workLabel(item.work_arrangement)&&<span>{workLabel(item.work_arrangement)}</span>}{item.applicant_scope!=='unknown'&&<span>{scopeLabels[item.applicant_scope]||item.applicant_scope}</span>}{item.sponsorship_status&&<span className={['confirmed','licensed_sponsor'].includes(item.sponsorship_status)?'positive':''}>{sponsorshipLabels[item.sponsorship_status]||item.sponsorship_status}</span>}{item.closes_at&&<span>Closes {new Date(item.closes_at).toLocaleDateString('en-GB')}</span>}</div><div className="opportunityCardFoot"><small>{freshness(item.published_at)}</small><div className="opportunityActions"><SaveOpportunityButton opportunityId={item.id} compact/><Link className="button dark" href={`/opportunities/${item.id}`}>View role →</Link></div></div></article>})}</div>:<div className="panel emptyState"><h3>No matching opportunities.</h3><p>Try a broader search or clear one of the filters.</p>{hasFilters&&<button className="button ghost" type="button" onClick={clearFilters}>Clear filters</button>}</div>}
    {totalPages>1&&<nav className="opportunityPagination" aria-label="Opportunity pages"><button className="button ghost" type="button" onClick={()=>setPage(p=>Math.max(1,p-1))} disabled={safePage===1}>← Previous</button><span>Page {safePage} of {totalPages}</span><button className="button ghost" type="button" onClick={()=>setPage(p=>Math.min(totalPages,p+1))} disabled={safePage===totalPages}>Next →</button></nav>}
    <style jsx>{`.opportunityList{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.opportunityCardV2{display:grid;gap:15px;padding:20px;border:1px solid rgba(16,19,29,.1);border-radius:18px;background:#fff;box-shadow:0 8px 24px rgba(16,19,29,.035);transition:transform .16s ease,box-shadow .16s ease,border-color .16s ease}.opportunityCardV2:hover{transform:translateY(-2px);border-color:rgba(198,137,42,.34);box-shadow:0 14px 34px rgba(16,19,29,.07)}.opportunityIdentity{display:grid;grid-template-columns:48px minmax(0,1fr);gap:13px;align-items:start}.opportunityLogo{width:48px;height:48px;display:grid;place-items:center;border-radius:13px;background:#f7efdd;color:#77531f;font-size:.78rem;font-weight:850}.opportunityType{display:inline-flex;margin-bottom:5px;color:#8b641f;font-size:.61rem;font-weight:850;letter-spacing:.08em}.opportunityCardV2 h3{margin:0;font-size:1.08rem;line-height:1.28}.opportunityCardV2 h3 a{color:#121722}.opportunityCompany{display:flex;flex-wrap:wrap;gap:7px;margin-top:5px;color:#606a77;font-size:.79rem}.opportunityCompany span:before{content:'·';margin-right:7px}.opportunitySummary{margin:0;color:#586270;font-size:.84rem;line-height:1.55;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.opportunitySignals{display:flex;flex-wrap:wrap;gap:7px}.opportunitySignals span{padding:6px 8px;border-radius:999px;background:#f4f5f6;color:#5f6874;font-size:.66rem;font-weight:700}.opportunitySignals .positive{background:#e9f6ef;color:#17633e}.opportunityCardFoot{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-top:13px;border-top:1px solid rgba(16,19,29,.07)}.opportunityCardFoot small{color:#858d98;font-size:.67rem}.opportunityActions{display:flex;align-items:center;gap:8px}.opportunityActions :global(.button){min-width:120px}.opportunityPagination{display:flex;align-items:center;justify-content:center;gap:14px;margin-top:22px}.opportunityPagination span{color:#6d7581;font-size:.78rem}@media(max-width:920px){.opportunityList{grid-template-columns:1fr}.opportunityToolbar{grid-template-columns:repeat(2,minmax(0,1fr))!important}}@media(max-width:560px){.opportunityToolbar{grid-template-columns:1fr!important}.opportunityCardV2{padding:17px}.opportunityIdentity{grid-template-columns:42px minmax(0,1fr)}.opportunityLogo{width:42px;height:42px}.opportunityCardFoot{display:grid}.opportunityActions{display:grid;grid-template-columns:1fr 1.3fr}.opportunityActions :global(.button){width:100%;min-width:0}.opportunityPagination{justify-content:space-between}.opportunityPagination .button{padding-inline:10px}}`}</style>
  </div>;
}
