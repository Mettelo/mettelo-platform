import {redirect} from 'next/navigation';import {createServerSupabaseClient} from '@/lib/supabase/server';import AdminContentManager from '@/components/AdminContentManager';
export default async function AdminContentPage(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect('/signin');if(user.app_metadata?.role!=='admin')redirect('/member');return <section className="section softSection"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Admin · Publishing</div><h1>Create and manage published content.</h1></div><p>Keep publishing separate from project selection, Proof review and access administration.</p></div><AdminContentManager/></div></section>;}
