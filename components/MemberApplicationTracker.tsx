'use client';

import {useState} from 'react';

type Formation={filled:number;threshold:number;status:string;is_full:boolean;kickoff_at:string|null};
type Application={id:string;status:string;submitted_at:string;project_id:string;projects:{title:string;status?:string}|null;project_roles:{title:string}|null;formation?:Formation|null};

const labels:{[key:string]:string}={submitted:'Submitted',in_review:'Under review',shortlisted:'Shortlisted',approved:'Approved',waiting_for_team:'Waiting for team',team_complete:'Team complete',accepted:'Approved',declined:'Declined',withdrawn:'Withdrawn'};
const stages=['Submitted','Under review','Approved','Team forming','Active'] as const;
function stageIndex(item:Application){if(item.status==='submitted')return 0;if(['in_review','shortlisted'].includes(item.status))return 1;if(['approved','accepted'].includes(item.status))return 2;if(item.status==='waiting_for_team')return 3;if(item.status==='team_complete'||item.projects?.status==='active')return 4;return 0;}

export default function MemberApplicationTracker({applications}:{applications:Application[]}){
  const [items,setItems]=useState(applications);const [working,setWorking]=useState('');const [message,setMessage]=useState('');
  async function withdraw(id:string){setWorking(id);setMessage('');try{const response=await fetch('/api/project-applications',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,action:'withdraw'})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'Unable to withdraw application.');setItems(current=>current.map(item=>item.id===id?{...item,status:'withdrawn'}:item));setMessage('Application withdrawn.');}catch(error){setMessage(error instanceof Error?error.message:'Unable to withdraw application.');}finally{setWorking('');}}
  if(!items.length)return <div className="emptyState"><h3>No applications yet.</h3><p>When you apply to a Labs project, you will see the project name, submission date and review progress here.</p><a className="linkArrow" href="/projects">Explore Labs →</a></div>;

  return <div className="memberApplicationList">{items.map(item=>{const current=stageIndex(item);const declined=item.status==='declined';const withdrawn=item.status==='withdrawn';const active=current===4;const forming=item.status==='waiting_for_team'||item.status==='approved'||item.status==='accepted';const formation=item.formation;const canWithdraw=['submitted','in_review','shortlisted'].includes(item.status);const percentage=formation?.threshold?Math.min(100,Math.round((formation.filled/formation.threshold)*100)):0;return <article className="memberApplicationCard" key={item.id}>
      <div className="memberApplicationHead"><div><span className="cardNumber">PROJECT APPLICATION</span><h3>{item.projects?.title||'Mettelo Labs project'}</h3><p>{item.project_roles?.title?`${item.project_roles.title} · `:''}Submitted {new Date(item.submitted_at).toLocaleDateString('en-GB')}</p></div><span className={`chip ${active?'green':''}`}>{(labels[item.status]||item.status.replaceAll('_',' ')).toUpperCase()}</span></div>
      {declined||withdrawn?<div className="applicationOutcome declined"><strong>{withdrawn?'Application withdrawn':'Application closed'}</strong><span>{withdrawn?'You withdrew this application before selection.':'This application was not selected. You can continue exploring other Labs projects.'}</span></div>:<div className="applicationProgress" aria-label={`Application status: ${labels[item.status]||item.status}`}>{stages.map((stage,index)=><div className={`applicationStep ${index<=current?'complete':''} ${index===current?'current':''}`} key={stage}><span>{index<current?'✓':index+1}</span><small>{stage}</small></div>)}</div>}
      {forming&&formation&&<div className="teamFormation"><div><strong>Team formation</strong><span>{formation.filled} of {formation.threshold} spots filled</span></div><div className="formationTrack" role="progressbar" aria-valuemin={0} aria-valuemax={formation.threshold} aria-valuenow={formation.filled}><span style={{width:`${percentage}%`}}/></div><small>{formation.is_full?'Team threshold reached. Kickoff is being activated.':'Your place is approved. The project starts when the team fills or Mettelo starts it early.'}</small></div>}
      <div className="memberApplicationFoot"><p>{active?'The team is complete and this project is active. Enter the workspace to collaborate.':forming?'You have been approved. We will update this tracker as the remaining team spots fill.':item.status==='shortlisted'?'You have progressed to the shortlist. The reviewer will make the final decision next.':item.status==='in_review'?'Your application is being reviewed by the Mettelo team.':'Your application has been received. Every status change will appear here.'}</p><div className="applicationActions">{canWithdraw&&<button className="button ghost" type="button" disabled={working===item.id} onClick={()=>withdraw(item.id)}>{working===item.id?'Withdrawing…':'Withdraw'}</button>}{active&&<a className="button dark" href={`/member/projects/${item.project_id}`}>Open workspace →</a>}</div></div>
    </article>})}<div className="formStatus" role="status" aria-live="polite">{message}</div><style jsx>{`.memberApplicationList{display:grid;gap:12px}.memberApplicationCard{padding:20px;border:1px solid rgba(16,19,29,.09);border-radius:16px;background:#fff}.memberApplicationHead{display:flex;align-items:flex-start;justify-content:space-between;gap:18px}.memberApplicationHead h3{margin:7px 0 4px;font-size:1.05rem}.memberApplicationHead p,.memberApplicationFoot p{margin:0;color:#66707e;font-size:.82rem}.applicationProgress{display:grid;grid-template-columns:repeat(5,1fr);gap:0;margin:20px 0 16px}.applicationStep{position:relative;display:grid;justify-items:center;gap:7px;color:#9aa1ab}.applicationStep:not(:last-child):after{content:"";position:absolute;top:14px;left:55%;right:-45%;height:2px;background:#e1e4e8}.applicationStep.complete:not(:last-child):after{background:#c6892a}.applicationStep span{position:relative;z-index:1;width:28px;height:28px;display:grid;place-items:center;border:2px solid #d7dbe0;border-radius:50%;background:#fff;font-size:.68rem;font-weight:800}.applicationStep.complete span{border-color:#c6892a;background:#f7efdd;color:#8b5a17}.applicationStep.current span{box-shadow:0 0 0 4px rgba(198,137,42,.12)}.applicationStep small{font-size:.68rem;font-weight:700;text-align:center}.applicationStep.complete small{color:#525b67}.teamFormation{display:grid;gap:8px;margin:0 0 16px;padding:14px;border-radius:12px;background:#f7f4ec}.teamFormation>div:first-child{display:flex;justify-content:space-between;gap:12px;font-size:.78rem}.teamFormation span,.teamFormation small{color:#66707e}.formationTrack{height:8px;overflow:hidden;border-radius:999px;background:#e4ded1}.formationTrack span{display:block;height:100%;border-radius:inherit;background:#c6892a}.memberApplicationFoot{display:flex;align-items:center;justify-content:space-between;gap:18px;padding-top:14px;border-top:1px solid rgba(16,19,29,.07)}.applicationActions{display:flex;gap:8px;align-items:center;flex-shrink:0}.applicationOutcome{margin:18px 0 14px;padding:13px 14px;border-radius:12px;background:#fbf2f2;color:#7f3030}.applicationOutcome strong,.applicationOutcome span{display:block}.applicationOutcome span{margin-top:3px;font-size:.76rem}@media(max-width:620px){.applicationProgress{grid-template-columns:repeat(5,minmax(70px,1fr));overflow-x:auto;padding-bottom:4px}.memberApplicationHead,.memberApplicationFoot{display:grid}.applicationActions{display:grid}.memberApplicationFoot .button{width:100%}.teamFormation>div:first-child{display:grid}}`}</style></div>;
}
