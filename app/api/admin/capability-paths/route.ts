import {NextResponse} from 'next/server';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';

const slugify=(value:string)=>value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'').slice(0,90);
const text=(value:unknown)=>String(value??'').trim();
async function admin(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();return user?.app_metadata?.role==='admin'?user:null}

async function detail(db:NonNullable<ReturnType<typeof serviceDb>>,id:string){
  const {data:path}=await db.from('capability_paths').select('*').eq('id',id).maybeSingle();
  if(!path)return null;
  const [{data:stages},{data:placements}]=await Promise.all([
    db.from('capability_path_stages').select('id,slug,name,description,position').eq('path_id',id).order('position'),
    db.from('capability_path_projects').select('path_id,project_id,stage_id,position,competency_focus,capability_built,prerequisite_project_id,prerequisite_mode,path_outcome,placement_type').eq('path_id',id).order('position')
  ]);
  const projectIds=[...new Set((placements||[]).map(item=>item.project_id))];
  const {data:projects}=projectIds.length?await db.from('projects').select('id,slug,title,status,visibility,project_type').in('id',projectIds):{data:[]};
  return {...path,stages:stages||[],placements:(placements||[]).map(item=>({...item,project:(projects||[]).find(project=>project.id===item.project_id)||null}))};
}

function validateStructure(stages:any[],placements:any[]){
  const errors:string[]=[];
  if(!stages.length)errors.push('Add at least one stage.');
  const stageSlugs=new Set<string>(),stagePositions=new Set<number>();
  for(const stage of stages){const slug=slugify(text(stage.slug||stage.name));const position=Number(stage.position);if(!text(stage.name))errors.push('Every stage needs a name.');if(!slug)errors.push('Every stage needs a valid slug.');if(!Number.isInteger(position)||position<1)errors.push('Every stage needs a positive position.');if(stageSlugs.has(slug))errors.push(`Stage slug “${slug}” is duplicated.`);if(stagePositions.has(position))errors.push(`Stage position ${position} is duplicated.`);stageSlugs.add(slug);stagePositions.add(position)}
  const projectIds=new Set<string>(),positions=new Set<number>();
  for(const item of placements){const projectId=text(item.project_id),position=Number(item.position),stageSlug=slugify(text(item.stage_slug));if(!projectId)errors.push('Every placement needs a project.');if(projectIds.has(projectId))errors.push('The same project cannot appear twice in one path.');if(!Number.isInteger(position)||position<1)errors.push('Every placement needs a positive position.');if(positions.has(position))errors.push(`Project position ${position} is duplicated.`);if(!stageSlugs.has(stageSlug))errors.push('Every placement must use a stage from this path.');if(!text(item.competency_focus))errors.push('Every placement needs a competency focus.');if(!text(item.capability_built))errors.push('Every placement needs a capability built.');if(text(item.prerequisite_project_id)===projectId)errors.push('A project cannot be its own prerequisite.');projectIds.add(projectId);positions.add(position)}
  for(const item of placements){const prerequisite=text(item.prerequisite_project_id);if(prerequisite&&!projectIds.has(prerequisite))errors.push('Prerequisites must be projects already placed in the same path.')}
  return [...new Set(errors)];
}

export async function GET(request:Request){try{const user=await admin();if(!user)return NextResponse.json({error:'Admin access required.'},{status:403});const db=serviceDb();if(!db)return NextResponse.json({error:'Admin database is not configured.'},{status:503});const url=new URL(request.url);const mode=url.searchParams.get('mode');
  if(mode==='projects'){const q=text(url.searchParams.get('q'));let query=db.from('projects').select('id,slug,title,status,visibility,project_type').order('updated_at',{ascending:false}).limit(30);if(q)query=query.ilike('title',`%${q.replaceAll('%','')}%`);const {data,error}=await query;if(error)throw error;return NextResponse.json({items:data||[]});}
  if(mode==='capabilities'){const {data,error}=await db.from('capabilities').select('id,slug,name,capability_type,is_active').eq('is_active',true).order('capability_type').order('sort_order').limit(500);if(error)throw error;return NextResponse.json({items:data||[]});}
  const projectId=text(url.searchParams.get('project_id'));if(projectId){const {data,error}=await db.from('capability_path_projects').select('path_id,project_id,stage_id,position,competency_focus,capability_built,placement_type').eq('project_id',projectId).order('position');if(error)throw error;const pathIds=[...new Set((data||[]).map(item=>item.path_id))];const {data:paths}=pathIds.length?await db.from('capability_paths').select('id,name,slug,status').in('id',pathIds):{data:[]};const stageIds=[...new Set((data||[]).map(item=>item.stage_id))];const {data:stages}=stageIds.length?await db.from('capability_path_stages').select('id,name').in('id',stageIds):{data:[]};return NextResponse.json({items:(data||[]).map(item=>({...item,path:(paths||[]).find(path=>path.id===item.path_id)||null,stage:(stages||[]).find(stage=>stage.id===item.stage_id)||null}))});}
  const id=text(url.searchParams.get('id'));if(id){const item=await detail(db,id);return item?NextResponse.json({item}):NextResponse.json({error:'Capability Path not found.'},{status:404});}
  const [{data:paths},{data:stages},{data:placements}]=await Promise.all([db.from('capability_paths').select('id,slug,name,target_role,target_outcome,status,sort_order,published_at,archived_at,created_at,updated_at').order('sort_order').order('name'),db.from('capability_path_stages').select('path_id,id'),db.from('capability_path_projects').select('path_id,project_id')]);
  return NextResponse.json({items:(paths||[]).map(path=>({...path,stage_count:(stages||[]).filter(stage=>stage.path_id===path.id).length,project_count:(placements||[]).filter(item=>item.path_id===path.id).length}))});
}catch(error){console.error(error);return NextResponse.json({error:'Unable to load Capability Paths.'},{status:500})}}

export async function POST(request:Request){try{const user=await admin();if(!user)return NextResponse.json({error:'Admin access required.'},{status:403});const db=serviceDb();if(!db)return NextResponse.json({error:'Admin database is not configured.'},{status:503});const body=await request.json() as Record<string,unknown>;const name=text(body.name),targetRole=text(body.target_role),targetOutcome=text(body.target_outcome);if(!name||!targetRole||!targetOutcome)return NextResponse.json({error:'Path name, target role and target outcome are required.'},{status:400});const slug=slugify(text(body.slug)||name);if(!slug)return NextResponse.json({error:'Enter a valid Path name or slug.'},{status:400});const {data:existing}=await db.from('capability_paths').select('id').eq('slug',slug).maybeSingle();if(existing)return NextResponse.json({error:'That Capability Path slug is already in use.'},{status:409});const now=new Date().toISOString();const {data:item,error}=await db.from('capability_paths').insert({name,slug,target_role:targetRole,target_outcome:targetOutcome,short_description:text(body.short_description)||null,description:text(body.description)||null,progression_summary:text(body.progression_summary)||null,sort_order:Number(body.sort_order)||100,status:'draft',created_by:user.id,updated_by:user.id,updated_at:now}).select('*').single();if(error)throw error;return NextResponse.json({ok:true,item:{...item,stages:[],placements:[]}},{status:201});}catch(error:any){console.error(error);if(error?.code==='23505')return NextResponse.json({error:'A Path with that name or slug already exists.'},{status:409});return NextResponse.json({error:'Unable to create Capability Path.'},{status:500})}}

export async function PUT(request:Request){try{const user=await admin();if(!user)return NextResponse.json({error:'Admin access required.'},{status:403});const db=serviceDb();if(!db)return NextResponse.json({error:'Admin database is not configured.'},{status:503});const body=await request.json() as any;const id=text(body.id);if(!id)return NextResponse.json({error:'Capability Path ID is required.'},{status:400});const stages=Array.isArray(body.stages)?body.stages.map((stage:any,index:number)=>({slug:slugify(text(stage.slug||stage.name)),name:text(stage.name),description:text(stage.description),position:Number(stage.position)||index+1})):[];const placements=Array.isArray(body.placements)?body.placements.map((item:any,index:number)=>({project_id:text(item.project_id),stage_slug:slugify(text(item.stage_slug)),position:Number(item.position)||index+1,competency_focus:text(item.competency_focus),capability_built:text(item.capability_built),prerequisite_project_id:text(item.prerequisite_project_id),prerequisite_mode:['recommended','required'].includes(text(item.prerequisite_mode))?text(item.prerequisite_mode):'recommended',path_outcome:text(item.path_outcome),placement_type:['recommended','required','optional'].includes(text(item.placement_type))?text(item.placement_type):'recommended'})):[];const validation=validateStructure(stages,placements);if(validation.length)return NextResponse.json({error:validation.join(' ')},{status:409});const {error}=await db.rpc('admin_replace_capability_path_structure',{p_path_id:id,p_stages:stages,p_placements:placements});if(error)throw error;await db.from('capability_paths').update({updated_at:new Date().toISOString(),updated_by:user.id}).eq('id',id);return NextResponse.json({ok:true,item:await detail(db,id)});}catch(error:any){console.error(error);return NextResponse.json({error:error?.message||'Unable to save Path structure.'},{status:500})}}

export async function PATCH(request:Request){try{const user=await admin();if(!user)return NextResponse.json({error:'Admin access required.'},{status:403});const db=serviceDb();if(!db)return NextResponse.json({error:'Admin database is not configured.'},{status:503});const body=await request.json() as any;const id=text(body.id),action=text(body.action)||'save';const current=await detail(db,id);if(!current)return NextResponse.json({error:'Capability Path not found.'},{status:404});const now=new Date().toISOString();if(action==='archive'){if(current.status==='archived')return NextResponse.json({ok:true,item:current});const {error}=await db.from('capability_paths').update({status:'archived',archived_at:now,archived_by:user.id,updated_at:now,updated_by:user.id}).eq('id',id);if(error)throw error;return NextResponse.json({ok:true,item:await detail(db,id)});}if(action==='restore'){if(current.status!=='archived')return NextResponse.json({error:'Only archived Paths can be restored.'},{status:409});const {error}=await db.from('capability_paths').update({status:'draft',archived_at:null,archived_by:null,updated_at:now,updated_by:user.id}).eq('id',id);if(error)throw error;return NextResponse.json({ok:true,item:await detail(db,id)});}
  const patch={name:text(body.name||current.name),slug:slugify(text(body.slug||current.slug)),target_role:text(body.target_role||current.target_role),target_outcome:text(body.target_outcome||current.target_outcome),short_description:text(body.short_description)||null,description:text(body.description)||null,progression_summary:text(body.progression_summary)||null,sort_order:Number(body.sort_order)||100,updated_at:now,updated_by:user.id};if(!patch.name||!patch.slug||!patch.target_role||!patch.target_outcome)return NextResponse.json({error:'Path name, slug, target role and target outcome are required.'},{status:400});
  if(action==='publish'){if(current.status==='archived')return NextResponse.json({error:'Restore an archived Path to Draft before publishing it again.'},{status:409});const structureErrors=validateStructure(current.stages||[],(current.placements||[]).map((item:any)=>({...item,stage_slug:(current.stages||[]).find((stage:any)=>stage.id===item.stage_id)?.slug||''})));if(!current.placements?.length)structureErrors.push('Add at least one project placement before publishing.');if(structureErrors.length)return NextResponse.json({error:`Complete the Path before publishing: ${[...new Set(structureErrors)].join(' ')}`},{status:409});Object.assign(patch,{status:'published',published_at:now,published_by:user.id,archived_at:null,archived_by:null});}
  const {error}=await db.from('capability_paths').update(patch).eq('id',id);if(error)throw error;return NextResponse.json({ok:true,item:await detail(db,id)});
}catch(error:any){console.error(error);if(error?.code==='23505')return NextResponse.json({error:'A Path with that name or slug already exists.'},{status:409});return NextResponse.json({error:'Unable to update Capability Path.'},{status:500})}}
