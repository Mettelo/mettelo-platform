import Link from 'next/link';
import {redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import SaveOpportunityButton from '@/components/SaveOpportunityButton';
import SavedOpportunityReminderToggle from '@/components/SavedOpportunityReminderToggle';
import '../../opportunities/opportunities.css';

export const dynamic='force-dynamic';
type Opportunity={id:string;title:string;organisation:string|null;summary:string|null;location:string|null;opportunity_type:string;work_arrangement:string|null;applicant_scope:string;sponsorship_status:string;closes_at:string|null;status:string;access_level:string;data_ai_relevance_status:string;official_application_url:string|null;source_url:string|null};
type SavedRow={saved_at:string;reminders_enabled:boolean;opportunities:Opportunity|null};
const sponsorshipLabels:Record<string,string>={confirmed:'Sponsorship confirmed',licensed_sponsor:'Licensed sponsor employer',not_offered:'No sponsorship',not_stated:'Sponsorship not stated',unclear:'Sponsorship unclear',unknown:'Sponsorship unknown'};
const scopeLabels:Record<string,string>={worldwide:'Worldwide applicants',international_accepted:'International applicants',country_restricted:'Country restricted',remote_worldwide:'Remote worldwide',unknown:'Eligibility unknown'};
function cleanText(value:string|null){return (value||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();}
export default async function SavedOpportunitiesPage(){const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/signin');const {data,error}=await supabase.from('saved_opportunities').select('saved_at,reminders_enabled,opportunities(id,title,organisation,summary,location,opportunity_type,work_arrangement,applicant_scope,sponsorship_status,closes_at,status,access_level,data_ai_relevance_status,official_application_url,source_url)').eq('user_id',user.id).order('saved_at',{ascending:false});const rows=((data||[]) as unknown as SavedRow[]).filter(row=>row.opportunities);const now=Date.now();
return <section className="section softSection memberWorkspace"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Saved opportunities</div><h1>Roles you want to come back to.</h1></div><p>Save relevant opportunities while you compare them. Deadline reminders are controlled per role, and closed or changed listings remain visible so you know what happened.</p></div>{error?<div className="panel emptyState"><h3>Saved opportunities are temporarily unavailable.</h3><p>Please try again shortly.</p></div>:rows.length?<div className="opportunityList">{rows.map(row=>{const item=row.opportunities!;const deadlinePassed=Boolean(item.closes_at&&new Date(item.closes_at).getTime()<=now);const closed=item.status!=='published'||item.access_level!=='public'||item.data_ai_relevance_status!=='high'||deadlinePassed;const summary=cleanText(item.summary);const applicationChannel=item.official_application_url||item.source_url?'Employer website':'Application unavailable';const changeReason=deadlinePassed?'The published closing date has passed.':item.status!=='published'?'The source listing is no longer published on Mettelo.':item.access_level!=='public'?'This listing is no longer public.':item.data_ai_relevance_status!=='high'?'This listing no longer meets Mettelo discovery criteria.':null;return <article className="opportunityCard" key={item.id}><div className="opportunityCardMain"><div className="opportunityTopline"><span className="chip">{item.opportunity_type.toUpperCase()}</span><span className={`metaPill${closed?' closed':''}`}>{closed?'Closed / changed':'Open'}</span>{item.work_arrangement&&item.work_arrangement!=='unknown'&&<span className="metaPill">{item.work_arrangement==='onsite'?'On-site':item.work_arrangement}</span>}<span className="metaPill">Apply: {applicationChannel}</span></div><h3><Link href={`/opportunities/${item.id}`}>{item.title}</Link></h3><div className="opportunityCompany"><strong>{item.organisation||'Organisation'}</strong>{item.location&&<span>· {item.location}</span>}</div>{summary&&<p>{summary.length>170?`${summary.slice(0,170).trim()}…`:summary}</p>}{changeReason&&<p><strong>Status update:</strong> {changeReason}</p>}<div className="opportunitySignals"><span className="signalPill">{scopeLabels[item.applicant_scope]||item.applicant_scope}</span><span className={`signalPill ${['confirmed','licensed_sponsor'].includes(item.sponsorship_status)?'positive':''}`}>{sponsorshipLabels[item.sponsorship_status]||item.sponsorship_status}</span></div></div><div className="opportunityCardAction"><small>Saved {new Date(row.saved_at).toLocaleDateString('en-GB')}</small>{!closed&&<SavedOpportunityReminderToggle opportunityId={item.id} initialEnabled={row.reminders_enabled}/>}<Link className="button dark" href={`/opportunities/${item.id}`}>{closed?'Review listing →':'View role →'}</Link><SaveOpportunityButton opportunityId={item.id} compact/></div></article>})}</div>:<div className="panel emptyState"><h3>No saved opportunities yet.</h3><p>Use Save role on an opportunity to keep it here for later.</p><Link className="button dark" href="/opportunities">Browse opportunities →</Link></div>}<style>{`@media (min-width:1025px){.memberWorkspace .opportunityCardAction{min-width:230px}}@media (min-width:481px) and (max-width:1024px){.memberWorkspace .opportunityCard{grid-template-columns:minmax(0,1fr) minmax(220px,280px)}}@media(max-width:480px){.memberWorkspace .opportunityCard{grid-template-columns:1fr}.memberWorkspace .opportunityCardAction{width:100%}.memberWorkspace .opportunityCardAction .button{width:100%;min-height:46px}}`}</style></div></section>}
