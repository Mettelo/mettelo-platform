import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';
import styles from './AdminProjectPulseHealth.module.css';

type Reason={code:string;label:string};
type Health={active_members:number;submissions:number;blocked:number;heavy:number;unsustainable:number;some_friction:number;significant_concern:number;support_maybe:number;support_yes:number;team_applicable:boolean;health_state:'no_current_concern'|'watch'|'needs_attention';reasons:Reason[]};
type Row={runId:string;projectId:string;projectTitle:string;runNumber:number|null;health:Health};
const healthLabels={no_current_concern:'NO CURRENT CONCERN',watch:'WATCH',needs_attention:'NEEDS ATTENTION'} as const;
function mondayUtc(value=new Date()){const date=new Date(Date.UTC(value.getUTCFullYear(),value.getUTCMonth(),value.getUTCDate()));const day=date.getUTCDay()||7;date.setUTCDate(date.getUTCDate()-day+1);return date.toISOString().slice(0,10)}

export default async function AdminProjectPulseHealth(){
 const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();if(!user||user.app_metadata?.role!=='admin')return null;
 const db=serviceDb();if(!db)return <section className={styles.panel}><p role="alert">Project pulse health is unavailable because the Admin data service is not configured.</p></section>;
 const periodStart=mondayUtc();const {data:runs,error}=await db.from('project_runs').select('id,project_id,run_number').eq('status','active').order('run_number',{ascending:true});if(error)return <section className={styles.panel}><p role="alert">Unable to load active project runs for weekly pulse health.</p></section>;
 const projectIds=[...new Set((runs||[]).map(run=>run.project_id))];const {data:projects}=projectIds.length?await db.from('projects').select('id,title').in('id',projectIds):{data:[] as {id:string;title:string}[]};const names=new Map((projects||[]).map(project=>[project.id,project.title]));const rows:Row[]=[];
 for(const run of runs||[]){const {data,error:healthError}=await auth.rpc('project_weekly_pulse_health',{target_project:run.project_id,target_run:run.id,target_period:periodStart});if(healthError)continue;const health=(Array.isArray(data)?data[0]:data) as Health|null;if(health)rows.push({runId:run.id,projectId:run.project_id,projectTitle:names.get(run.project_id)||'Mettelo project',runNumber:run.run_number,health})}
 rows.sort((a,b)=>({needs_attention:0,watch:1,no_current_concern:2}[a.health.health_state]-{needs_attention:0,watch:1,no_current_concern:2}[b.health.health_state]));
 const periodLabel=new Date(`${periodStart}T00:00:00Z`).toLocaleDateString('en-GB',{dateStyle:'medium',timeZone:'UTC'});
 return <section className={styles.panel} aria-labelledby="admin-pulse-health-heading"><div className={styles.head}><div><span>WEEKLY PROJECT PULSE</span><h2 id="admin-pulse-health-heading">Project health triage</h2><p>Explainable operational signals only. Individual responses, identities, private notes and productivity scores are not shown.</p></div><small>Week of {periodLabel} · UTC</small></div>{rows.length?<div className={styles.grid}>{rows.map(row=>{const missing=Math.max(0,Number(row.health.active_members)-Number(row.health.submissions));return <article className={styles.card} key={row.runId}><div className={styles.cardHead}><div><strong>{row.projectTitle}</strong><span>{row.runNumber?`Team ${row.runNumber}`:'Active run'} · {healthLabels[row.health.health_state]}</span></div><a href={`/member/projects/${row.projectId}?run=${row.runId}#mettelo-lab`}>Open project →</a></div>{row.health.reasons?.length?<ul>{row.health.reasons.map(reason=><li key={reason.code}>{reason.label}</li>)}</ul>:<p>No current pulse signal requires attention.</p>}<dl><Metric label="Submitted" value={`${row.health.submissions}/${row.health.active_members}`}/><Metric label="Missing" value={missing}/><Metric label="Blocked" value={row.health.blocked}/><Metric label="Heavy / unsustainable" value={Number(row.health.heavy)+Number(row.health.unsustainable)}/>{row.health.team_applicable?<Metric label="Team friction / concern" value={Number(row.health.some_friction)+Number(row.health.significant_concern)}/>:null}<Metric label="Support maybe / yes" value={Number(row.health.support_maybe)+Number(row.health.support_yes)}/></dl></article>})}</div>:<p>No active project runs are available for this week’s pulse health view.</p>}</section>;
}
function Metric({label,value}:{label:string;value:string|number}){return <div><dt>{label}</dt><dd>{value}</dd></div>}
