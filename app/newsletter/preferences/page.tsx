import type {Metadata} from 'next';
import Link from 'next/link';
import {serviceDb} from '@/lib/project-flow';

export const metadata:Metadata={title:'Newsletter preferences',description:'Choose the marketing updates you want from Mettelo or unsubscribe.'};export const dynamic='force-dynamic';
type Params={token?:string;saved?:string;unsubscribed?:string;error?:string};type Preferences={projects?:boolean;events?:boolean;opportunities?:boolean;insights?:boolean};
export default async function NewsletterPreferencesPage({searchParams}:{searchParams:Promise<Params>}){const params=await searchParams;const token=String(params.token||'');const db=serviceDb();let row:{email:string;status:string;marketing_preferences:Preferences}|null=null;if(db&&/^[0-9a-f-]{36}$/i.test(token)){const {data}=await db.from('newsletter_subscribers').select('email,status,marketing_preferences').eq('unsubscribe_token',token).maybeSingle();row=data as typeof row;}if(!row)return <section className="section softSection"><div className="shell"><div className="panel emptyState"><h1>Newsletter preferences link unavailable.</h1><p>This link is invalid or no longer matches a newsletter subscription.</p><Link className="button dark" href="/newsletter">Subscribe again →</Link></div></div></section>;const prefs=row.marketing_preferences||{};const unsubscribed=params.unsubscribed==='1'||row.status==='unsubscribed';return <section className="section softSection"><div className="shell newsletterPrefs"><div><div className="eyebrow">Newsletter preferences</div><h1>Choose the Mettelo updates you want.</h1><p className="lead">These controls apply only to optional marketing/newsletter messages. Transactional account, security, recruitment and project communications are managed separately and are not changed here.</p></div><form className="formCard" action="/api/newsletter/preferences" method="post"><input type="hidden" name="token" value={token}/>{params.saved==='1'&&<p className="formStatus success" role="status">Your newsletter preferences are saved.</p>}{unsubscribed&&<p className="formStatus success" role="status">This email is unsubscribed from Mettelo marketing updates. You can choose topics below and save to subscribe again.</p>}{params.error==='topics'&&<p className="formStatus error" role="alert">Choose at least one topic, or use Unsubscribe from all marketing.</p>}<p><strong>{row.email}</strong></p><fieldset><legend>Marketing topics</legend><label className="prefCheck"><input type="checkbox" name="projects" defaultChecked={Boolean(prefs.projects)}/><span><strong>Projects</strong><small>New Labs work and project opportunities.</small></span></label><label className="prefCheck"><input type="checkbox" name="events" defaultChecked={Boolean(prefs.events)}/><span><strong>Events</strong><small>Public workshops, sessions and showcases.</small></span></label><label className="prefCheck"><input type="checkbox" name="opportunities" defaultChecked={Boolean(prefs.opportunities)}/><span><strong>Opportunities</strong><small>Selected Data & AI roles and external opportunities.</small></span></label><label className="prefCheck"><input type="checkbox" name="insights" defaultChecked={Boolean(prefs.insights)}/><span><strong>Insights</strong><small>Practical articles, research and product updates.</small></span></label></fieldset><button className="button dark" type="submit" name="intent" value="save">Save marketing preferences</button><button className="button ghost" type="submit" name="intent" value="unsubscribe">Unsubscribe from all marketing</button></form></div><style>{`.newsletterPrefs{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,.75fr);gap:28px;align-items:start}.newsletterPrefs h1{font-size:clamp(2.7rem,6vw,5rem);margin:0}.newsletterPrefs fieldset{display:grid;gap:10px;margin:18px 0;padding:0;border:0}.newsletterPrefs legend{margin-bottom:10px;font-weight:850}.prefCheck{display:grid!important;grid-template-columns:auto 1fr;gap:10px;align-items:start;padding:12px;border:1px solid var(--line);border-radius:11px}.prefCheck input{width:20px!important;min-height:20px!important;margin:2px 0}.prefCheck strong,.prefCheck small{display:block}.prefCheck small{margin-top:3px;color:var(--slate);font-weight:500}.newsletterPrefs .button{width:100%;min-height:46px;margin-top:8px}.newsletterPrefs :is(input,button,a):focus-visible{outline:3px solid #315ea8;outline-offset:2px}@media(min-width:1025px){.newsletterPrefs{grid-template-columns:minmax(0,1fr) minmax(360px,.72fr)}}@media(min-width:481px) and (max-width:1024px){.newsletterPrefs{grid-template-columns:1fr}.newsletterPrefs .formCard{max-width:680px}}@media(max-width:480px){.newsletterPrefs{grid-template-columns:1fr;gap:18px}.newsletterPrefs .formCard{padding:20px}.newsletterPrefs h1{font-size:clamp(2.35rem,12vw,3.4rem)}}`}</style></section>}
