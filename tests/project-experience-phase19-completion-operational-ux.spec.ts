import {expect,test} from '@playwright/test';
import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();function read(file:string){return fs.readFileSync(path.join(root,file),'utf8')}

test.describe('Phase 19 completion operational UX',()=>{
 test('real Mettelo Lab mounts canonical final Proof and completion UI',()=>{const lab=read('components/MetteloLabPanel.tsx');expect(lab).toContain("import ProjectFinalProofPanel from '@/components/ProjectFinalProofPanel'");expect(lab).toContain('<ProjectFinalProofPanel');expect(lab).toContain('data-lab-review-section');expect(lab).toContain('lab-completion-review');});
 test('completion UI exposes readiness, submit, review, changes, history and completed state',()=>{const panel=read('components/ProjectFinalProofPanel.tsx');for(const text of ['COMPLETION READINESS','Submit final Proof for review','Request changes','Approve Partner completion','CHANGES REQUIRED','Resubmit final Proof for review','Completion history','PROJECT RUN COMPLETED'])expect(panel).toContain(text);});
 test('status projection includes canonical readiness and completion history for exact run',()=>{const status=read('app/api/project-final-proof/status/route.ts');for(const text of ["rpc('project_run_completion_readiness'","from('project_completion_requests')",'history:history||[]','projectCompletionReadiness','project_run_id'])expect(status).toContain(text);});
 test('Phase 19 project completion does not depend on Phase 20 Verified Proof',()=>{const readiness=read('lib/project-completion-readiness.ts'),submit=read('app/api/project-final-proof/route.ts'),completion=read('app/api/project-completion/route.ts');expect(readiness).toContain('Phase 20 owns individual');expect(readiness).toContain("!/proof|contribution verification|verified member/i");expect(submit).not.toContain('Required milestones, tasks or verified member Proof are not complete yet.');expect(submit).toContain('Project completion requirements are not satisfied.');expect(completion).toContain('Individual Verified Proof remains subject to contribution verification.');});
 test('readiness reports explicit project-level blockers',()=>{const readiness=read('lib/project-completion-readiness.ts');for(const text of ['required milestone(s) incomplete','required task(s) incomplete','required deliverable(s) missing','success criterion/criteria not yet satisfied','Required presentation is not verified'])expect(readiness).toContain(text);});
 test('final review freezes recruitment and closes active collaboration intake',()=>{const route=read('app/api/project-final-proof/route.ts');for(const text of ['freezeRecruitment',"recruitment_open:false","from('project_collaboration_needs')","status:'closed'","closed_reason:reason","from('project_member_collaboration_invitations')","status:'revoked'",'phase19_final_review'])expect(route).toContain(text);});
 test('public and member recruitment surfaces reject review-state runs',()=>{const publicPage=read('app/collaborate/[id]/page.tsx'),grow=read('components/ProjectGrowTeamSection.tsx'),invites=read('app/api/member-collaboration-invitations/route.ts');expect(publicPage).toContain("['forming','active'].includes(run.status)");expect(grow).toContain("run.status==='review'");expect(invites).toContain("['forming','active'].includes(run.status)");});
 test('changes requested returns work to active without reopening recruitment',()=>{const completion=read('app/api/project-completion/route.ts'),panel=read('components/ProjectFinalProofPanel.tsx');expect(completion).toContain("status:'active'");expect(completion).toContain('completion_requested_at:null');expect(completion).toContain('recruitment_open:false');expect(completion).not.toContain('recruitment_open:true');expect(panel).toContain("completion_request?.status==='changes_requested'");expect(panel).toContain('Resubmit final Proof for review');});
 test('review decision and completion use compare-and-set guards for concurrency and idempotency',()=>{const completion=read('app/api/project-completion/route.ts');expect(completion).toContain(".eq('status','pending').select('*').maybeSingle()");expect(completion).toContain(".eq('status','review').is('completed_at',null)");expect(completion).toContain("run.status==='completed'");expect(completion).toContain('idempotent:true');});
 test('completion revalidates readiness and does not leak review reason through email',()=>{const completion=read('app/api/project-completion/route.ts');expect(completion).toContain("rpc('project_run_completion_readiness'");expect(completion).toContain('projectCompletionReadiness');expect(completion).toContain('A secure completion-review update is available in Mettelo');expect(completion).not.toContain('needs changes: ${notes}');});
 test('completion controls retain accessible touch and status feedback',()=>{const panel=read('components/ProjectFinalProofPanel.tsx');expect(panel).toContain('min-height:44px');expect(panel).toContain(':focus-visible');expect(panel).toContain('@media(max-width:520px)');expect(panel).toContain('aria-live="polite"');expect(panel).toContain('role="status"');});
});