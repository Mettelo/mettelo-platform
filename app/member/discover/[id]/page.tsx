import {notFound,redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';
import {loadProjectRoleUsage} from '@/lib/project-role-capacity';
import {loadMemberProjectTeamState,type MemberProjectTeamState} from '@/lib/member-project-team-state';
import {calculateMemberReadiness} from '@/lib/member-readiness';
import {evaluateMemberProjectFit} from '@/lib/member-project-fit';
import {memberProjectPrimaryAction,memberProjectStateCopy,memberProjectStateLabel,resolveMemberProjectState} from '@/lib/member-project-journey';
import {getProjectDetailContent} from '@/lib/project-detail-content';
import {getProjectExperiencePlanning} from '@/lib/project-experience-data';
import {buildProjectExperienceModel} from '@/lib/project-experience-model';
import {getProjectExperienceRoleDetails} from '@/lib/project-experience-role-data';
import MemberProjectDetailV2 from '@/components/project-experience/MemberProjectDetailV2';
import polish from '@/components/project-experience/ProjectExperiencePolish.module.css';

export const dynamic='force-dynamic';

type Role={id:string;title:string;description:string|null;skills:string[]|null;openings:number;discipline:string|null;canonical_role_key:string|null};
type TaxonomyRef={slug:string;name:string};
type RoleFamilyCatalogue={slug:string;title:string;description:string|null};
type RoleFamilyRelation={project_role_catalogue:RoleFamilyCatalogue|RoleFamilyCatalogue[]|null};
type Project={id:string;canonical_project_key:string|null;title:string;summary:string;problem_statement:string|null;status:string;project_type:string|null;visibility:string;partner_name:string|null;location:string|null;location_type:string|null;difficulty_level:string|null;duration_weeks:number|null;weekly_commitment:string|null;application_deadline:string|null;applications_open:boolean|null;participation_mode:'solo'|'team'|'flexible'|null;min_team_size:number|null;target_team_size:number|null;max_team_size:number|null;team_size_threshold:number|null;starts_at:string|null;ends_at:string|null;project_roles:Role[]|null;project_domains:{domains:TaxonomyRef|null}[]|null;project_tools:{tools:TaxonomyRef|null}[]|null;project_methods:{methods:TaxonomyRef|null}[]|null};
type Application={id:string;status:string;application_kind:string|null;project_run_id:string|null};
type Membership={membership_status:string;project_run_id:string|null;project_runs:{status:string}|null};

function relationValues(rows:{domains?:TaxonomyRef|null;tools?:TaxonomyRef|null;methods?:TaxonomyRef|null}[]|null|undefined,key:'domains'|'tools'|'methods'){return(rows||[]).map(row=>row[key]).filter((value):value is TaxonomyRef=>Boolean(value))}
function relationOne<T>(value:T|T[]|null|undefined):T|null{return Array.isArray(value)?value[0]||null:value||null}
function preferenceValues(rows:unknown[]|null|undefined,key:'domains'|'tools'){return(rows||[]).map(row=>relationOne((row as Record<string,unknown>)[key] as TaxonomyRef|TaxonomyRef[]|null)).filter((value):value is TaxonomyRef=>Boolean(value))}
const unknownTeamState:MemberProjectTeamState={known:false,confirmedMembers:0,reservedMembers:0,occupiedPlaces:0,minTeamSize:null,targetTeamSize:null,maxTeamSize:null,capacityAvailable:false,stateLabel:'Team status unavailable'};

export default async function MemberProjectDetailPage({params}:{params:Promise<{id:string}>}){
 const {id}=await params;const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect(`/signin?next=${encodeURIComponent(`/member/discover/${id}`)}`);
 const [projectResult,applicationsResult,membershipResult,savedResult,profileResult,domainPrefs,toolPrefs,detail,planning,roleDetails,roleFamiliesResult]=await Promise.all([
  supabase.from('projects').select('id,canonical_project_key,title,summary,problem_statement,status,project_type,visibility,partner_name,location,location_type,difficulty_level,duration_weeks,weekly_commitment,application_deadline,applications_open,participation_mode,min_team_size,target_team_size,max_team_size,team_size_threshold,starts_at,ends_at,project_roles(id,title,description,skills,openings,discipline,canonical_role_key),project_domains(domains(slug,name)),project_tools(tools(slug,name)),project_methods(methods(slug,name))').eq('id',id).in('visibility',['public','members']).maybeSingle(),
  supabase.from('project_applications').select('id,status,application_kind,project_run_id').eq('project_id',id).eq('user_id',user.id).order('submitted_at',{ascending:false}).limit(20),
  supabase.from('project_members').select('membership_status,project_run_id,project_runs(status)').eq('project_id',id).eq('user_id',user.id).in('membership_status',['waiting','active','completed']).order('joined_at',{ascending:false}).limit(1).maybeSingle(),
  supabase.from('saved_projects').select('project_id').eq('project_id',id).eq('user_id',user.id).maybeSingle(),
  supabase.from('profiles').select('full_name,headline,current_job_title,professional_area,bio,location,experience_level,employment_status,project_availability,weekly_capacity,primary_goal,linkedin_url,github_url,portfolio_url,skills,preferred_roles').eq('id',user.id).maybeSingle(),
  supabase.from('profile_domain_preferences').select('domains(slug,name)').eq('user_id',user.id),supabase.from('profile_tool_preferences').select('tools(slug,name)').eq('user_id',user.id),getProjectDetailContent(id),getProjectExperiencePlanning(id),getProjectExperienceRoleDetails(id),supabase.from('project_role_families').select('project_role_catalogue(slug,title,description)').eq('project_id',id)
 ]);
 if(projectResult.error||!projectResult.data)notFound();const project=projectResult.data as unknown as Project;
 const applications=(applicationsResult.data||[]) as Application[];const application=applications.find(item=>!['declined','withdrawn'].includes(item.status))||null;const membership=(membershipResult.data||null) as unknown as Membership|null;const profile=profileResult.data as Record<string,unknown>|null;
 const preferredDomains=preferenceValues(domainPrefs.data as unknown[]|null,'domains'),preferredTools=preferenceValues(toolPrefs.data as unknown[]|null,'tools');const memberReadiness=calculateMemberReadiness({profile:profile||{},domainCount:preferredDomains.length,toolCount:preferredTools.length});const applicationReady=memberReadiness.applicationReadiness.ready;
 const rolePool=project.canonical_project_key?(project.project_roles||[]).filter(role=>Boolean(role.canonical_role_key)):(project.project_roles||[]);const roles=rolePool.filter(role=>{const status=roleDetails.get(role.id)?.roleStatus;return status!=='closed'&&status!=='filled'});
 const db=serviceDb();let availabilityKnown=false;let filled=new Map<string,number>();let teamState=unknownTeamState;
 if(db){const [usage,team]=await Promise.all([loadProjectRoleUsage(db,id,project.project_type),loadMemberProjectTeamState(db,{projectId:id,projectType:project.project_type,projectStatus:project.status,minTeamSize:project.min_team_size,targetTeamSize:project.target_team_size,maxTeamSize:project.max_team_size,legacyThreshold:project.team_size_threshold})]);availabilityKnown=usage.known;filled=usage.filled;teamState=team;if(!usage.known)console.error('member detail contribution-area capacity lookup failed');if(!team.known)console.error('member detail project team-state lookup failed')}
 const run=membership?.project_runs||null;const state=resolveMemberProjectState({project,application,membership,run,applicationReady,capacityAvailable:teamState.capacityAvailable,capacityKnown:teamState.known});
 const displayRoles=roles.map(role=>{const used=filled.get(role.id)||0;const rich=roleDetails.get(role.id);return{id:role.id,title:role.title,description:role.description,skills:role.skills||[],openings:role.openings,remaining:availabilityKnown?Math.max(0,role.openings-used):null,available:availabilityKnown&&used<role.openings,responsibilities:rich?.responsibilities||[],recommendedSkills:rich?.recommendedSkills||[],experienceExpectation:rich?.experienceExpectation||null,weeklyCommitment:rich?.weeklyCommitment||null,applicationRequirements:rich?.applicationRequirements||null,canonicalRoleKey:role.canonical_role_key}});
 if(roleFamiliesResult.error)console.warn('member project contribution-area enrichment unavailable',roleFamiliesResult.error.message);const contributionMap=new Map<string,RoleFamilyCatalogue>();for(const row of (roleFamiliesResult.data||[]) as unknown as RoleFamilyRelation[]){const value=relationOne(row.project_role_catalogue);if(value&&!contributionMap.has(value.slug))contributionMap.set(value.slug,value)}const contributionAreas=[...contributionMap.values()].sort((a,b)=>a.title.localeCompare(b.title));
 const domains=relationValues(project.project_domains,'domains'),tools=relationValues(project.project_tools,'tools'),methods=relationValues(project.project_methods,'methods');const fit=evaluateMemberProjectFit({profile:{skills:Array.isArray(profile?.skills)?profile.skills as string[]:[],preferredRoles:Array.isArray(profile?.preferred_roles)?profile.preferred_roles as string[]:[],experienceLevel:typeof profile?.experience_level==='string'?profile.experience_level:null,weeklyCapacity:typeof profile?.weekly_capacity==='string'?profile.weekly_capacity:null},preferredDomains,preferredTools,project:{difficultyLevel:project.difficulty_level,weeklyCommitment:project.weekly_commitment,domains,tools},roles:displayRoles.map(role=>({id:role.id,title:role.title,skills:role.skills,recommendedSkills:role.recommendedSkills,canonicalRoleKey:role.canonicalRoleKey}))});
 const model=buildProjectExperienceModel({project:{id:project.id,title:project.title,summary:project.summary,problemStatement:project.problem_statement,status:project.status,projectType:project.project_type,applicationsOpen:project.applications_open,partnerName:project.partner_name,location:project.location,locationType:project.location_type,difficultyLevel:project.difficulty_level,durationWeeks:project.duration_weeks,weeklyCommitment:project.weekly_commitment,applicationDeadline:project.application_deadline,participationMode:project.participation_mode,minTeamSize:project.min_team_size,targetTeamSize:project.target_team_size,maxTeamSize:project.max_team_size,teamSizeThreshold:project.team_size_threshold,startsAt:project.starts_at,endsAt:project.ends_at},roles:roles.map(role=>{const rich=roleDetails.get(role.id);return{id:role.id,title:role.title,description:role.description,discipline:role.discipline,skills:role.skills||[],openings:role.openings,responsibilities:rich?.responsibilities||[],recommendedSkills:rich?.recommendedSkills||[],experienceExpectation:rich?.experienceExpectation||null,weeklyCommitment:rich?.weeklyCommitment||null,roleStatus:rich?.roleStatus||null,applicationRequirements:rich?.applicationRequirements||null}}),domains,tools,methods,detail,brief:planning.brief,milestones:planning.milestones});
 return <div className={`${polish.host} ${polish.memberHost}`}><MemberProjectDetailV2 model={model} state={state} stateLabel={memberProjectStateLabel(state)} stateCopy={memberProjectStateCopy(state)} primaryAction={memberProjectPrimaryAction(state,project.id)} applicationReady={applicationReady} profileCompletion={memberReadiness.profileCompletion.percentage} applicationMissing={memberReadiness.applicationReadiness.missing.map(item=>item.label)} saved={Boolean(savedResult.data)} roles={displayRoles} contributionAreas={contributionAreas} fit={fit} teamState={teamState}/></div>;
}
