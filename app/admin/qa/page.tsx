import {redirect} from 'next/navigation';import {createServerSupabaseClient} from '@/lib/supabase/server';import AdminQaTeamSetup from '@/components/AdminQaTeamSetup';
export default async function AdminQaPage(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect('/signin');if(user.app_metadata?.role!=='admin')redirect('/member');return <section className="section softSection"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Admin · QA team</div><h1>Manage project quality assurance.</h1></div><p>QA and technical review controls live here instead of inside the main admin dashboard.</p></div><AdminQaTeamSetup/></div></section>;}
