import type {Metadata} from 'next';
import Image from 'next/image';
import Link from 'next/link';
import {insightArticles} from '@/lib/insights-seed';
import './insights.css';

export const metadata:Metadata={title:'Insights & News',description:'Practical Mettelo thinking on Data & AI careers, real work, capability, projects and the systems behind professional growth.'};

export default function BlogPage(){return <>
<section className="hero insightsHero"><div className="shell heroGrid"><div><div className="eyebrow">Mettelo Insights & News</div><h1>Ideas for people building careers, products and systems in Data & AI.</h1><p className="heroLead">Career reality, product thinking, practical frameworks and transparent updates from the technology company we are building.</p><div className="actions"><a className="button dark" href="#latest-insights">Read the latest →</a><a className="button ghost" href="/newsletter">Get new insights</a></div></div><aside className="heroPanel"><span className="chip">EDITORIAL PRINCIPLE</span><h3 style={{marginTop:18}}>Useful before impressive.</h3><p>We publish around real professional problems, product decisions, project evidence and what we are learning as Mettelo develops.</p><div className="path"><span>Career reality</span><span>Product</span><span>Labs</span><span>Research</span><span>Building Mettelo</span></div></aside></div></section>
<section className="section" id="latest-insights"><div className="shell"><div className="sectionHead insightsSectionHead"><div><div className="eyebrow">Latest</div><h2>News, insight and the thinking behind Mettelo.</h2></div><p>{insightArticles.length} published test articles · distinct editorial covers · full article pages.</p></div><div className="insightGrid">{insightArticles.map(article=><article className="insightCard" key={article.slug}><Link className="insightImageLink" href={`/blog/${article.slug}`} aria-label={`Read ${article.title}`}><Image src={article.image} alt={article.imageAlt} width={1200} height={675} className="insightImage"/></Link><div className="insightCardBody"><div className="insightMeta"><span>{article.category}</span><span>{new Date(article.publishedAt+'T12:00:00Z').toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'})}</span></div><h3><Link href={`/blog/${article.slug}`}>{article.title}</Link></h3><p>{article.summary}</p><div className="insightCardFooter"><span>{article.readTime}</span><Link href={`/blog/${article.slug}`}>Read article →</Link></div></div></article>)}</div></div></section>
<section className="section softSection"><div className="shell"><div className="ctaBand"><div><div className="cardNumber">METTELO THINKING</div><h2>Follow the work, not just the announcement.</h2><p>Get practical insight, company updates, Labs learning and new thinking on how professional capability is built and proven.</p></div><div className="actions"><a className="button dark" href="/newsletter">Subscribe for updates →</a></div></div></div></section>
</>}
