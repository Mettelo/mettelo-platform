import {redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';
import AdminSectionTabs from '@/components/AdminSectionTabs';
import AdminCommunicationCentre from '@/components/AdminCommunicationCentre';

type Template={id:string;template_key:string;journey:string;name:string;description:string|null;send_mode:'automatic'|'admin_review'|'manual';subject_template:string;body_template:string;cta_label:string|null;cta_url_template:string|null;variables:string[];active:boolean;version:number;updated_at:string};export const dynamic='force-dynamic';
export default async function CommunicationTemplatesPage(){const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();if(!user)redirect('/signin');if(user.app_metadata?.role!=='admin')redirect('/member');const db=serviceDb();const {data}=db?await db.from('communication_templates').select('id,template_key,journey,name,description,send_mode,subject_template,body_template,cta_label,cta_url_template,variables,active,version,updated_at').order('journey').order('name'):{data:[]};const templates=(data||[]) as Template[];return <section className="section softSection"><div className="shell"><AdminSectionTabs label="Communication sections" tabs={[{label:'Overview',href:'/admin/notifications/overview'},{label:'Templates',href:'/admin/notifications/templates'},{label:'Delivery Queue',href:'/admin/notifications/delivery'},{label:'Event Catalogue',href:'/admin/notifications/events'}]}/><div className="adminPageHeader"><div><div className="eyebrow">Admin / Content &amp; Comms / Communications / Templates</div><h1>Communication templates</h1><p>Search the catalogue, edit one governed template at a time, preview changes, send a test and publish a new version.</p></div></div><AdminCommunicationCentre initialTemplates={templates}/></div></section>}
