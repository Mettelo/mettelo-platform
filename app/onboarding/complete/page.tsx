import type {Metadata} from 'next';

export const metadata:Metadata={title:'Profile setup complete',description:'Your Mettelo professional profile setup is complete.'};
function safeNext(value:string|string[]|undefined){const item=Array.isArray(value)?value[0]:value;return item&&item.startsWith('/')&&!item.startsWith('//')?item:'/member'}
export default async function OnboardingCompletePage({searchParams}:{searchParams?:Promise<{next?:string|string[]}>}){const next=safeNext((await searchParams||{}).next);const projectReturn=next.startsWith('/member/discover/');return <section className="section softSection"><div className="shell" style={{maxWidth:760}}><div className="panel" style={{padding:'clamp(28px,5vw,54px)',textAlign:'center'}}><div aria-hidden="true" style={{width:64,height:64,borderRadius:'50%',display:'grid',placeItems:'center',margin:'0 auto 20px',background:'#E8F5ED',color:'#157347',fontSize:28,fontWeight:900}}>✓</div><div className="eyebrow" style={{justifyContent:'center'}}>Profile setup complete</div><h1 style={{fontSize:'clamp(2.4rem,6vw,4rem)',margin:'0 0 14px'}}>Your professional profile is ready to use.</h1><p className="lead" style={{margin:'0 auto',maxWidth:610}}>{projectReturn?'Your profile setup is complete. Continue to the same project you selected before creating your account.':'Mettelo can now use your profile signals to make project discovery and recommendations more relevant. Your next step is to explore project work where you could contribute. Mettelo Proof comes later, from real contributions that complete review.'}</p><div className="actions" style={{justifyContent:'center',flexWrap:'wrap'}}><a className="button dark" href={next}>{projectReturn?'Return to selected project →':'Continue to My Mettelo →'}</a><a className="button ghost" href="/member/profile">Review profile</a></div></div></div></section>}
