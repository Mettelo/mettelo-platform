'use client';

import {useMemo,useState} from 'react';
import AdminStatusBadge from './AdminStatusBadge';

type Item={id:string;name:string;avatar_url?:string|null;email:string;project_id?:string;project:string;role:string;status:string;submitted_at:string;statement:string;portfolio_url:string|null;availability:string|null;reviewer_notes?:string|null;profile_completeness?:number;profile:{headline:string|null;skills:string[];experience_level:string|null;project_availability:string|null;weekly_capacity:string|null};communications:{title:string;body:string;created_at:string}[]};
const defaults:Record<string,(item:Item)=>string>={in_review:item=>`Your application for ${item.project} is now in review. No action is required from you right now. We will update My Mettelo when the next decision is available.`,shortlisted:item=>`You have been shortlisted for ${item.project}. No action is required right now; we will contact you if we need more information before the final decision.`,approved:item=>`Your application for ${item.project} has been approved. Your place is confirmed and will move into team formation. Keep your availability current while the team is completed.`,declined:item=>`Your application for ${item.project} was not selected for this team. You can continue exploring other Mettelo projects that match your profile.`};
const statusOptions=['submitted','in_review','shortlisted','approved','declined'];

export default function AdminApplicationQueue({initialItems}:{initialItems:Item[]}){
  const [items,setItems]=useState(initialItems);const [selected,setSelected]=useState<string[]>([]);const [projectFilter,setProjectFilter]=useState('all');const [statusFilter,setStatusFilter]=useState<string[]>(['submitted','in_review','shortlisted']);const [dateFrom,setDateFrom]=useState('');const [dateTo,setDateTo]=useState('');const [sort,setSort]=useState('newest');const [working,setWorking]=useState('');const [message,setMessage]=useState('');const [pending,setPending]=useState<{status:'in_review'|'shortlisted'|'approved'|'declined';ids:string[]}|null>(null);const [customMessage,setCustomMessage]=useState('');
  const projects=useMemo(()=>[...new Set(items.map(item=>item.project))].sort(),[items]);
  const visible=useMemo(()=>{const rows=items.filter(item=>(projectFilter==='all'||item.project===projectFilter)&&(!statusFilter.length||statusFilter.includes(item.status))&&(!dateFrom||new Date(item.submitted_at)>=new Date(`${dateFrom}T00:00:00`))&&(!dateTo||new Date(item.submitted_at)<=new Date(`${dateTo}T23:59:59`)));return [...rows].sort((a,b)=>sort==='oldest'?new Date(a.submitted_at).getTime()-new Date(b.submitted_at).getTime():sort==='project'?a.project.localeCompare(b.project):new Date(b.submitted_at).getTime()-new Date(a.submitted_at).getTime())},[items,projectFilter,statusFilter,dateFrom,dateTo,sort]);
  const selectedItems=items.filter(item=>selected.includes(item.id));
  function toggle(id:string){setSelected(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])}
  async function update(item:Item,status:string,override?:string){const memberMessage=(override||'').trim()||(defaults[status]?defaults[status](item):'');const response=await fetch('/api/admin/applications',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id:item.id,status,reviewer_notes:item.reviewer_notes||'',custom_message:memberMessage})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||'Unable to update application.');setItems(current=>current.map(row=>row.id===item.id?{...row,status:payload.application?.status||status}:row));}
  async function confirmUpdate(){if(!pending)return;setWorking('bulk');setMessage('');try{for(const id of pending.ids){const item=items.find(row=>row.id===id);if(item)await update(item,pending.status,pending.ids.length===1?customMessage:'')}setMessage(`${pending.ids.length} application${pending.ids.length===1?'':'s'} updated to ${pending.status.replaceAll('_',' ')} and communication recorded.`);setSelected([]);setPending(null);setCustomMessage('');}catch(error){setMessage(error instanceof Error?error.message:'Unable to update applications.')}finally{setWorking('')}}
  const openAction=(status:'in_review'|'shortlisted'|'approved'|'declined',ids:string[])=>{setPending({status,ids});const first=items.find(item=>item.id===ids[0]);setCustomMessage(first&&ids.length===1&&defaults[status]?defaults[status](first):'')};
  return <div className="adminApplications">
    <div className="applicationToolbar"><label>Project<select value={projectFilter} onChange={e=>setProjectFilter(e.target.value)}><option value="all">All projects</option>{projects.map(project=><option key={project}>{project}</option>)}</select></label><label>Status<select multiple value={statusFilter} onChange={e=>setStatusFilter(Array.from(e.target.selectedOptions).map(option=>option.value))}>{statusOptions.map(status=><option key={status} value={status}>{status.replaceAll('_',' ')}</option>)}</select></label><label>Applied from<input type="date" value={dateFrom} onChange={e=>setDateFrom(e.target.value)}/></label><label>Applied to<input type="date" value={dateTo} onChange={e=>setDateTo(e.target.value)}/></label><label>Sort<select value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option><option value="project">By project</option></select></label></div>
    {selected.length>0&&<div className="applicationBulkBar"><strong>{selected.length} selected</strong><button onClick={()=>openAction('in_review',selected)}>Mark under review</button><button onClick={()=>openAction('shortlisted',selected)}>Shortlist selected</button><button onClick={()=>openAction('approved',selected)}>Approve selected → team</button><button onClick={()=>openAction('declined',selected)}>Decline selected</button><button onClick={()=>setSelected([])}>Clear</button></div>}
    {!items.length?<div className="adminEmpty"><h3>No applications waiting for review</h3><p>New project applications will appear here automatically.</p></div>:!visible.length?<div className="adminEmpty"><h3>No applications match your filters</h3><button className="button ghost" type="button" onClick={()=>{setProjectFilter('all');setStatusFilter(['submitted','in_review','shortlisted']);setDateFrom('');setDateTo('')}}>Clear filters</button></div>:<><div className="applicationTableWrap"><table className="applicationTable"><thead><tr><th><input type="checkbox" aria-label="Select visible applications" checked={visible.length>0&&visible.every(item=>selected.includes(item.id))} onChange={e=>setSelected(e.target.checked?visible.map(item=>item.id):[])}/></th><th>Applicant</th><th>Applied-to project</th><th>Applied date</th><th>Status</th><th>Profile</th><th><span className="srOnly">Actions</span></th></tr></thead><tbody>{visible.map(item=><tr key={item.id}><td><input type="checkbox" checked={selected.includes(item.id)} onChange={()=>toggle(item.id)} aria-label={`Select ${item.name}`}/></td><td><div className="applicantCell"><span className="miniAvatar" style={item.avatar_url?{backgroundImage:`url(${item.avatar_url})`}:undefined}>{item.avatar_url?'':item.name.slice(0,1).toUpperCase()}</span><span><strong>{item.name}</strong><small>{item.email}</small></span></div></td><td><a href={item.project_id?`/admin/project-operations/projects/${item.project_id}`:'#'}>{item.project}</a><small className="roleLine">{item.role}</small></td><td>{new Date(item.submitted_at).toLocaleDateString('en-GB')}</td><td><AdminStatusBadge status={item.status}/></td><td><a className="profilePct" href={`/people?search=${encodeURIComponent(item.name)}`}>{item.profile_completeness??0}%</a></td><td><details className="rowMenu"><summary aria-label={`Actions for ${item.name}`}>⋯</summary><div><a href={`#application-${item.id}`}>View application</a>{item.status!=='approved'&&<button type="button" onClick={()=>openAction('approved',[item.id])}>Approve</button>}{item.status!=='shortlisted'&&<button type="button" onClick={()=>openAction('shortlisted',[item.id])}>Shortlist</button>}<button type="button" onClick={()=>openAction('in_review',[item.id])}>Mark under review</button><button className="danger" type="button" onClick={()=>openAction('declined',[item.id])}>Decline</button></div></details></td></tr>)}</tbody></table></div><div className="applicationMobileList">{visible.map(item=><article id={`application-${item.id}`} key={item.id}><div className="mobileAppTop"><input type="checkbox" checked={selected.includes(item.id)} onChange={()=>toggle(item.id)} aria-label={`Select ${item.name}`}/><div><strong>{item.name}</strong><small>{item.project} · {new Date(item.submitted_at).toLocaleDateString('en-GB')}</small></div><details className="rowMenu"><summary aria-label={`Actions for ${item.name}`}>⋯</summary><div><button onClick={()=>openAction('in_review',[item.id])}>Under review</button><button onClick={()=>openAction('shortlisted',[item.id])}>Shortlist</button><button onClick={()=>openAction('approved',[item.id])}>Approve</button><button className="danger" onClick={()=>openAction('declined',[item.id])}>Decline</button></div></details></div><div className="mobileAppMeta"><AdminStatusBadge status={item.status}/><span>{item.profile_completeness??0}% profile</span></div></article>)}</div></>}
    <div className="formStatus" role="status" aria-live="polite">{message}</div>
    {pending&&<div className="modalBackdrop" onMouseDown={e=>{if(e.currentTarget===e.target)setPending(null)}}><section className="confirmModal" role="dialog" aria-modal="true" aria-labelledby="application-confirm-heading"><span className="eyebrow">Confirm status & communication</span><h2 id="application-confirm-heading">Move {pending.ids.length===1?selectedItems.find(i=>i.id===pending.ids[0])?.name||'applicant':`${pending.ids.length} applicants`} to {pending.status.replaceAll('_',' ')}?</h2><p>This updates the canonical application status and records the member communication at the same time.</p><div className="affectedList">{items.filter(item=>pending.ids.includes(item.id)).slice(0,12).map(item=><span key={item.id}>{item.name} · {item.project}</span>)}{pending.ids.length>12&&<span>+ {pending.ids.length-12} more applicants</span>}</div><div className="messagePreview"><small>MESSAGE PREVIEW</small>{pending.ids.length===1?<textarea value={customMessage} onChange={e=>setCustomMessage(e.target.value)} rows={5}/>:<p>Each applicant receives the approved Mettelo message for the resulting status. Messages are generated per applicant and recorded in communication history.</p>}</div><div className="actions"><button className="button ghost" type="button" onClick={()=>setPending(null)}>Cancel</button><button className="button dark" type="button" disabled={working==='bulk'} onClick={confirmUpdate}>{working==='bulk'?'Updating…':'Confirm status & send'}</button></div></section></div>}
    <style jsx>{`
      .adminApplications{display:grid;gap:14px}.applicationToolbar{display:grid;grid-template-columns:1.1fr .9fr .8fr .8fr .9fr;gap:8px;padding:12px;border:1px solid #d9dde2;border-radius:14px;background:#fff}.applicationToolbar label{display:grid;gap:5px;font-size:.65rem;font-weight:800;color:#5b6470}.applicationToolbar input,.applicationToolbar select{min-height:42px;border:1px solid #cfd4da;border-radius:9px;background:#fff;padding:8px}.applicationToolbar select[multiple]{height:42px;overflow:hidden}.applicationBulkBar{position:sticky;top:76px;z-index:45;display:flex;align-items:center;gap:8px;overflow:auto;padding:10px 12px;border-radius:12px;background:#10131d;color:#fff;box-shadow:0 8px 26px rgba(16,19,29,.18)}.applicationBulkBar button{flex:none;min-height:38px;border:1px solid rgba(255,255,255,.2);border-radius:8px;background:#fff;padding:0 10px;font-weight:750}.applicationTableWrap{overflow:auto;border:1px solid #d9dde2;border-radius:14px;background:#fff}.applicationTable{width:100%;border-collapse:collapse;min-width:900px}.applicationTable th{padding:11px;text-align:left;background:#f5f6f8;border-bottom:1px solid #d9dde2;font-size:.65rem;text-transform:uppercase;letter-spacing:.04em;color:#5b6470}.applicationTable td{padding:11px;border-bottom:1px solid #e5e8ec;font-size:.75rem;vertical-align:middle}.applicantCell{display:flex;align-items:center;gap:9px}.miniAvatar{width:34px;height:34px;display:grid;place-items:center;flex:none;border-radius:10px;background:#e8e2d5;color:#72501b;font-weight:850;background-size:cover;background-position:center}.applicantCell span:last-child{display:grid;gap:2px}.applicantCell small,.roleLine{display:block;color:#5b6470;font-size:.68rem}.profilePct{display:inline-flex;min-width:48px;justify-content:center;padding:5px 8px;border-radius:999px;background:#f2f4f7;font-weight:800}.rowMenu{position:relative}.rowMenu summary{width:38px;height:38px;display:grid;place-items:center;border-radius:8px;list-style:none;cursor:pointer;font-size:1.15rem}.rowMenu summary::-webkit-details-marker{display:none}.rowMenu>div{position:absolute;right:0;top:40px;z-index:60;min-width:190px;display:grid;padding:6px;border:1px solid #d9dde2;border-radius:10px;background:#fff;box-shadow:0 12px 28px rgba(16,19,29,.15)}.rowMenu a,.rowMenu button{padding:9px;border:0;border-radius:7px;background:none;text-align:left;color:#10131d;font:inherit}.rowMenu a:hover,.rowMenu button:hover{background:#f2f4f7}.rowMenu .danger{color:#7a1e2c}.applicationMobileList{display:none}.adminEmpty{padding:28px;border:1px dashed #cfd4da;border-radius:14px;background:#fff}.adminEmpty h3,.adminEmpty p{margin:0 0 8px}.adminEmpty p{color:#5b6470}.modalBackdrop{position:fixed;inset:0;z-index:200;display:grid;place-items:center;padding:20px;background:rgba(16,19,29,.56)}.confirmModal{width:min(580px,100%);max-height:88vh;overflow:auto;padding:24px;border-radius:16px;background:#fff}.confirmModal h2{margin:7px 0 10px}.confirmModal>p{color:#48515e;line-height:1.55}.affectedList{display:grid;gap:4px;padding:10px;border-radius:10px;background:#f6f7f9;font-size:.72rem}.messagePreview{display:grid;gap:7px;margin-top:14px}.messagePreview small{font-size:.62rem;font-weight:850;letter-spacing:.08em;color:#72501b}.messagePreview textarea{width:100%;box-sizing:border-box;border:1px solid #cfd4da;border-radius:10px;padding:10px;line-height:1.5}.messagePreview p{margin:0;padding:12px;border-radius:10px;background:#fff8e8;color:#48515e;font-size:.76rem;line-height:1.55}.confirmModal .actions{justify-content:flex-end;margin-top:16px}
      @media(max-width:1000px){.applicationToolbar{grid-template-columns:1fr 1fr 1fr}}
      @media(max-width:760px){.applicationTableWrap{display:none}.applicationMobileList{display:grid;gap:8px}.applicationMobileList article{padding:12px;border:1px solid #d9dde2;border-radius:12px;background:#fff}.mobileAppTop{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:10px;align-items:center}.mobileAppTop div{display:grid;gap:3px}.mobileAppTop small{color:#5b6470}.mobileAppMeta{display:flex;align-items:center;gap:8px;margin-top:10px;padding-left:30px;font-size:.7rem;color:#5b6470}.applicationBulkBar{top:118px}.applicationToolbar{grid-template-columns:1fr 1fr}}
      @media(max-width:480px){.applicationToolbar{grid-template-columns:1fr}.applicationBulkBar strong{position:sticky;left:0;background:#10131d;padding-right:8px}.rowMenu>div{position:fixed;right:16px}}
    `}</style>
  </div>;
}
