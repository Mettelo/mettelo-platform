import type { Metadata } from 'next';
import { createPublicSupabaseClient } from '@/lib/supabase/public';

export const metadata:Metadata={title:'Events',description:'Join Mettelo Data & AI AMAs, workshops, showcases, build sessions and professional conversations.'};
export const dynamic='force-dynamic';

type EventItem={id:string;title:string;event_type:string;summary:string|null;starts_at:string;ends_at:string|null;location_label:string|null;registration_url:string|null;replay_url:string|null;status:string};

export default async function EventsPage(){
  const supabase=createPublicSupabaseClient();
  let events:EventItem[]=[];let loadError=false;
  if(supabase){const result=await supabase.from('events').select('id,title,event_type,summary,starts_at,ends_at,location_label,registration_url,replay_url,status').in('status',['published','completed']).order('starts_at',{ascending:true});if(result.error)loadError=true;else events=(result.data||[]) as EventItem[];}else loadError=true;
  const now=Date.now();
  const upcoming=events.filter(e=>e.status==='published'&&new Date(e.starts_at).getTime()>=now);
  const past=events.filter(e=>e.status==='completed'||new Date(e.starts_at).getTime()<now);

  return <>
<section className="hero"><div className="shell heroGrid"><div><div className="eyebrow">Mettelo Events</div><h1>Meet the people. Learn the context. Show the work.</h1><p className="heroLead">AMAs, workshops, project showcases, build sessions and focused professional conversations — published only when the date and destination are confirmed.</p><div className="actions"><a className="button dark" href="#upcoming">See upcoming events →</a></div></div><aside className="heroPanel"><span className="chip">LIVE EVENT SYSTEM</span><h3 style={{marginTop:18}}>A public event means registration is real.</h3><p>Admin can draft an event privately, but it cannot be published without a registration destination. Afterward, the same record can hold the replay.</p><div className="path"><span>Publish</span><span>Register</span><span>Attend</span><span>Replay</span></div></aside></div></section>

<section className="section" id="upcoming"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Upcoming events</div><h2>{upcoming.length?`Join what is happening next.`:'No placeholder dates.'}</h2></div><p>Only confirmed public sessions appear here.</p></div>
{loadError?<div className="panel emptyState"><h3>Event data is temporarily unavailable.</h3><p>Please try again later or subscribe for event updates.</p></div>:upcoming.length?<div className="eventList">{upcoming.map(item=>{const date=new Date(item.starts_at);return <article className="eventCard" key={item.id}><div className="eventDate"><span>{date.toLocaleDateString('en-GB',{month:'short',year:'numeric'}).toUpperCase()}</span><strong>{date.getDate()}</strong></div><div><span className="chip">{item.event_type.replace('_',' ').toUpperCase()}</span><h3 style={{margin:'10px 0 6px'}}>{item.title}</h3>{item.summary&&<p>{item.summary}</p>}<small>{date.toLocaleString('en-GB',{dateStyle:'medium',timeStyle:'short'})}{item.location_label?` · ${item.location_label}`:''}</small></div>{item.registration_url?<a className="button dark" href={item.registration_url} target="_blank" rel="noopener noreferrer">Register →</a>:<span className="chip">REGISTRATION REVIEW</span>}</article>})}</div>:<div className="panel emptyState"><h3>No public event is open for registration right now.</h3><p>When a session is confirmed and published, it appears here automatically.</p><div className="actions"><a className="button dark" href="/newsletter">Get event alerts →</a><a className="button ghost" href="/contact">Propose a session</a></div></div>}
</div></section>

<section className="section softSection"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Event formats</div><h2>Every format has a job.</h2></div><p>Events create connection, context or proof — not calendar activity for its own sake.</p></div><div className="grid4"><article className="card"><span className="chip">AMA</span><h3 style={{marginTop:18}}>Career reality</h3><p>Applications, interviews, progression and practitioner Q&A.</p></article><article className="card"><span className="chip">WORKSHOP</span><h3 style={{marginTop:18}}>Practical capability</h3><p>Focused sessions around real workflows, communication and delivery.</p></article><article className="card"><span className="chip">SHOWCASE</span><h3 style={{marginTop:18}}>Project proof</h3><p>Teams explain what they built, what changed and what each person owned.</p></article><article className="card"><span className="chip">BUILD</span><h3 style={{marginTop:18}}>Working sessions</h3><p>Build sprints, open-source orientations and collaborative problem-solving.</p></article></div></div></section>

{past.length>0&&<section className="section"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Past events</div><h2>Useful sessions keep creating value.</h2></div><p>Replays appear only where a real recording is available and permission is clear.</p></div><div className="grid3">{past.slice().reverse().map(item=><article className="card featureCard" key={item.id}><div><span className="chip">{item.event_type.replace('_',' ').toUpperCase()}</span><h3 style={{marginTop:18}}>{item.title}</h3>{item.summary&&<p>{item.summary}</p>}</div>{item.replay_url?<a className="button ghost" href={item.replay_url} target="_blank" rel="noopener noreferrer">Watch replay →</a>:<span className="chip">NO PUBLIC REPLAY</span>}</article>)}</div></div></section>}

<section className="section compact"><div className="shell"><div className="ctaBand"><div><div className="cardNumber">SPEAK · HOST · PARTNER</div><h2>Have a session useful to serious Data & AI professionals?</h2><p>Bring a practitioner topic, project showcase or partnership idea with a clear audience and outcome.</p></div><div className="actions"><a className="button dark" href="/contact">Propose an event →</a></div></div></div></section>
</>;
}
