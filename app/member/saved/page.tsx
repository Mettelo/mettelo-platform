import Link from 'next/link';
import {redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';

export const dynamic='force-dynamic';
type SavedRow={saved_at:string;projects:{id:string;title:string;summary:string;status:string;visibility:string}|null};

export default async function MemberSavedPage(){
  const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/signin?next=%2Fmember%2Fsaved');
  const {data,error}=await supabase.from('saved_projects').select('saved_at,projects(id,title,summary,status,visibility)').eq('user_id',user.id).order('saved_at',{ascending:false});
  const rows=((data||[]) as unknown as SavedRow[]).filter(row=>row.projects);
  return <div className="msPage"><header className="msHero"><div className="msEyebrow">EXPLORE · SAVED</div><h1>Saved</h1><p>Return to member projects you bookmarked before deciding whether to apply. Saving a project never creates an application.</p></header>{error?<section className="msEmpty" role="alert"><h2>Saved projects are temporarily unavailable</h2><p>Nothing has been changed. Refresh to try again.</p></section>:rows.length?<section className="msGrid" aria-label="Saved projects">{rows.map(row=>{const project=row.projects!;return <article key={project.id}><span className="msStatus">Saved project</span><h2>{project.title}</h2><p>{project.summary}</p><small>Saved {new Date(row.saved_at).toLocaleDateString('en-GB')}</small><Link className="msButton msPrimary" href={`/member/discover/${project.id}`}>View project</Link></article>})}</section>:<section className="msEmpty"><h2>No saved projects yet</h2><p>Use Save project in Discover or Member Project Detail to keep a project here for later.</p><Link className="msButton msPrimary" href="/member/discover">Discover projects</Link></section>}<section className="msOpportunities"><div><div className="msEyebrow">SAVED OPPORTUNITIES</div><h2>Looking for saved roles?</h2><p>Employment opportunities remain a separate journey from member project participation.</p></div><Link className="msButton" href="/member/saved-opportunities">View saved opportunities</Link></section><style>{`.msPage{width:min(100%,1180px);margin:0 auto;color:#111318}.msEyebrow{font-family:var(--font-plex-mono),ui-monospace,monospace;text-transform:uppercase;letter-spacing:.11em;font-size:10px;color:#72551e;font-weight:700}.msHero{padding:10px 0 25px;border-bottom:1px solid #d8dde3}.msHero h1{margin:8px 0 11px;font-size:clamp(40px,5vw,58px);letter-spacing:-.05em}.msHero p{max-width:760px;margin:0;color:#59636f;line-height:1.66}.msGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:20px}.msGrid article,.msEmpty,.msOpportunities{background:#fff;border:1px solid #d8dde3;border-radius:16px;padding:18px}.msGrid article{display:grid;gap:10px}.msGrid h2,.msEmpty h2,.msOpportunities h2{margin:0;font-size:21px}.msGrid p,.msEmpty p,.msOpportunities p{margin:0;color:#59636f;line-height:1.55}.msGrid small{color:#68727d}.msStatus{width:max-content;padding:5px 9px;border-radius:999px;background:#f4f0e7;font-size:10px;font-weight:800}.msButton{min-height:44px;padding:0 15px;border:1px solid #b8c0c9;border-radius:10px;background:#fff;color:#111318;display:inline-flex;align-items:center;justify-content:center;text-decoration:none;font-size:13px;font-weight:800;width:max-content;max-width:100%}.msPrimary{background:#111318;border-color:#111318;color:#fff}.msEmpty{margin-top:20px;display:grid;gap:10px}.msOpportunities{margin-top:28px;background:#e9e3d7;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:20px;align-items:center}.msButton:focus-visible{outline:3px solid #173f8f;outline-offset:3px}@media(max-width:1024px){.msGrid{grid-template-columns:1fr}.msOpportunities{grid-template-columns:1fr}}@media(max-width:480px){.msHero h1{font-size:36px}.msButton{width:100%}}`}</style></div>;
}
