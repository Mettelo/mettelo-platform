import type { Metadata } from 'next';
import { createPublicSupabaseClient } from '@/lib/supabase/public';
import OpportunityBoard from '@/components/OpportunityBoard';
import './opportunities.css';

export const metadata:Metadata={title:'Opportunities',description:'Explore current Data & AI jobs, volunteering, fellowships and internships selected by Mettelo.'};
export const dynamic='force-dynamic';

type Opportunity={id:string;title:string;organisation:string|null;opportunity_type:string;summary:string|null;location:string|null;eligibility:string|null;source_url:string|null;official_application_url:string|null;closes_at:string|null;published_at:string|null;data_ai_relevance_score:number|null;remote_scope:string|null;source_organisation:string|null};

export default async function OpportunitiesPage(){
  const supabase=createPublicSupabaseClient();let opportunities:Opportunity[]=[];let loadError=false;
  if(supabase){const result=await supabase.from('opportunities').select('id,title,organisation,opportunity_type,summary,location,eligibility,source_url,official_application_url,closes_at,published_at,data_ai_relevance_score,remote_scope,source_organisation').eq('status','published').eq('access_level','public').eq('data_ai_relevance_status','high').order('published_at',{ascending:false}).limit(500);if(result.error)loadError=true;else opportunities=(result.data||[]) as Opportunity[];}else loadError=true;
  return <>
    <section className="hero"><div className="shell heroGrid"><div><div className="eyebrow">Mettelo Talent · Data & AI opportunities</div><h1>Find relevant opportunities without digging through a noisy job board.</h1><p className="heroLead">Search current Data & AI roles by company, location and opportunity type. Mettelo automatically excludes weak matches before they reach this page.</p><div className="actions"><a className="button dark" href="#opportunity-feed">Browse current roles →</a></div></div><aside className="heroPanel"><span className="chip">DATA & AI ONLY</span><h3 style={{marginTop:18}}>A narrower feed is more useful.</h3><p>Listings must pass Mettelo&apos;s Data & AI relevance threshold before publication. Admin can remove any listing that does not meet the standard.</p><div className="path"><span>Discover</span><span>Score</span><span>Publish</span><span>Act</span></div></aside></div></section>
    <section className="section" id="opportunity-feed"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Current opportunities</div><h2>{opportunities.length?`${opportunities.length} current Data & AI opening${opportunities.length===1?'':'s'}.`:'No relevant openings are live right now.'}</h2></div><p>Ten listings per page, with search and filters to get to the right opportunity faster.</p></div>{loadError?<div className="panel emptyState"><h3>Opportunity data is temporarily unavailable.</h3><p>Please try again shortly.</p></div>:opportunities.length?<OpportunityBoard items={opportunities}/>:<div className="panel emptyState"><h3>No Data & AI opportunities are currently published.</h3><p>The feed stays empty rather than showing roles that fail the relevance threshold.</p><div className="actions"><a className="button dark" href="/newsletter">Get opportunity alerts →</a></div></div>}</div></section>
    <section className="section softSection"><div className="shell"><div className="sectionHead"><div><div className="eyebrow">Mettelo standard</div><h2>Relevant first. Volume second.</h2></div><p>Automated discovery is useful only when the public feed remains focused.</p></div><div className="grid3"><article className="card"><span className="chip">RELEVANCE</span><h3 style={{marginTop:18}}>Data & AI threshold</h3><p>Strong role-title and technical-signal matching is required before automated publication.</p></article><article className="card"><span className="chip">FRESHNESS</span><h3 style={{marginTop:18}}>Recent openings</h3><p>Discovery prioritises current listings rather than building a stale archive of expired jobs.</p></article><article className="card"><span className="chip">CONTROL</span><h3 style={{marginTop:18}}>Admin removal</h3><p>Mettelo admins retain the final ability to remove a listing immediately if it does not fit.</p></article></div></div></section>
  </>;
}
