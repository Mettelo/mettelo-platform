'use client';

import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';

type Project={id:string;title:string;summary:string;problem_statement:string|null;status:string;visibility:string;project_type:string;partner_name:string|null;team_size_threshold:number|null;duration_weeks:number|null;weekly_commitment:string|null;application_deadline:string|null;applications_open?:boolean|null};
type Run={id:string;run_number:number;status:string;required_team_size:number;filled:number;has_started:boolean}|null;
type Lifecycle='publish_pilot'|'publish_open'|'publish_recruiting'|'pause_intake'|'resume_intake'|'unpublish'|'archive';

export default function AdminProjectDetailActions({project,currentRun=null}:{project:Project;currentRun?:Run}){
 const router=useRouter();const[editing,setEditing]=useState(false);const[confirming,setConfirming]=useState<'delete'|'start'|null>(null);const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');
 async function patch(changes:Record<string,unknown>){setBusy(true);setMessage('');try{const r=await fetch('/api/admin/projects',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id:project.id,...changes})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to update project.');setMessage('Project updated.');router.refresh();return true}catch(error){setMessage(error instanceof Error?error.message:'Unable to update project.');return false}finally{setBusy(false)}}
 async function lifecycle(action:Lifecycle){await patch({action})}
 async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();const data:Record<string,unknown>=Object.fromEntries(new FormData(event.currentTarget).entries());if(await patch(data))setEditing(false)}
 async function runConfirm(){if(!confirming)return;setBusy(true);setMessage('');try{if(confirming==='start'){if(!currentRun)throw new Error('No Partner team is ready to start.');const r=await fetch('/api/project-team-lifecycle',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'start',project_id:project.id,project_run_id:currentRun.id})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to start Partner project.');setMessage('Partner project started.');setConfirming(null);router.refresh();return}const r=await fetch('/api/admin/projects',{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({id:project.id})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to delete project.');router.push('/admin/project-operations/projects');router.refresh()}catch(error){setMessage(error instanceof Error?error.message:'Unable to complete action.')}finally{setBusy(false)}}
 const partnerCanStart=project.project_type==='partner'&&currentRun&&!currentRun.has_started&&currentRun.status==='forming'&&currentRun.filled>=currentRun.required_team_size;
 const published=project.visibility==='public'&&project.status!=='draft';
 return <div className="projectDetailActions"><button className="button ghost" type="button" onClick={()=>setEditing(true)}>Edit details</button>{project.status==='draft'&&<><button className="button primary" type="button" disabled={busy} onClick={()=>lifecycle('publish_pilot')}>Publish as Pilot</button>{project.project_type==='open'?<button className="button dark" type="button" disabled={busy} onClick={()=>lifecycle('publish_open')}>Publish Open</button>:<button className="button dark" type="button" disabled={busy} onClick={()=>lifecycle('publish_recruiting')}>Publish recruiting</button>}</>}{published&&project.applications_open!==false&&<button className="button ghost" type="button" disabled={busy} onClick={()=>lifecycle('pause_intake')}>Pause intake</button>}{published&&project.applications_open===false&&<button className="button dark" type="button" disabled={busy} onClick={()=>lifecycle('resume_intake')}>Resume intake</button>}{published&&<button className="button ghost" type="button" disabled={busy} onClick={()=>lifecycle('unpublish')}>Unpublish</button>}<a className="button ghost" href={`/admin/project-operations/team-formation?project=${project.id}`}>Team formation</a>{partnerCanStart&&<button className="button primary" type="button" onClick={()=>setConfirming('start')}>Start Partner project</button>}<details className="detailOverflow"><summary aria-label="More project actions">⋯</summary><div><button type="button" disabled={busy||project.status==='archived'} onClick={()=>lifecycle('archive')}>Archive</button><button className="danger" type="button" onClick={()=>setConfirming('delete')}>Delete</button></div></details><div className="formStatus" role="status" aria-live="polite">{message}</div>{editing&&<div className="modalBackdrop"><form className="editModal" role="dialog" aria-modal="true" aria-labelledby="edit-project-heading" onSubmit={save}><span className="eyebrow">Edit project</span><h2 id="edit-project-heading">Project details</h2><p className="lifecycleNote">Status, visibility and application intake are governed separately. Editing content cannot accidentally publish, close or reopen a project.</p><label>Title<input name="title" required maxLength={180} defaultValue={project.title}/></label><label>Summary<textarea name="summary" required maxLength={900} rows={4} defaultValue={project.summary}/></label><label>Brief / problem statement<textarea name="problem_statement" maxLength={4000} rows={8} defaultValue={project.problem_statement||''}/></label><div className="editGrid"><label>Project type<select name="project_type" required defaultValue={project.project_type}><option value="open">Open Project · repeatable</option><option value="partner">Partner Project · single engagement</option></select></label><label>Team size required<input name="team_size_threshold" type="number" min={1} max={50} defaultValue={project.team_size_threshold||5}/></label><label>Duration (weeks)<input name="duration_weeks" type="number" min={1} max={104} defaultValue={project.duration_weeks||''}/></label><label>Weekly commitment<input name="weekly_commitment" maxLength={120} defaultValue={project.weekly_commitment||''}/></label></div><label>Partner name<input name="partner_name" maxLength={180} defaultValue={project.partner_name||''}/></label><label>Application deadline<input name="application_deadline" type="datetime-local" defaultValue={project.application_deadline?new Date(project.application_deadline).toISOString().slice(0,16):''}/></label><div className="actions"><button className="button ghost" type="button" onClick={()=>setEditing(false)}>Cancel</button><button className="button dark" type="submit" disabled={busy}>{busy?'Saving…':'Save details'}</button></div></form></div>}{confirming&&<div className="modalBackdrop"><section className="confirmModal" role="dialog" aria-modal="true" aria-labelledby="confirm-project-action"><span className="eyebrow">Confirm action</span><h2 id="confirm-project-action">{confirming==='start'?'Start this Partner project?':'Delete this project permanently?'}</h2><p>{confirming==='start'?'The team has reached its required size. Partner projects never auto-start; this explicit action activates the single engagement.':'This is irreversible. Mettelo will block deletion when project history exists.'}</p><div className="actions"><button className="button ghost" type="button" onClick={()=>setConfirming(null)}>Cancel</button><button className={`button ${confirming==='delete'?'dangerButton':'dark'}`} type="button" disabled={busy} onClick={runConfirm}>{busy?'Working…':confirming==='start'?'Start project':'Delete permanently'}</button></div></section></div>}<style jsx>{`.projectDetailActions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.projectDetailActions>.button{min-height:44px}.detailOverflow{position:relative}.detailOverflow summary{width:44px;height:44px;display:grid;place-items:center;border:1px solid #cfd4da;border-radius:10px;background:#fff;cursor:pointer;list-style:none}.detailOverflow summary::-webkit-details-marker{display:none}.detailOverflow>div{position:absolute;right:0;top:48px;z-index:80;display:grid;min-width:160px;padding:6px;border:1px solid #d9dde2;border-radius:10px;background:#fff;box-shadow:0 12px 28px rgba(16,19,29,.15)}.detailOverflow button{padding:10px;border:0;border-radius:7px;background:none;text-align:left}.detailOverflow .danger{color:#7a1e2c}.modalBackdrop{position:fixed;inset:0;z-index:220;display:grid;place-items:center;padding:20px;background:rgba(16,19,29,.58)}.editModal,.confirmModal{width:min(680px,100%);max-height:90vh;overflow:auto;box-sizing:border-box;padding:24px;border-radius:16px;background:#fff}.editModal{display:grid;gap:12px}.editModal h2,.confirmModal h2{margin:6px 0}.lifecycleNote{margin:0;padding:10px;border-radius:9px;background:#fbf7ee;color:#5b6470;font-size:.72rem}.editModal label{display:grid;gap:5px;font-size:.68rem;font-weight:800;color:#5b6470}.editModal input,.editModal select,.editModal textarea{width:100%;box-sizing:border-box;border:1px solid #cfd4da;border-radius:9px;padding:10px;background:#fff;color:#10131d}.editGrid{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.confirmModal p{color:#48515e;line-height:1.55}.dangerButton{background:#7a1e2c!important;color:#fff!important}.actions{justify-content:flex-end;margin-top:6px}@media(max-width:650px){.projectDetailActions{display:grid;grid-template-columns:1fr 1fr}.projectDetailActions>.button{width:100%}.editGrid{grid-template-columns:1fr}.modalBackdrop{padding:12px}}`}</style></div>
}
