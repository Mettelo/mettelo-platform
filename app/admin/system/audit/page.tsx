import type {Metadata} from 'next';
import {redirect} from 'next/navigation';
import {createServerSupabaseClient} from '@/lib/supabase/server';
import {serviceDb} from '@/lib/project-flow';
import {hasAdminCapability} from '@/lib/admin-capabilities';

export const metadata:Metadata={title:'Admin Audit Log | Mettelo',description:'Read-only operational history for privileged Mettelo Admin actions.'};
export const dynamic='force-dynamic';

type AuditRow={id:string;actor_user_id:string;actor_email:string|null;capability:string|null;action:string;resource_type:string;resource_id:string|null;result:'success'|'failure'|'denied';reason:string|null;created_at:string};
type SearchParams={actor?:string;action?:string;resource_type?:string;result?:string};

function clean(value:unknown,max=180){return String(value||'').trim().slice(0,max)}
function formatDate(value:string){try{return new Intl.DateTimeFormat('en-GB',{dateStyle:'medium',timeStyle:'short',timeZone:'Europe/London'}).format(new Date(value))}catch{return value}}

export default async function AdminAuditPage({searchParams}:{searchParams:Promise<SearchParams>}){
  const auth=await createServerSupabaseClient();const {data:{user}}=await auth.auth.getUser();
  if(!user)redirect('/signin');if(!hasAdminCapability(user,'system.audit.read'))redirect('/member');
  const params=await searchParams;const actor=clean(params.actor);const action=clean(params.action,120);const resourceType=clean(params.resource_type,120);const result=clean(params.result,20);
  const db=serviceDb();let rows:AuditRow[]=[];let sourceError=false;
  if(db){
    let query=db.from('admin_audit_log').select('id,actor_user_id,actor_email,capability,action,resource_type,resource_id,result,reason,created_at').order('created_at',{ascending:false}).limit(100);
    if(actor)query=query.eq('actor_user_id',actor);if(action)query=query.eq('action',action);if(resourceType)query=query.eq('resource_type',resourceType);if(['success','failure','denied'].includes(result))query=query.eq('result',result);
    const {data,error}=await query;if(error)sourceError=true;else rows=(data||[]) as AuditRow[];
  }else sourceError=true;

  return <section className="section softSection"><div className="shell adminAudit"><header className="auditHead"><div><div className="eyebrow">Admin · System · Audit Log</div><h1>Audit log</h1><p>Read-only history for privileged operational changes. Audit records cannot be edited from Admin.</p></div><a className="button ghost" href="/admin">Back to Overview</a></header>
    <form className="auditFilters" method="get" aria-label="Filter audit history"><label>Actor ID<input name="actor" defaultValue={actor} placeholder="User UUID"/></label><label>Action<input name="action" defaultValue={action} placeholder="e.g. content.publish"/></label><label>Resource type<input name="resource_type" defaultValue={resourceType} placeholder="e.g. project"/></label><label>Result<select name="result" defaultValue={result}><option value="">All results</option><option value="success">Success</option><option value="failure">Failure</option><option value="denied">Denied</option></select></label><div className="filterActions"><button className="button" type="submit">Apply filters</button><a className="button ghost" href="/admin/system/audit">Clear</a></div></form>
    {sourceError?<div className="auditState" role="alert"><strong>Audit history is unavailable.</strong><p>The Admin data service could not load this history. No empty-state assumption has been made.</p></div>:rows.length===0?<div className="auditState"><strong>No audit events match these filters.</strong><p>Clear or change the filters to inspect other operational history.</p></div>:<div className="auditTableWrap" tabIndex={0} aria-label="Scrollable audit history table"><table><caption>{rows.length} most recent matching Admin audit event{rows.length===1?'':'s'}, limited to 100.</caption><thead><tr><th scope="col">Time</th><th scope="col">Actor</th><th scope="col">Action</th><th scope="col">Resource</th><th scope="col">Result</th><th scope="col">Reason</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><time dateTime={row.created_at}>{formatDate(row.created_at)}</time></td><td><strong>{row.actor_email||'Admin user'}</strong><small>{row.actor_user_id}</small>{row.capability&&<small>{row.capability}</small>}</td><td><code>{row.action}</code></td><td><strong>{row.resource_type}</strong>{row.resource_id&&<small>{row.resource_id}</small>}</td><td><span className={`auditResult ${row.result}`}>{row.result}</span></td><td>{row.reason||'—'}</td></tr>)}</tbody></table></div>}
  </div><style>{`
    .adminAudit{padding-bottom:48px}.auditHead{display:flex;align-items:end;justify-content:space-between;gap:24px}.auditHead h1{margin:6px 0;font-size:clamp(2rem,4vw,3.25rem)}.auditHead p{margin:0;max-width:720px;color:#5b6470}.auditFilters{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:24px 0;padding:16px;border:1px solid #d7dce2;border-radius:14px;background:#fff}.auditFilters label{display:grid;gap:6px;font-size:.72rem;font-weight:800}.auditFilters input,.auditFilters select{min-height:44px;width:100%;padding:9px 10px;border:1px solid #b8c0ca;border-radius:9px;background:#fff;color:#10131d}.filterActions{grid-column:1/-1;display:flex;gap:8px;flex-wrap:wrap}.auditState{padding:24px;border:1px solid #d7dce2;border-radius:14px;background:#fff}.auditState strong{font-size:1rem}.auditState p{margin:6px 0 0;color:#5b6470}.auditTableWrap{max-width:100%;overflow-x:auto;border:1px solid #d7dce2;border-radius:14px;background:#fff}.auditTableWrap:focus-visible{outline:3px solid #1e40af;outline-offset:3px}.auditTableWrap table{width:100%;min-width:900px;border-collapse:collapse}.auditTableWrap caption{padding:12px 14px;text-align:left;color:#5b6470;font-size:.72rem}.auditTableWrap th,.auditTableWrap td{padding:12px 14px;border-top:1px solid #e5e8ec;text-align:left;vertical-align:top}.auditTableWrap th{font-size:.68rem;text-transform:uppercase;letter-spacing:.05em;color:#5b6470;background:#f8f9fa}.auditTableWrap td{font-size:.75rem}.auditTableWrap td small{display:block;margin-top:4px;color:#66707e;overflow-wrap:anywhere}.auditTableWrap code{font-size:.72rem;overflow-wrap:anywhere}.auditResult{display:inline-flex;min-height:28px;align-items:center;padding:4px 8px;border:1px solid currentColor;border-radius:999px;font-size:.68rem;font-weight:850;text-transform:capitalize}.auditResult.success{color:#166534;background:#f0fdf4}.auditResult.failure{color:#9a3412;background:#fff7ed}.auditResult.denied{color:#991b1b;background:#fef2f2}@media(max-width:1024px){.auditFilters{grid-template-columns:1fr 1fr}}@media(max-width:600px){.auditHead{align-items:start;display:grid}.auditFilters{grid-template-columns:1fr}.filterActions{grid-column:auto}.filterActions .button{flex:1;justify-content:center;min-height:44px}.auditTableWrap{border-radius:10px}}@media(prefers-reduced-motion:reduce){.adminAudit *{scroll-behavior:auto!important}}
  `}</style></section>;
}
