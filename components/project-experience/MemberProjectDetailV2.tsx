'use client';

import Link from 'next/link';
import {useMemo,useState} from 'react';
import SaveProjectButton from '@/components/SaveProjectButton';
import ProjectMemberCanonicalSections from '@/components/project-experience/ProjectMemberCanonicalSections';
import type {MemberProjectState} from '@/lib/member-project-journey';
import type {ProjectExperienceModel} from '@/lib/project-experience-model';
import styles from './MemberProjectDetailV2.module.css';

type Role={id:string;title:string;description:string|null;skills:string[];openings:number;remaining:number|null;available:boolean;responsibilities:string[];recommendedSkills:string[];experienceExpectation:string|null;weeklyCommitment:string|null;applicationRequirements:string|null};
type Props={model:ProjectExperienceModel;state:MemberProjectState;stateLabel:string;stateCopy:string;primaryAction:{label:string;href:string}|null;applicationReady:boolean;profileCompletion:number;applicationMissing:string[];roleAvailabilityKnown:boolean;saved:boolean;roles:Role[]};

function date(value:string|null){if(!value)return'Not published';const parsed=new Date(value);if(Number.isNaN(parsed.getTime()))return'Not published';return new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',year:'numeric'}).format(parsed)}
function openStatus(state:MemberProjectState,deadline:string|null){if(state!=='open_eligible')return null;if(!deadline)return'Open for applications';const days=Math.ceil((new Date(deadline).getTime()-Date.now())/86400000);return days>0&&days<=7?`Closing soon · ${days} ${days===1?'day':'days'}`:'Open for applications'}

export default function MemberProjectDetailV2({model,state,stateLabel,stateCopy,primaryAction,applicationReady,profileCompletion,applicationMissing,roleAvailabilityKnown,saved,roles}:Props){
 const project=model.project;
 const available=useMemo(()=>roles.filter(role=>role.available),[roles]);
 const [selectedRole,setSelectedRole]=useState(available[0]?.id||'');
 const selected=roles.find(role=>role.id===selectedRole)||null;
 const applyHref=selected?`/member/discover/${project.id}/apply?role=${encodeURIComponent(selected.id)}`:`/member/discover/${project.id}/apply`;
 const status=openStatus(state,project.applicationDeadline)||stateLabel;
 const action=state==='open_eligible'?{label:'Apply for a role',href:'#roles'}:primaryAction;
 const workingModel=project.locationType?.replaceAll('_',' ')||project.location||'Project-specific';
 return <div className={styles.page}>
  <a className={styles.skip} href="#member-project-main">Skip to project details</a>
  <nav className={styles.breadcrumb} aria-label="Project breadcrumb"><Link href="/member">My Mettelo</Link><span aria-hidden="true">/</span><Link href="/member/discover">Discover</Link><span aria-hidden="true">/</span><strong>{project.title}</strong></nav>
  <header className={styles.hero} aria-labelledby="member-project-title"><div className={styles.heroMain}><div className={styles.eyebrow}>MEMBER PROJECT DETAIL</div><h1 id="member-project-title">{project.title}</h1><p>{project.summary}</p><div className={styles.pills} aria-label="Project characteristics"><span className={`${styles.pill} ${state==='open_eligible'?styles.open:''}`}>{status}</span>{project.durationWeeks&&<span className={styles.pill}>{project.durationWeeks} {project.durationWeeks===1?'week':'weeks'}</span>}{project.weeklyCommitment&&<span className={styles.pill}>{project.weeklyCommitment}</span>}<span className={styles.pill}>{workingModel}</span></div></div>
   <aside className={styles.decision} aria-labelledby="member-decision-title"><div className={styles.eyebrow}>YOUR DECISION</div><h2 id="member-decision-title">{state==='open_eligible'?'Ready to contribute?':stateLabel}</h2><p>{stateCopy}</p><dl className={styles.meta}><div><dt>Duration</dt><dd>{project.durationWeeks?`${project.durationWeeks} ${project.durationWeeks===1?'week':'weeks'}`:'Not published'}</dd></div><div><dt>Commitment</dt><dd>{project.weeklyCommitment||'Not published'}</dd></div><div><dt>Open roles</dt><dd>{roleAvailabilityKnown?available.length:'Checking availability'}</dd></div><div><dt>Applications close</dt><dd>{date(project.applicationDeadline)}</dd></div></dl>{state==='ineligible'&&!applicationReady&&<div className={styles.readiness} role="status"><strong>{applicationMissing.length} profile requirement{applicationMissing.length===1?'':'s'} remaining</strong><span>Profile completion: {profileCompletion}%</span></div>}{action&&<Link className={styles.primaryButton} href={action.href}>{action.label}</Link>}<SaveProjectButton projectId={project.id} initialSaved={saved}/>{state==='ineligible'&&!applicationReady&&<Link className={styles.secondaryButton} href={`/member/profile?next=${encodeURIComponent(`/member/discover/${project.id}`)}`}>Complete profile requirements</Link>}</aside>
  </header>
  <main id="member-project-main" className={`${styles.layout} pdv2Layout`}><div className={styles.content}><ProjectMemberCanonicalSections model={model}/>
   <section className="pdv2Section" id="roles" aria-labelledby="member-roles-heading"><div className="pdv2Eyebrow">08 · CHOOSE YOUR CONTRIBUTION</div><h2 id="member-roles-heading">Open project roles</h2><p>Choose the responsibility you can realistically own. Capacity is checked against the existing project-role service before application.</p>{roles.length?<div className="pdv2RoleGrid">{roles.map(role=><article className={`pdv2Role${selectedRole===role.id?' pdv2RoleSelected':''}${!role.available?' pdv2RoleClosed':''}`} key={role.id}><div className="pdv2RoleTop"><div><h3>{role.title}</h3>{role.description&&<p>{role.description}</p>}</div><span className="pdv2RoleStatus">{role.available?'Available':'Unavailable'}</span></div>{role.skills.length>0&&<div className="pdv2Pills">{role.skills.slice(0,8).map(skill=><span className="pdv2Pill" key={skill}>{skill}</span>)}</div>}<div className="pdv2RoleMeta">{role.remaining!==null&&<span>{role.remaining} of {role.openings} place{role.openings===1?'':'s'} remaining</span>}</div>
    {(role.responsibilities.length>0||role.experienceExpectation||role.weeklyCommitment||role.applicationRequirements||role.recommendedSkills.length>0)&&<div className="pdv2RoleDetails">{role.responsibilities.length>0&&<div className="pdv2RoleDetail"><span>Responsibilities</span><ul>{role.responsibilities.slice(0,4).map(item=><li key={item}>{item}</li>)}</ul></div>}{(role.experienceExpectation||role.weeklyCommitment)&&<div className="pdv2RoleDetail"><span>Role expectation</span>{role.experienceExpectation&&<p>{role.experienceExpectation}</p>}{role.weeklyCommitment&&<p><strong>Commitment:</strong> {role.weeklyCommitment}</p>}</div>}{role.recommendedSkills.length>0&&<div className="pdv2RoleDetail"><span>Recommended skills</span><div className="pdv2Pills">{role.recommendedSkills.slice(0,6).map(skill=><span className="pdv2Pill" key={skill}>{skill}</span>)}</div></div>}{role.applicationRequirements&&<div className="pdv2RoleDetail"><span>Application requirement</span><p>{role.applicationRequirements}</p></div>}</div>}
    {state==='open_eligible'&&<button className="pdv2Button" type="button" disabled={!role.available} aria-pressed={selectedRole===role.id} onClick={()=>setSelectedRole(role.id)}>{selectedRole===role.id?'Selected':'Select this role'}</button>}</article>)}</div>:<div className="pdv2Empty" role="status"><strong>No participation roles are published yet.</strong><span>The project remains reviewable, but application stays unavailable until a real role exists.</span></div>}{!roleAvailabilityKnown&&<div className="pdv2Empty" role="status"><strong>Role capacity could not be confirmed safely.</strong><span>Applying is temporarily unavailable until Mettelo can confirm capacity.</span></div>}</section>
   <section className="pdv2Section" aria-labelledby="member-next-heading"><div className="pdv2Eyebrow">09 · WHAT HAPPENS NEXT</div><h2 id="member-next-heading">Your project journey</h2><div className="pdv2Timeline"><div><span>1</span><p><strong>Review the canonical brief</strong>Understand the problem, objectives, resources, deliverables and quality bar.</p></div><div><span>2</span><p><strong>Choose one available role</strong>Select the responsibility you can credibly own.</p></div><div><span>3</span><p><strong>Submit your application</strong>Your profile readiness, evidence and role selection remain governed by the existing application workflow.</p></div><div><span>4</span><p><strong>Track the decision</strong>Your status and next action remain visible in My Mettelo.</p></div><div><span>5</span><p><strong>Deliver in Mettelo Lab</strong>Accepted project members move into the authorised workspace with the same canonical project definition.</p></div></div></section>
  </div><aside className={styles.side} aria-label="Project actions">{state==='open_eligible'&&<section className={styles.sideCard}><div className={styles.eyebrow}>SELECTED ROLE</div><h3>{selected?.title||'Choose an available role'}</h3><p>{selected?.weeklyCommitment?`Role commitment: ${selected.weeklyCommitment}`:'You can change your role before submitting the application.'}</p><Link className={`${styles.primaryButton}${selected?'':` ${styles.disabled}`}`} aria-disabled={!selected} tabIndex={selected?undefined:-1} href={selected?applyHref:'#'}>{selected?`Apply as ${selected.title}`:'Choose a role to apply'}</Link></section>}<section className={styles.sideCard}><div className={styles.eyebrow}>PROJECT LINKS</div><Link href={`/projects/${project.id}`}>View public project page ↗</Link><Link href="/member/discover">Back to Discover</Link><Link href="/member/recommended">Recommended for you</Link></section></aside></main>
  {state==='open_eligible'&&<div className={styles.mobileCta}><div><strong>{selected?.title||'Choose a role'}</strong><span>{selected?.weeklyCommitment||project.weeklyCommitment||'Review commitment before applying'}</span></div><Link className={`${styles.primaryButton}${selected?'':` ${styles.disabled}`}`} aria-disabled={!selected} tabIndex={selected?undefined:-1} href={selected?applyHref:'#'}>Apply</Link></div>}
 </div>
}
