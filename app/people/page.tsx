import type {Metadata} from 'next';
import Link from 'next/link';
import {createPublicSupabaseClient} from '@/lib/supabase/public';
import PaginatedCardGrid from '@/components/PaginatedCardGrid';
import {calculateMemberReadiness} from '@/lib/member-readiness';
import {getPublicWebsiteCmsPage} from '@/lib/website-pages-cms';

export const metadata:Metadata={title:'People',description:'Discover Data & AI professionals through their capability, interests and contribution on Mettelo.'};
export const revalidate=60;
type PublicProfile={id:string;full_name:string|null;headline:string|null;current_job_title:string|null;location:string|null;professional_area:string|null;skills:string[]|null;bio:string|null;linkedin_url:string|null;github_url:string|null;portfolio_url:string|null;avatar_url:string|null};type PublicProof={user_id:string};
export default async function PeoplePage(){
 const supabase=createPublicSupabaseClient();let people:PublicProfile[]=[];const proofCountByUser=new Map<string,number>();if(supabase){const {data}=await supabase.from('profiles').select('id,full_name,headline,current_job_title,location,professional_area,skills,bio,linkedin_url,github_url,portfolio_url,avatar_url').eq('is_public',true).order('updated_at',{ascending:false}).limit(96);people=((data||[]) as PublicProfile[]).filter(person=>calculateMemberReadiness({profile:person}).publicProfileReadiness.ready).slice(0,48);const ids=people.map(person=>person.id);if(ids.length){const proof=await supabase.from('contributions').select('user_id').eq('verification_status','verified').eq('is_public',true).in('user_id',ids);for(const row of (proof.data||[]) as PublicProof[])proofCountByUser.set(row.user_id,(proofCountByUser.get(row.user_id)||0)+1)}}
 const copy=(await getPublicWebsiteCmsPage('people')).values;
 return <div className="peoplePage"><section className="hero"><div className="shell heroGrid"><div><div className="eyebrow">{copy.hero_eyebrow}</div><h1>{copy.hero_title}</h1><p className="heroLead">{copy.hero_lead}</p></div><aside className="heroPanel"><span className="chip">PUBLIC PROFILES</span><h3 style={{marginTop:18}}>Professional context that is useful.</h3><p>Profiles appear only when the member explicitly chooses public visibility and completes the information required for a useful public profile.</p></aside></div></section><section className="section"><div className="shell"><div className="sectionHead"><div><div className="cardNumber">MEMBER DIRECTORY</div><h2>{copy.section_title}</h2></div><p>{copy.section_body}</p></div>{people.length?<PaginatedCardGrid label="people" className="peopleGrid">{people.map(person=>{const name=person.full_name?.trim()||'Mettelo member';const role=person.headline||person.current_job_title||person.professional_area||'Data & AI professional';const proofCount=proofCountByUser.get(person.id)||0;return <article className="personCard" key={person.id}><div className="personAvatar" style={person.avatar_url?{backgroundImage:`url(${person.avatar_url})`,backgroundSize:'cover',backgroundPosition:'center'}:undefined}>{person.avatar_url?'':name.charAt(0).toUpperCase()}</div><div><h3>{name}</h3><p>{role}{person.location?` · ${person.location}`:''}</p>{person.skills?.length?<div className="metaRow">{person.skills.slice(0,5).map(skill=><span className="metaPill" key={skill}>{skill}</span>)}</div>:null}<span className="proofSignal">Public profile ready</span>{proofCount>0?<span className="proofSignal">{proofCount} verified Proof {proofCount===1?'contribution':'contributions'}</span>:null}<div style={{marginTop:14}}><Link className="linkArrow" href={`/people/${person.id}`}>View profile →</Link></div></div></article>})}</PaginatedCardGrid>:<div className="emptyState"><h3>{copy.empty_title}</h3><p>{copy.empty_body}</p><Link className="linkArrow" href={copy.final_cta_href}>{copy.final_cta_label}</Link></div>}</div></section><section className="section softSection"><div className="shell"><div className="ctaBand"><div><div className="cardNumber">YOUR METTELO IDENTITY</div><h2>Turn capability into a profile people can understand.</h2><p>Add your professional identity, skills, goals, availability and evidence, then decide whether you want to be discoverable.</p></div><Link className="button dark" href={copy.final_cta_href}>{copy.final_cta_label}</Link></div></div></section></div>;
}
