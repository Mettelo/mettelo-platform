export default async function WorkspaceNavigationResponsiveGate({searchParams}:{searchParams:Promise<{width?:string}>}){const params=await searchParams;const width=Math.min(1440,Math.max(320,Number(params.width)||390));return <main style={{padding:12,background:'#e8e9ec',minHeight:'100vh'}}><p style={{margin:'0 auto 8px',maxWidth:width,font:'700 12px system-ui'}}>Workspace navigation gate · {width}px</p><iframe title={`Workspace navigation preview at ${width}px`} src="/dev/workspace-navigation-preview" style={{display:'block',width,maxWidth:'100%',height:1100,margin:'auto',border:'1px solid #cfd3da',borderRadius:12,background:'#fff'}}/></main>}
