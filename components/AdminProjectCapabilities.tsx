'use client';

import {useEffect,useMemo,useState} from 'react';

type Capability={id:string;slug:string;name:string;capability_type:'technical'|'professional';is_active:boolean};
type Selected={capability_id:string};

export default function AdminProjectCapabilities({projectId}:{projectId:string}){
  const [items,setItems]=useState<Capability[]>([]),[selected,setSelected]=useState<string[]>([]),[query,setQuery]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
  async function load(){setBusy(true);try{const r=await fetch(`/api/admin/capability-paths?mode=project-capabilities&project_id=${encodeURIComponent(projectId)}`);const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to load project capabilities.');setItems(b.items||[]);setSelected((b.selected||[]).map((item:Selected)=>item.capability_id))}catch(error){setMessage(error instanceof Error?error.message:'Unable to load project capabilities.')}finally{setBusy(false)}}
  useEffect(()=>{void load()},[projectId]);
  const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return items.filter(item=>!q||[item.name,item.slug,item.capability_type].join(' ').toLowerCase().includes(q))},[items,query]);
  const groups=[{key:'technical',label:'Technical capabilities'},{key:'professional',label:'Professional capabilities'}] as const;
  function toggle(id:string){setSelected(current=>current.includes(id)?current.filter(value=>value!==id):[...current,id])}
  async function save(){setBusy(true);setMessage('');try{const r=await fetch('/api/admin/capability-paths',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action:'set-project-capabilities',project_id:projectId,capability_ids:selected})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Unable to save project capabilities.');setMessage('Canonical project capabilities saved.')}catch(error){setMessage(error instanceof Error?error.message:'Unable to save project capabilities.')}finally{setBusy(false)}}
  return <section className="detailPanel capabilityEditor"><div className="panelSplit"><div><span className="eyebrow">CAPABILITY TAXONOMY</span><h2>Technical & professional capabilities</h2></div><button className="button ghost" type="button" disabled={busy} onClick={save}>{busy?'Working…':'Save capabilities'}</button></div><p>Use the canonical capability taxonomy. Technologies remain Tools; analytical techniques remain Methods.</p><label className="capabilitySearch"><span className="srOnly">Search capabilities</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search capability taxonomy"/></label>{items.length?<div className="capabilityGroups">{groups.map(group=><fieldset key={group.key}><legend>{group.label}</legend><div>{filtered.filter(item=>item.capability_type===group.key).map(item=><label key={item.id}><input type="checkbox" checked={selected.includes(item.id)} onChange={()=>toggle(item.id)}/><span>{item.name}</span></label>)}{filtered.filter(item=>item.capability_type===group.key).length===0&&<small>No matching {group.label.toLowerCase()}.</small>}</div></fieldset>)}</div>:!busy&&<div className="capabilityEmpty"><strong>No canonical capabilities yet</strong><p>Capability values will be normalised before workbook import rather than created as uncontrolled project text.</p></div>}<div className="formStatus" role="status" aria-live="polite">{message}</div><style jsx>{`.capabilityEditor{display:grid;gap:12px}.capabilityEditor>p{margin:0;color:#5b6472;font-size:.72rem;line-height:1.5}.capabilitySearch input{width:100%;box-sizing:border-box;min-height:42px;padding:8px 10px;border:1px solid #cfd4da;border-radius:9px;background:#fff}.capabilityGroups{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.capabilityGroups fieldset{min-width:0;margin:0;padding:12px;border:1px solid #e3e6e9;border-radius:11px;background:#fbfaf7}.capabilityGroups legend{padding:0 5px;font-size:.66rem;font-weight:850;color:#5b6472}.capabilityGroups fieldset>div{display:grid;gap:5px;max-height:240px;overflow:auto}.capabilityGroups label{display:flex;align-items:flex-start;gap:8px;padding:6px;border-radius:7px;font-size:.72rem}.capabilityGroups label:hover{background:#fff}.capabilityGroups input{margin-top:2px}.capabilityGroups small{padding:8px;color:#7a8290}.capabilityEmpty{padding:13px;border:1px dashed #d9dde2;border-radius:10px}.capabilityEmpty p{margin:4px 0 0;color:#5b6472;font-size:.7rem}.formStatus{min-height:18px;color:#5b6472;font-size:.69rem}@media(max-width:680px){.capabilityGroups{grid-template-columns:1fr}}`}</style></section>;
}
